import { defineConfig } from "@playwright/test";
export default defineConfig({ testDir: ".", testMatch: "call-avatar.human.spec.ts", workers: 1, retries: 0, timeout: 60000, reporter: "list", outputDir: "../../.planning/call-avatar-extension1", use: { trace: "retain-on-failure" }, projects: [{ name: "desktop", use: { viewport: { width: 1440, height: 900 } } }, { name: "mobile", use: { viewport: { width: 390, height: 844 } } }] });
