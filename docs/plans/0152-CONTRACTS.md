# Murage 0.1.52 frozen shared contracts (K0)

Written by lane K0 on `lane/0152-K0`. Every 0.1.52 lane follows these shapes. If a lane truly has to change one, it records why in its report and in its commit message. Line references are approximate; search for the named symbol.

## Shared types

| File | Exports | Consumers |
|---|---|---|
| `shared/workspace-files.ts` | `OUTPUT_NAMESPACE`, `WORKSPACE_FILES_ROUTES`, bounds (`WORKSPACE_LIST_PAGE_SIZE` 200, `WORKSPACE_SEARCH_MAX_ENTRIES` 2,000, `WORKSPACE_SEARCH_MAX_DEPTH` 8, `WORKSPACE_TEXT_MAX_BYTES` 2 MiB), `WorkspaceScopeRef`, opaque `FileRevision`, `WorkspaceRootInfo`, `WorkspaceEntry`, list/search request and response (`cursor`, `incomplete`), `WorkspaceReadResult`/`ReadResult` (`revision`, `encoding`, `bom`, `newline`), `WorkspaceWriteRequest`/`WriteRequest` (`baseRevision`, `requestId`, `content`, `bom`), `SaveReceipt`, save-version request and response, `WORKSPACE_FILE_ERROR_STATUS` (code → HTTP status), and the guards `isWorkspaceScopeRef`, `isFileRevision`, `isWorkspaceRelativePath`, `isOutputNamespacePath`, `isWorkspaceFileErrorCode` | R3-T1, R3-T2, F4-T1..T4, F4-T7 |
| `shared/output-publication.ts` | `LocalOutputReceipt` (`producer`: `shell-output`, `image-operation` or `assistant-image`; `stage`: `retained`, `attached`, `registered` or `failed`), `OUTPUT_PUBLICATION_LIMITS` (25 MiB per file, 20 files per turn, 1 host card per turn), `OUTPUT_STAGE_TRANSITIONS`/`canAdvanceOutputStage`, and the guards | R3-T3, R3-T4 (C2), F5-T5 |
| `shared/media-assets.ts` | `MediaAsset` (exactly as in the media design), `MediaAssetRef`, `MEDIA_ROUTES`, `MediaResolveResponse`, capability constants (`MEDIA_CAPABILITY_QUERY_PARAM` `cap`, `MEDIA_CAPABILITY_TTL_MS` 10 min, token shape `mc1.<claims>.<hmac>`), `MEDIA_BYTES_RESPONSE_HEADERS`, `redactMediaCapability`, `MEDIA_PLAYABLE_MIMES` (U-28), `ImageReferenceSource` (attachment, artifact+sha256, or workspace-relative), `ResolvedImageReference {id, sha256, mime, bytes}`, `IMAGE_REFERENCE_LIMITS` (4 references, 10 MiB each, 20 MiB total), `isImageReferenceSource` | F5-T1..T5 |
| `shared/artifacts.ts` | `Artifact.producer?: OutputProducer`. The field is absent for manual or tool registration and for older rows. | R3-T2, R3-T3 |
| `server/contracts.ts` | `ProviderStopResult` (`{closeConfirmed:true}` or `{closeConfirmed:false, reason:"timeout"\|"stop-failed"}`). `interruptTurn` now returns `Promise<void \| ProviderStopResult>`. Optional `awaitTurnTeardown?(threadId, turnId)`. `stopCloseConfirmed(result)` returns `undefined` for legacy `void`. | R1-T2 (ACP core, Pi), R1-T8 |
| `electron/main-trust.mjs` | `isOwnedMainSender(event, {window, origin})` checks the same webContents, top frame, non-detached frame and exact origin, and fails closed. `mainRendererOrigin({packaged, serverPort, devUrl})`. | R2-T1, S1-T3, F4-T5 |
| `shared/questions.ts` (K0 amendment, ASK2) | `QuestionOption {label, description?}`, `QuestionSpec {id, question, header?, options[], multiSelect, allowOther, secret?}`, `QuestionAnswer {id, selected[], other?}`, `QUESTION_LIMITS` (4 questions, 10 options, 2,000-char free text), `QUESTION_TIMEOUT_MS` (30 min), `isQuestionCard`, `questionsForCard`, `questionFromChoices`, `answerComplete`, `answersAsMessage` | ASK2 (Claude), later question lanes (Codex, Fuigo/ACP, Pi, Telegram) |

## Routes (all wired in `server/index.ts`; modules own the behaviour)

| Route | Module entry point | Authority | K0 answer |
|---|---|---|---|
| `/api/workspace-files` and `/api/workspace-files/*` | `workspaceFilesRoute(request, deps)` in `server/workspace-files.ts` | Desktop proof. The entry is in `DESKTOP_AUTHORITY_ROUTES` (GET/POST), and the module hides every non-desktop call (404). | desktop 501 `{code:"not-implemented"}` |
| `/api/media`, `/api/media/resolve`, other `/api/media/*` | `mediaAssetsRoute(request, deps)` in `server/media-assets.ts` | Desktop proof (`DESKTOP_AUTHORITY_ROUTES`) | desktop 501 |
| `/api/media/bytes/<assetId>?cap=` (GET/HEAD, single range) | same | U-03 capability, deliberately exempt from the desktop header, which media elements cannot send. Until F5-T1 issues capabilities, non-desktop callers get 404. | desktop 501 |
| `POST /api/internal/resolve-image-reference` | `resolveImageReferenceRoute(request, claim, deps)` | Active `agents` internal capability. Identity (`botId`/`threadId`/`generation`) comes from the claim, never from the body. | 501; GET 405 |

**Added by F4-T5** (no existing shape changed): `WORKSPACE_FILES_ROUTES.native` — `GET /api/workspace-files/native?botId&threadId&path` → `WorkspaceNativeFile {scope, relativePath, root, revision, bytes, identity}`. The editor design lists native open/reveal as a workspace-files operation with an owner-bound capability, and the Electron main process needs an authorized file identity it can revalidate; the K0 table had no route for it. Desktop-only like the rest of the prefix, same root/link/hard-link/private-file policy as `read`, no bytes and no size limit. Only `electron/workspace-file-actions.mjs` consumes it; the renderer never sees `root` or `identity`.

Modules return `DelegatedResult {status, headers?, body?, bytes?, stream?}`, and `sendDelegated` writes it (`server/route-delegation.ts`). `deps` is `{dataDir, database, store, artifactScopes}` (`featureRouteDeps` in index.ts). Adding a dependency is a one-line change to that object. The routing lines stay as they are.

## Hooks (`server/output-publication.ts`, created once as `outputPublisher`)

- `beforeDispatch({botId, threadId, runId: dispatchClaimId, workspaceRoot: cwd, managed})` runs synchronously at dispatch, after the workspace claim and before the provider sees the prompt. `managed` is true only for the Murage-managed dedicated task workspace (not a custom folder, not a cloud run). It must not throw.
- `publishTerminalOutputs(event)` runs once per `turn.completed` in the main event fold, right after terminal-message marking. It sits **outside** the direct-run lease-release block, so R1-T2 and R3-T3 edit different hunks. It should not reject; index.ts logs a redacted message if it does.

## Schema (`server/artifacts.ts` `initializeArtifacts`, idempotent, frozen)

- `artifacts` gains nullable `producer TEXT` and `publication_id TEXT`, added with guarded `ALTER TABLE`. The existing `run_id TEXT NOT NULL` stays the run column, with `''` meaning unknown. The INSERT now names its columns.
- New `output_publications(id PK, producer, bot_id, thread_id, run_id, path_token, sha256, mime, bytes, stage, artifact_id?, attachment_id?, message_id?, error_category?, created_at, updated_at, UNIQUE(producer,bot_id,thread_id,run_id,path_token,sha256))` with indexes on `(bot_id,thread_id,run_id)` and `(stage,updated_at)`. Stage and producer values are validated in code with the shared guards (no SQL CHECK), so the enums can grow in a later release without a table rebuild.
- **No lane edits the schema after K0.**

## Rules

- **OUTPUT_NAMESPACE (U-02).** Only `outputs/` directly beneath a Murage-managed dedicated task workspace qualifies (`ensureTaskWorkspace`/per-bot default; never a custom cwd, project or HOME). Take a snapshot of `(name,size,mtime,dev/ino)` in `beforeDispatch`, and diff it on `turn.completed ok:true`. Only regular files of at most 25 MiB qualify, at most 20 per turn, with one host card per turn. Cancelled or failed turns leave receipts `retained` and do not auto-register them. Files outside the namespace stay discoverable without invented authorship.
- **Media capability (U-03, U-04).** Use a short-lived HMAC over asset + revision + surface `desktop`, with a 10-minute TTL, sent as `?cap=` on the loopback byte route. Redact it in logs with `redactMediaCapability`. Send `Referrer-Policy: no-referrer`. The reusable desktop secret never appears in a URL. All new routes are desktop-only in 0.1.52, and the companion allowlist stays default-deny (pinned by `companion/test/routes.test.ts`).
- **Image references.** Resolve every reference to pinned bytes before approval or billing. One failed reference fails the whole request, with no silent subset, no provider fallback and no numeric seed substitution.
- **Close-confirmed stop (A2).** A requested kill or an acknowledged cancel is not closure. Keep resource ownership until `closeConfirmed:true`, and retain it on timeout.
- **i18n (U-25).** Add English strings to `src/locales/en.json` only, under a key prefix for your area. Do not edit other locales or `source-hashes.json`; Q1-T4 regenerates them. An `i18n:check` failure caused only by that is expected.
- **Dependencies.** Tiptap `@tiptap/{core,pm,react,starter-kit,markdown,extension-table,extension-task-list,extension-list}` is pinned exactly to `3.31.3` in K0 (React peer `^17 || ^18 || ^19`). No other lane changes `pnpm-lock.yaml` except Q1-T3, and Q1-T3 runs alone.

## Amendment A1 — agent questions (ASK1 + ASK2, `0152-ASK-USER-RESEARCH.md` §4)

Additive only; every field is optional and older consumers keep working.

- **`RuntimeEvent` `request.opened`** gains `questions?: QuestionSpec[]` next to the existing `questionTool?: true` (ASK1). `summary` stays the first question's text and `choices` its option labels, so voice (`CallView`/`GroupCallView`) and older clients degrade gracefully.
- **`ProviderAdapter.respondToRequest`** decision gains `answers?: QuestionAnswer[]`. `message` stays the same answer as plain text for drivers that read one string. A `deny` on a question is an honest "no answer" (skipped), never a refusal — the broker used to reject it, which left the engine waiting out its whole timeout.
- **`OptionCardData`** (`server/store.ts` and the renderer's mirror in `src/state/store.tsx`) gains `questions`, `answers`, `expired`, `sentAsMessage`, `unattended`. Cards persist in `messages.db`, so a question survives a reload; a boot sweep marks anything still open as Expired.
- **Routes.** `POST /api/bots/:id/respond` and `POST /api/threads/:id/respond` accept `answers` (validated against the persisted card) and `behavior:"skip"`, plus `{behavior:"answer", answers, sentAsMessage:true}` to record an expired question's late answer after it went out through the ordinary composer route. A question is never answered with `allow`.
- **`DecisionKind`** gains `question-expired` and `question-skipped`. An answer itself is conversation, not authorization, and is never logged; a secret question's answer is never written to the transcript or the log.
- **Normalizer.** `server/question-normalize.ts` owns every engine mapping (`fromClaude`, `fromMuragebox`, `toClaudeAnswers`, `toMessageText`, `validateAnswers`, `parseAnswers`, `answersFromMessage`, `recordableAnswers`, `QUESTION_NOTES`). Engine input becomes a card only once it is bounded and well formed. Later question lanes add `from*`/`to*` pairs here rather than in a driver. ASK3 added `fromCodex`/`toCodexAnswers`/`codexNoAnswers`, `fromFuigo`/`toFuigoAnswers`, `fromElicitationForm`/`toElicitationContent`/`fromElicitationUrl` and `fromPi`/`toPiValue`; no shared shape changed. Telegram's `TelegramApproval` gained an optional `questions` and `TelegramApprovalActions` an optional `answer` (ASK3) — Telegram-internal, additive.

## Merge-order hotspots (lane map §2)

| File | Owners (region) | Risk | Order / mitigation |
|---|---|---|---|
| `server/index.ts` | K0 stubs · R1-T2 (interruptDirectThread, terminal lease block) · R3-T3 (hook body, `artifactScopes` runId, register) · R1-T6 (approval join) · R3-T4 and F5-T4 (image MCP route) · R2-T3 (provider replace, conditional) | H | K0 → R1-T2 → R1-T6 → R3-T3 → R3-T4 → F5-T4. R3-T3 fills the module, not the call sites. |
| `server/index.test.ts` | R3-T3 (register case) · F1-T3, R3-T4, F5-T4 (image MCP scenario) | H | One owner at a time for the image scenario: F1-T3 → R3-T4 → F5-T4. Others add new `it()` blocks. |
| `electron/main.mjs` | R2-T2 · R2-T1 · R2-T4 · R2-T5 · R2-T3 · S1-T2 · S1-T3 (sweep) · F4-T5 | H | S1-T3 runs after R2 and S1-T2 merge, and F4-T5 after S1-T3. Everyone else uses `main-trust.mjs`. |
| `electron/preload.cjs` | S1-T3 · F4-T5 | M | F4-T5 after S1-T3 |
| `server/artifacts.ts` | K0 DDL · R3-T1 (export validators) · R3-T3 (register runId/producer) · F4-T1 (save-version) · F5-T1 (read) | M | All DDL is in K0 |
| `server/image-operations.ts` (+test) | R3-T4 · F5-T4 · F1-T4 | H (test) | R3-T4 → F5-T4 → F1-T4 |
| `server/image-generation.ts` | F1 lane · F5-T4 (only if needed) | L | single F1 lane |
| `server/drivers/agents-proxy.ts` | R3-T3 · F5-T4 | L | — |
| `src/components/ChatView.tsx` | R0-T1 · F5-T2 · U0-T1 · F4-T3 · F5-T3 | H | R0-T1 → F5-T2 → U0-T1 → F4-T3 |
| `src/components/ChatMarkdown.tsx` | U0-T3 · F4-T6 · F5-T2 · F5-T3 | M | U0-T3 → F5-T2 → F4-T6 → F5-T3 |
| `src/components/Files.tsx` | F5-T2 · R3-T2 · F4-T3 | H | F5-T2 → R3-T2 → F4-T3 |
| `src/components/AttachmentPreview.tsx` | F5-T2 · F5-T3 | M | serial in the media lanes |
| `src/state/store.tsx` | F4-T3 (+ R3-T2) | L | — |
| `server/drivers/acp/core.ts` | R1-T2 · R1-T8 · R1-T4 | H | same lane, serial |
| `server/drivers/pi.ts` | R1-T2 · R1-T5 · R1-T6 | L | merge R1-T5 and R1-T6 first |
| `server/drivers/claude.ts` | R1-T1 · R1-T4 | M | R1-T4 after R1-T1 |
| `server/procs.ts` | R1-T2 · R1-T8 | M | same lane |
| `server/contracts.ts` | K0 · R1-T2 | L | shape frozen here |
| `electron/secure-credential-state.mjs` | R2-T3 · R2-T5 | M | same lane |
| `electron/skill-recorder.mjs` | R2-T1 · R2-T4 | L | same lane |
| `companion/src/browser.ts`, `devices.ts` | S1-T4 · S1-T5 · S1-T7 | M | same lane |
| `installer/bin/murage.mjs` | F2-T1 · F2-T3 · F2-T4 | M | same lane |
| `cloudflare/composio-broker/src/index.ts` | F3-T1..T3 | M | same lane |
| `package.json` / `pnpm-lock.yaml` | K0 (Tiptap) · Q1-T3 · Q1-T4 · S1-T2 (optional script) | H (lockfile) | Tiptap lands in K0; Q1-T3 later and alone |
| `src/locales/*.json` + `source-hashes.json` | every UI task | H | English keys only, under a lane prefix; Q1-T4 regenerates once |
| `.github/workflows/ci.yml` | Q1-T1 · S1-T2 (optional) | L | — |

## F4-T1 notes (lane W_F4T1, no shared shape changed)

- **Routes served.** `GET read` (200 `WorkspaceReadResult`), `POST write` (200 `SaveReceipt`), `POST save-version` (201 `WorkspaceSaveVersionResponse`). A wrong method answers 400 `invalid-request` without reading a body; non-desktop callers still get the hidden 404.
- **Body bound.** `DelegatedRequest.readBody(maxBytes?)` gained an optional per-route bound (default unchanged at 1,000,000 bytes in `server/index.ts` `readBody`). `write` uses `WORKSPACE_WRITE_BODY_MAX_BYTES` (6 × 2 MiB + 64 KiB, because JSON escaping can grow text six-fold). `save-version` uses 64 KiB. Reason: the frozen 2 MiB text bound could not be saved through the old 1 MB body bound.
- **Bot-active hold.** `featureRouteDeps` gained `projectFolders: projectTurnLeases.folders`. Every local bot turn already holds a writer lease on its working folder. An overwrite takes a restore-mode lease on the workspace for its synchronous commit window: an overlapping bot turn answers 423 `bot-writing` (nothing written), and no bot turn can start inside the window. Without `projectFolders` an overwrite fails closed with `bot-writing`. `baseRevision: null` (Save a copy) is an exclusive create (`link`, never replaces) and is allowed while a bot works. External programs do not take Murage leases; the identity recheck just before `rename` narrows but cannot close that race.
- **`SaveReceipt.artifactId`.** Before every overwrite, the revision being replaced is kept as a saved version in Files through `registerArtifact` (idempotent, no producer/run). `artifactId` names that saved version. If it cannot be kept (507 `quota-exceeded`, or `write-failed`), nothing is overwritten. The kept copy must hash to the base revision's content digest (`registerArtifact` checks `expectedSha256` before it writes a blob or a row); otherwise the save answers `revision-conflict` and Files gains nothing — the same rule refuses a `save-version` whose file changed after its revision was checked (HZ1 follow-ups 1-2). A save of the unchanged bytes rewrites nothing and returns the same revision with no `artifactId`. A create has no prior revision and no `artifactId`.
- **Scope of writes.** Only `.md`/`.markdown`, ≤ 2 MiB including the BOM, well-formed Unicode (a lone surrogate is refused rather than written as U+FFFD), in a root Files already authorizes for that exact conversation (a managed workspace dispatch has not pinned yet is `scope-unavailable`). Files and folders are never created implicitly; the existing file mode is preserved and a new file is 0600. Reads accept any strict UTF-8 regular file ≤ 2 MiB. Hard-linked files answer `not-regular-file` (discovery gives them no revision). A file that changes while it is read answers `revision-conflict` without `currentRevision`.

## U1-T1 — upstream attribution and final disposition ledger (lane W_U1)

`NOTICE` now carries the Apache-2.0 attribution for every OpenMausBot change adapted into 0.1.52, with the exact upstream commit for each. The SHAs come from the lane commit bodies on `release/v0.1.52` and from `0152-UPSTREAM-RESEARCH.md` / `0152-UPSTREAM-SECURITY.md`.

**Taken (9 pull requests, 8 Murage commits).** All eight are ancestors of `release/v0.1.52`.

| Upstream | Upstream commit | Murage commit | Murage area | Kind |
|---|---|---|---|---|
| #987 | merge `391f0b2b4c8778360191820b6604fe0d4cad217f` | `e0332d62` (S1-T1) | `server/redact.ts` | adapted from source |
| #986 | merge `7aa86499bca77253d971c52826956d8c1bb639d9` | `42358cc4` (S1-T2) | `electron/app-permissions.mjs`, `electron/main.mjs` | adapted from source |
| #1023 | `a4352595a9d4d8b8df0722c3f8a5b05b6d0c25cb` | `1f708d31` (U0-T3) | `src/components/ChatMarkdown.tsx` | adapted from source |
| #762 | merge `7b73664c0ce3c72b9803347926d8bd2ada2e5f1f` (production `03d9fb3f4942259ecac44d7d94571bb53260d894`) | `f2878c5f` (U0-T2) | `src/components/Sidebar.tsx`, `src/lib/sidebar-selection.ts` | adapted from source |
| #767 | merge `1df832d3dc256461a90041c89bd51fce9425a495` (production `2eb4c7c5d32c85d8e0ae3e43d7f7861e81c2977f`) | `f2878c5f` (U0-T2) | same | adapted from source |
| #758 | merge `86b19df10a0aaebdc66f9da41c46f42d26dd3843` (earlier duplicate production `70a29ffd89eb17a8444af0941213a992a078a1cc`) | `a73d3346` (F3-T4) | `src/components/PluginsPanel.tsx` | adapted from source |
| #979 | merge `b5a8a1bbf9517dba9b9ec07e6b5b23bf65d052a9` | `b3c87e68` (F4-T6) | `src/lib/code-block.ts`, `src/components/ChatMarkdown.tsx` | adapted from source |
| #988 | `1e6737b0db71f65ec22644756be9ac2b7bf3e20f` | `692c0c99` (R0-T2) | `server/routines.ts` | schedule-preservation slice only, written independently |
| #920 | merge `368f653fecce3930f9a374a80ec1b3b9a27245d3` | `4b350ff3` (R0-T3) | `server/screen-frame-gate.ts` | namespace handling only, written independently |

A scan of every commit in `acaee1db..release/v0.1.52` for `OpenMausBot`/`upstream` finds no other upstream adaptation in this release, so the NOTICE list is complete for 0.1.52. Two of the nine were written independently against the upstream behaviour rather than copied: the NOTICE says so per item instead of claiming a uniform code lineage.

**Reviewed and not selected (ledger preserved, nothing silently dropped).** #836 Codex native instruction separation (needs a supported protocol/resume decision) · #1038 resume recovery (absence of `init` is not proof of a replayable state) · #1031 / #1037 context and CLI configuration changes · #996 Qwen configured routes · #994 RTL · #978 raw Markdown toggle · #1027 table normalization · #733 interval schedule windows · #849 / #856 / #857 Podman ownership, sandbox and font work · the new inspector, Verify, room-management and mobile features · the delegation-completion half of #988. U1-T2 (oldest-first detached routine order, the FIFO half of #988) stays a product policy choice and is **not selected** for 0.1.52 per U-27; nothing in this release changes detached traversal order, and no Murage change may be described as fixing channel FIFO.

**Framing rule for release copy.** Murage's common ancestor with OpenMausBot is the 0.1.44 source (`6140532e`). 0.1.52 adapts the nine items above and nothing else. Do not describe any upstream release as imported, and do not present the release-mirror SHA (`436271a`) as application source — the 0.1.71 app source is `32b47b03f8a327ce081f3bd33c55cab061078268`.

## F5-T3 notes (lane W_F5T3, no shared shape changed)

- **Player identity.** The renderer never turns a transcript path into a URL. `src/lib/media-resolve.ts` asks `GET /api/workspace-files/root`, `GET /api/workspace-files/list` (one directory, up to 5 pages) and `POST /api/media/resolve` with `{ref:{source:"workspace", scope, relativePath, revision}}`, the revision being the one discovery issued. A path outside the reported root, a link, a directory, a non-media file, another conversation's scope or any refusal leaves the caller's existing affordance (file chip, Save a copy) unchanged; `availability` `missing`/`changed`/`denied`/`unsupported` on an audio/video asset shows that reason beside it. Answers are shared per `(botId, threadId, path)` for 60 s.
- **Scopes offered.** ChatView passes `{botId: bot.id, threadId: bot.threadId}`; GroupView passes `{botId: message.from.botId, threadId: group.threadId}` for a member's message (the room-task scope `resolveWorkspaceRoot` authorizes) and nothing for an unattributed one. A person's own `<attached-file>` path is outside every workspace and stays a chip: the frozen `MediaAssetRef` has no user-file source, by design. On the server side `artifactScopes()` (server/index.ts) emits, per conversation, the working-folder scope **and** an R3-T4 `managedOutput: true` scope for the generated-images root, plus thread-less `threadAvailable: false` retained-artifact rows; `locateWorkspace` in `server/media-assets.ts` reads only the working folder (F5-T4's skip), and `src/lib/media-resolve.harness.test.ts` mirrors all three scope kinds so the joined test fails if that skip is lost.
- **Save a copy.** Every download link a card shows — beside the live player, on the cannot-play card and after a failure — renews through the same one-refresh path when the capability has lapsed by the clock, so a card that stopped playing never hands out a link the harness refuses.
- **Capability life.** `MediaPlayerCard` keeps the URL it was given until the element fails; if the failure is a network error, or the clock says the capability is within 15 s of `expiresAt`, it asks `LocalMedia` for one refresh (cache dropped, the three questions again) and continues from the same position. A decode (3) or codec (4, with a live capability) failure, or a second failure after a refresh, is reported and Save a copy stays. `MEDIA_CAPABILITY_TTL_MS` is unchanged.
- **Playback policy.** `preload="metadata"`, no `autoplay`, `playsInline` video; one Murage player at a time (module-level floor in `MediaPlayer.tsx`, released on pause/ended/unmount); unmount pauses, removes `src` and calls `load()` so the byte stream is released. `canPlayType` gates the element (`""` → unplayable card); the U-28 container set is the hint table `mediaHintForPath` in `src/lib/composer-attachments.ts`. Per-platform codec claims are not made; they are the WIN/MAC gate.

## F5-T5 notes (lane W_F5T5, no shared shape changed)

- **The joined proof.** `src/e2e/media-publication.human.spec.ts` (config `src/e2e/media-publication.config.ts`) runs against the real harness from `launchVerificationServer`, the real mounted agents MCP proxy, the real approval route and the real Vite app. Its `--import` instrumentation is fixture-only: the server suite's image provider fixture (`server/testing/search-fetch-preload.mjs`, receipt per call, no pass-through), a `FAKE_CLAUDE_FINISH_GATE_DIR`, and one extra instance `echo` (fake CLI in `slow` mode behind `FAKE_CLAUDE_REPLY_GATE`, dumping every turn) whose reply echoes the prompt so a workspace path reaches the transcript as a link.
- **What one approved `generate_image` leaves.** One transcript attachment, one committed attachment file, one retained original under `workspaces/<bot>/generated-images/<thread>/`, one `output_publications` row at `registered`, one saved Files version (`producer: image-operation`). The transcript thumbnail, the lightbox `src` and its Download link, `/api/attachments/<id>`, `/api/artifacts/<id>/download`, `/api/artifacts/<id>/native` (`sha256` and the verified saved-copy path), `/api/artifacts/<id>/preview`, both `/api/media/resolve` refs (attachment and artifact) and their capability byte URLs, `resolve_image_reference` by attachment id and by artifact+sha256, and `POST /api/media/reference` all name the same sha256. The three files (attachment, retained original, saved copy) are three distinct paths.
- **Recovery.** With `attachments/` made unwritable after the provider answered, the tool answers 409 "Image received and kept locally … (filesystem)", the receipt is `failed`/`filesystem`, nothing is attached or saved, and the receipt file shows the one call. After `fixture.restart()` the startup `resumePendingPublications` finishes it: second attachment, second saved version, receipt `registered`, and the provider receipt is byte-identical (zero calls).
- **Players.** A WAV written into the managed `outputs/` during a turn (after the dispatch snapshot, before the echo gate opens) is one `shell-output` receipt, one saved version (`kind: other`) and one host card; in chat every player over that link shares one `/api/media/bytes/<asset>?cap=` URL (206 on a range), and the player's Save a copy, the card's Download and `/api/artifacts/<id>/download` hand out the WAV's bytes. The other bot sees none of it, and the saved copy survives the workspace file changing (`sourceState: changed`).
- **Harness incident recorded, not fixed.** Intermittently (about one run in four on the shared 10-core machine, also reproduced by a plain HTTP probe with a 2.5 s pause before the first turn) a turn ends with `error: MEMORY_CONTEXT_REVOKED` before any engine process starts: a memory data write lands between bundle build and dispatch (`memory_meta.data_revision` advances by more than the turn's own writes) while the roster policy stays `granted`. The spec sends such a turn again after 1.5 s, at most three times, and annotates each retry (`harness-incident`) so the frequency stays visible. The memory owner (S2) should look at `server/index.ts` dispatch preparation around `buildMemoryBundleAfterReset` / `memoryReceipt.assertCurrent()` and at what writes memory data a few seconds after bot creation.
  - *RED2C disposition (Q1-T5 §4.1):* the write is the memory worker finishing the capture of a message in the same thread (the turn's own prompt, or the greeting/reply before it), which rolls the thread's checkpoint that the bundle selected. `server/memory/bundle.ts` `supersededThreadCheckpoint` now treats the thread's own checkpoint superseded by a newer active version as stale, not revoked, with every other check (tombstones, evidence sources, scope, policy revision, deletion epoch) intact; `server/turn-refused-at-acceptance-api.test.ts` reproduces the incident deterministically through the real server and proves the turn now runs. A roster/policy change during the window still revokes.

## LOCAL-MODELS notes (lane LM2, additive only)

- **`server/contracts.ts` `ModelCatalog.options[]`** gained two optional fields, both renderer-facing and both absent for every non-local row: `localServer?: string` (the server serving a local model, in the form the picker shows, e.g. "llama.cpp on seanbeast") and `localTools?: "pass" | "partial" | "failed"` (outcome of the last Local models tool test; absent means never tested). No existing field changed; every consumer that ignores them keeps working. Reason: spec V3 requires the picker's Local rail to name the server on the row and to mark a model whose tools test failed, and the picker only ever sees the catalog.
- **`AcpSupport.spawnArgs`** (`server/drivers/acp/core.ts`) takes an optional third `ctx` argument (`{ requestedModel?: string }`) so a driver can tell a local pick from a native turn at spawn time. Existing two-argument implementations are unaffected.

## F4-T3 notes (lane W_F4T3, no shared shape changed)

- **Pane state.** `AppState.workspacePane` (`src/lib/workspace-pane.ts`: `WorkspacePaneState`, `workspacePaneReducer`) holds `open`, `width`, `expanded`, `compactView`, `tabs`, `activeTabId`, `closeRequest`. Only the store action `{type:"workspacePane", action}` touches it; the right-slot toggles (settings, computer, inspector) leave it alone. A tab is `{id, scope, relativePath, mode: "preview"|"edit", pinned, dirty}` — the same scope + relative path identity the workspace-files routes use, never an absolute path. Tabs carry their own scope, so Files' *Open beside chat* and a saved version's *Open here* never change `selectedId`.
- **Tab rules.** A single click reuses the one unpinned, clean preview tab; *Keep open* (`pin`) and *Edit* make a tab persistent (`mode:"edit"` always pins). A dirty tab is never replaced; `close` on a dirty tab sets `closeRequest` and the pane asks (`force:true` closes). `setMode` back to preview is refused while dirty.
- **Layout.** Above `md` the pane is a rail on the right of `ChatView`'s new flex-row `<main>`; `clampWorkspaceWidth` keeps it ≥ 320 px and the chat ≥ 360 px (`WORKSPACE_PANE_MIN_CHAT_WIDTH`), with the width echoed to `localStorage` as a convenience only. `expanded` collapses the chat column (`data-collapsed`, `md:` only) without unmounting it. Below `md` the pane covers the chat (`max-md:absolute`) and `compactView` picks which one shows; the hidden one stays mounted, so drafts and the transcript's scroll survive *Back to chat*.
- **Editing.** One `DocumentSessionStore` + `MarkdownEditorController` (F4-T2/F4-T4) per edit tab, kept in the surface's registry for as long as the tab is open and disposed (draft flushed to IndexedDB) when the tab closes, leaves edit mode or the pane unmounts. The tab's `dirty` flag mirrors `hasUnsavedChanges` of its own session. Saves go through `writeWorkspaceFile` (`POST write`), which keeps the F4-T1 error `code`/`currentRevision` (`workspaceApi` throws `WorkspaceFileRequestError`), so a receipt settles only the tab whose `requestId` it answers; typing during a save and switching tabs are covered by `src/components/WorkspacePane.test.ts` and the browser proof.
- **External changes.** No watch route exists; an open document probes its parent folder's listing (`probeWorkspaceRevision`, ≤ 5 pages, every `WORKSPACE_PANE_PROBE_MS` = 5 s and on focus) and only when the listed revision differs reads the file and calls `controller.observeDisk` (clean → reload, dirty → conflict) or, for a preview, shows the new text with *Updated from the file on disk*. A vanished file shows *missing on disk* and keeps the text.
- **Previews.** Markdown renders through `ChatMarkdown` (scope-aware, same image policy as chat) with a Source view; HTML uses the same `artifactPreviewHtml` + `sandbox=""` frame as Files; text/code/CSV is an escaped `<pre>`; images resolve through `POST /api/media/resolve` with the listed `revision`; PDF/Office/archives/media are an honest open/download fallback and are never read. *Save this version* is the F4-T1 `save-version` route (disabled while dirty), *Save a copy* is an exclusive create (`baseRevision:null`), *Open in app* / *Show in folder* go through F4-T5's bridge with the listed entry.
- **i18n.** English keys under `workspacePane.*` only.

## STOPRESTORE notes (lanes STOPRESTORE1 + STOPRESTORE2, one shared code added)

- **The Stop → engine-close window.** The Claude driver's interrupt is "requested, not observed": the bot reads idle as soon as Stop returns, while the turn's project-folder writer lease is released only at the engine's terminal event (the CLI child closed). `ProjectTurnLeases.markStopRequested(generation)` (called by `interruptDirectThread`) marks that window; `acquireRestoreWhenStopped(owner, cwd, {timeoutMs})` acquires a restore-mode lease at once when the folder is free, waits for the release when every holder is such a stopped turn, and refuses at once for a live writer or another restore. Its conflict answer carries the registry's own `code` (`conflict`, `owner-in-use`, `invalid-path`, ...) so a caller can tell a busy folder from an unusable path. The bound is the engine's close budget, `providerCloseDeadlineMs()` (5 s, `MURAGE_PROVIDER_CLOSE_MS`). No stopped turn is ever pre-empted and no writer overlaps a held restore.
- **Checkpoint restore (STOPRESTORE1).** `POST /api/bots/:id/checkpoints/restore` answers 409 `restore_folder_in_use` at once for a live turn or another restore, and 409 `restore_stopped_turn_closing` ("A stopped turn is still closing … retry") when the only holders are stopped turns that did not close within the budget.
- **Workspace editor save (STOPRESTORE2).** `WorkspaceFilesDeps` gained `projectTurns` (the same `ProjectTurnLeases`) and `stoppedTurnCloseMs` (`providerCloseDeadlineMs`); `featureRouteDeps` passes both. `writeWorkspaceMarkdown` is now `async` (only the hold can wait; from the hold to the commit nothing yields, so no bot turn can start inside the window) and an overwrite's hold goes through `acquireRestoreWhenStopped`: a live turn is still 423 `bot-writing` at once; a stopped turn is waited for, then the save writes; at the bound the answer is the new shared code **`workspace_stopped_turn_closing`** (423, `WORKSPACE_FILE_ERROR_STATUS`), with a message that says to save again. After a wait the conversation must still resolve to and authorize the held folder, else `root-changed`. The editor (`MarkdownEditor` `saveErrorMessage`, key `markdownEditor.saveError.stoppedTurnClosing`) shows that refusal by name and keeps the text; `document-session` treats it as retryable like `bot-writing`. `save-version` and Save a copy (`baseRevision: null`) never take the hold and are unchanged. The code's underscore form was set by the finding that named it (`workspace_stopped_turn_closing`, parallel to `restore_stopped_turn_closing`); every other `WorkspaceFileErrorCode` stays kebab-case.
- **No Restore UI in 0.1.52 (audited).** No client calls `POST /api/bots/:id/checkpoints/restore` (`rg` over `src/`, `electron/`, `companion/`, `apps/`, `shared/` finds none). Neither `0152-LANE-MAP.md` nor `MURAGE-0152-PLAN.md` promises a Restore action, no 0149/0150 planning document (`GOAL-0150.md`, `STATE.md` and its 0150 history) mentions checkpoint restore at all, and the 0152 audit reports name `checkpoint` only as a git-history word; the only mention of a "restore preview/undo UI" is P20 of the pre-0.1.52 `MURAGE-PLAN.md` (Wave 6, never scheduled or executed, not carried into the 0.1.52 plan), and the feature's origin commit (`5326fa54`, "Per-turn workspace checkpoints with rollback") shipped it API-only ("Turns store nothing in v1 — the list endpoint is the timeline"). Checkpoint list and restore are therefore **API/agent-only in 0.1.52**: `GET /api/bots/:id/checkpoints?cwd=` and `POST /api/bots/:id/checkpoints/restore {cwd, hash}` are the contract, with the two 409 codes above, and a Restore action in the pane stays a candidate for a later release (P20).

## AUTOOP notes (lanes AUTOOP1 + AUTOOP2, no shared shape changed)

- **Auto inheritance (AUTOOP1).** `POST /api/internal/create-bot` gives a Chief-created operator `autoApprove` equal to the Chief's *human-enabled* Auto in the conversation making the call, and never from an unattended turn (`isUnattended(fromThreadId)`: webhook, channel automation, or a hop from one). The operator's `computer` is `"off"`, `approvePeerComms` and `composio` stay `false`, `alwaysAllow` is empty, and the response (and the Chief's `create_bot` tool result) carries `auto: true|false`. Nothing in the call's arguments can raise it: a Chief in Ask makes Ask operators.
- **Which Auto bit, precisely.** create-bot reads the **live** bit at the moment of the call — `store.projectBotForTask(chief.id, fromThreadId).autoApprove` for a 1:1 conversation (the task's own bit, not the profile's; `server/index.test.ts` "create_bot reads the Auto of the task that calls it" has a two-task Chief with the profile bit ON and task A in Ask, and is mutation-checked against a profile-bit resolver), and the profile bit for a channel conversation. The permission fold (`request.opened` → `autoVerdict`) reads the bot **as frozen in the run snapshot at dispatch** (`botForDirectThread` → `directRuns.get(threadId).snapshot`, with only `alwaysAllow` overlaid live). The two are the same human-enabled Auto but are read at different times, so they can differ mid-turn: the thread route (`PATCH /api/bots/:id/tasks/:threadId`) refuses settings changes while that thread runs, but the profile route (`PATCH /api/bots/:id`) still accepts `autoApprove` during a single-task bot's turn (its only run guard is `directRuns.forBot(id).length > 1`) and `store.patchBot` mirrors it into that task, and a channel Chief's profile bit can change at any time. A toggle during the turn therefore reaches the next `create_bot` immediately and reaches the Chief's own approvals only on its next turn. AUTOOP1's commit body said the two were "the same … the permission host reads"; the bit is the same, the read time is not.
- **Profile-level consent (AUTOOP2 finding 1).** Both routes that can switch Auto on apply one rule, `autoMountsLocalComputer(resolvedComputer)` (`server/local-routing.ts`, over `shouldMountLocalComputer`): an explicit `"local"` mounts this computer on macOS and Linux; a bot that never chose a computer (`undefined`, the "Auto" destination) mounts it on macOS; `"cloud"`, `"vm"`, `"browser"`, `"off"` never do. Where it mounts, switching Auto on needs `acknowledgeLocalAuto: true` in the same body (400 otherwise, error text names `acknowledgeLocalAuto`), the flag is never persisted, and the granted `(computer, autoApprove)` combination is the proof: re-asserting Auto or patching unrelated fields needs no re-ack, moving `undefined` ↔ `"local"` with Auto already granted is the same desktop and needs none, and leaving this computer ends the grant so coming back asks again. Before this the profile route only checked an explicit `"local"` or `null`, so a fresh Mac bot could be put in Auto on the person's own desktop at profile level with no warning. The renderer's Auto switches (`SettingsPanel` Auto switch, composer chip) decide with `autoNeedsLocalComputerWarning` + `localAutoHostPlatform` (`src/lib/local-computer.ts`), and the `ComputerPanel` destination grid with `computerSwitchNeedsLocalAutoWarning` — both over one `autoMountsThisComputer`, the renderer's copy of the server rule, which takes no provider-support input (FOLLOW3: the grid used to gate the warning on `localSelectable`, so a Mac bot on a provider without local-computer capability got a bare 400 instead of the dialog). All open `LocalComputerAutoWarning` rather than firing a PATCH the harness refuses; `src/e2e/auto-consent.human.spec.ts` proves the settings switch against the real harness, `src/components/ComputerPanel.test.ts` the grid's decision (`planComputerDestinationChange`). Tests that put a default-computer bot in Auto at profile level now send the acknowledgement (harmless where no warning is due).
- **The harness announces its platform (FOLLOW5, one additive field).** The server rule runs on the harness's `process.platform`, but in a plain browser (dev rig, browser door) the renderer had no desktop shell to ask and `localAutoHostPlatform` guessed from `navigator.userAgent` — the browser's machine, not the harness's. A Linux/Windows tab on a Mac harness got a bare 400 and no dialog (Playwright 1.57's "Desktop Chrome" device is a Windows UA, so the AUTOOP2 spec itself was red on a Mac), and a Mac tab on a Linux harness got a dialog the server never required. `GET /api/config` (and the `config` SSE frame, which replaces the renderer's config wholesale) now carries **`harness: { platform: process.platform }`** — `ConfigStatus.harness` in `src/state/store.tsx`, in `ConfigStatusFrame`/`configStatusFromFrame` so a push keeps it; both doors serve it; not a secret (the platform is in the routes' own error strings). It rides on the route the renderer already reads at startup — no second startup fetch. `localAutoHostPlatform(capabilities, { harness, userAgent })` (signature changed from `(capabilities, userAgent)`) prefers the announced platform over both the UA and the desktop shell's answer whenever present (`darwin`/`linux`/`win32`, anything else folded to `other`, never to a UA guess), and falls back to the previous rule — shell platform, else UA — only while the harness has not answered (`state.config` not loaded yet, or an older harness). All three decisions go through it: the composer chip and the SettingsPanel switch pass `state.config?.harness`; `planComputerDestinationChange` gained `harness?`. Tests: `src/lib/local-computer.test.ts` (non-Mac UA + darwin harness → warning; Mac UA + linux harness + computer undefined → none), `src/components/ComputerPanel.test.ts`, `server/index.test.ts` ("AUTOOP2 finding 1" asserts `harness` on both doors; "keeps native search keys write-only" asserts it on the `config` frame), and `src/e2e/auto-consent.human.spec.ts` gained a `test.use({ userAgent })` case that drives the real dialog from a browser whose UA names the *other* platform (Linux UA on a Mac harness → dialog then ack; Mac UA on a Linux harness → no dialog, PATCH lands without `acknowledgeLocalAuto`).

## STOP1 + STOP2 notes — a stopped turn is never a finished turn (no shared shape changed)

STOP1 made every engine settle a requested stop as `turn.completed { ok: true, stopReason: "cancelled" }` (ACP core, Pi, Claude, Codex, Antigravity, the API-chat and box engines; `server/drivers/acp/acp.test.ts` "interrupt settles a hung turn as cancelled", `server/drivers/pi.test.ts`, `server/drivers/claude.test.ts`, `codex.test.ts`, `antigravity.test.ts`, `boxagent.test.ts`, `openai-chat.test.ts`, `grok.test.ts`). `ok` therefore means "not an engine failure", never "the work finished". `server/turn-outcome.ts` is the one classifier — `turnStopped` (stopReason `cancelled`), `turnSucceeded` (`ok && !stopped`), `turnOutcome` (`completed | cancelled | failed`) — pinned by `server/turn-outcome.test.ts` (STOP2) for ok:true cancelled, ok:true end_turn/null, ok:false interrupted, ok:false exit_before_result, ok:false cancelled and both host-stop reasons. Everything that acts on a finished turn reads it, which changed these ACP/Pi semantics (a cancelled ACP/Pi turn used to count as `ok`):

- **Delegations queued by a stopped turn are discarded, not drained** (`server/index.ts` bus subscriber near `discardDelegations`: `!turnSucceeded(event)` → `discardDelegations(commsBus, threadId, botId)`, else `drainDelegations`). Engine-independent (it reads the bus). Receipt `status: "dropped"`; the target never runs. Tests: `server/user-stop-api.test.ts` "Stop on a source turn drops the delegation it queued instead of running it" (real server, fake Claude CLI); `server/delegations.test.ts` "writes a dropped receipt for every handoff a failed turn discards".
- **A delegated turn that is stopped leaves a stopped receipt, not done** (`server/index.ts` bus subscriber: `const delegationStopped = turnStopped(event)`, `delegationFailureName` is `DELEGATION_STOPPED_NAME` when stopped, else `Delegated turn did not finish — <stopReason>` for a failed turn, else `undefined`; the call is `finalizeDelegationWatch(event.threadId, turnSucceeded(event), reply, delegationFailureName, delegationStopped)`): receipt `{ status: "failed", result: "Delegated turn was stopped before it finished" }`, and the A⇄B channel mirrors `Delegation to @<bot> was stopped before it finished`. Test: `server/user-stop-api.test.ts` "a delegated turn that is stopped leaves a stopped receipt, not done".
- **`ask_bot` to a stopped peer reports `stopped`, not a reply** (`waitForReply` settles `{ status: "stopped", text, stopReason }` on `turnStopped(e)`; the route answers `(the bot's turn was stopped before it finished)` — with `; partial reply follows` and the partial text when there is any — and mirrors `Turn was stopped before it finished` into the channel). Test: `server/user-stop-api.test.ts` "ask_bot to a peer whose turn is stopped reports the stop, not a reply".
- **Outputs (U-02), routines, memory.** A stopped turn publishes nothing (`server/output-publication.test.ts` "treats a stopped turn (ok:true, stopReason cancelled) like a cancelled one"), a routine whose turn the host stopped is not `completed` (`server/routines.test.ts` "… turn the host stopped (ok:true, stopReason cancelled) (STOP1)"), and memory settles the turn `cancelled` (`turnOutcomes` in `user-stop-api.test.ts`).
- **A host-stopped goal member turn is `provider_failed`, retried once.** `runGroupMemberTurn`'s bus watcher finishes `provider_failed` on any `turn.completed` that is `!turnSucceeded(e)` (so a cancelled member turn too, with `stopReason` copied into the orchestration result); `runGroupGoalStep` treats it as transient and retries exactly once (`retriedTransient`), each attempt costing a goal turn. Test (STOP2): `server/host-stop-api.test.ts` "a goal member turn stopped by the host is provider_failed: retried once, then the run fails honestly" — a real Anthropic-preset connection (fixture key, catalog from an instrumented fetch) is turned off mid-turn; the card ends `failed` with `turnCount: 2`, one `stopped:` notice from the lead and one `error:` chip (the retry refused on the disabled connection).

**Host-initiated stops (STOP1, made visible by STOP2).** `noteHostStoppedTurn(threadId, botId, reason)` in `server/index.ts` posts one activity `{ tool: { name: "stopped: <reason>", ok: false } }` (with `from` on a room thread) before calling the driver's `interruptTurn`, so the turn settles like a user Stop and the conversation says why. Two reasons today: `the model connection it was using was changed or turned off` (`providerConnections.subscribe`, any thread whose active route's connection changed revision) and `this computer was switched off for the bot` (`PATCH /api/bots/:id` from `computer: "local"` to anything else while busy — direct thread only). The `stopped:` prefix lives in `shared/host-stop.ts` (`HOST_STOPPED_PREFIX`, `hostStoppedActivityName`, `hostStoppedReason`), the same convention as the `error:` prefix and no change to `Message.tool`'s shape. Renderer rule: `ChatView`/`GroupView` render a `stopped:` activity as `StoppedRow` (neutral, `role="status"`, "Stopped — <reason>") before the `error:` and Tool-calls checks, so it is visible in a 1:1 thread with Settings → Tool calls off; ordinary tool chips stay hidden there. The sidebar preview and the task timeline read it as "Stopped — <reason>" (`src/lib/host-stop.ts`); the Markdown thread export and a delegation's activity summary spell it the same way through `hostStoppedDisplayName` (`shared/host-stop.ts`, FOLLOW3), and the bot avatar treats it as a neutral stop — no failure motion, no alert glyph (`activityFailed` in `src/lib/mascot.ts`, `activityMotion` in `src/state/store.tsx`). Tests: `server/user-stop-api.test.ts` "a host stop (this computer switched off for the bot) …", `server/host-stop-api.test.ts` "turning the connection off mid-turn …", `src/e2e/stop-state.human.spec.ts` "a host stop shows why the turn ended in a 1:1 thread with Tool calls off", `src/lib/host-stop.test.ts`, `server/task-timeline.test.ts`.

## MEMJSON2 notes (lane MEMJSON2, additive to the MEMJSON1 memory frame)

- **Turn-local memory handles.** `shared/memory.ts` gained `MEMORY_HANDLE_PATTERN` (`/^m[1-9][0-9]{0,2}$/`), `MEMORY_HANDLE_LIMIT` (999), `memoryHandle(position)` and `memoryHandlePosition(handle)`. Every line of a `<remembered-context>` frame now opens with its handle — `- m1 (the owner said; fact; pinned by the owner) "…"` — where position N is `bundle.recordVersions[N-1]`, the same order the disclosure receipt persists. `MEMORY_REFERENCE_PREAMBLE` gained one sentence telling the engine what the handle is for; open/close tags and `memoryRequestPrefix` are unchanged. Reason (MEMJSON1 verifier finding 1): record ids left the prompt, so an engine wanting `memory_get`/`memory_propose_correction` on a remembered line had to `memory_search` first. A handle carries no provenance and resolves only through the receipt of the dispatch the capability was minted for (`MemoryDispatchReceipt.resolveHandle`, same bot/thread/generation), so nothing about record ids, scopes or byte ranges reaches the prompt.
- **Memory tool schemas** (`server/drivers/memory-proxy.ts`, `server/memory/routes.ts`). `memory_get.handles[]` items are `{handle}` **or** `{id, version}` (never a mix; JSON schema lists all three properties with `required: []`, zod enforces the union). `memory_propose_correction` takes `{handle, replacement, evidence, idempotencyKey}` **or** `{id, version, replacement, evidence, idempotencyKey}`; `required` is now `["replacement","evidence","idempotencyKey"]`. Records returned for a handle carry `handle` next to their real `id`/`version`; a correction proposed by handle echoes `handle` with `record`. An unknown handle is `404 MEMORY_HANDLE_UNKNOWN`; a malformed or mixed reference is `400 INVALID_MEMORY_ARGUMENTS`. `memoryAgentRoute` gained an optional fifth `handles?: MemoryHandleResolver` argument; `server/index.ts` passes `memoryDispatches.get(threadId)`.
- **Own-message recall exclusion.** `buildMemoryBundle` options gained `excludeMessageIds?: readonly string[]`; a recall hit whose evidence rests only on sources captured from those messages in the dispatching thread is left out (pins and the thread checkpoint are untouched). `server/index.ts` passes the turn's own user message and drained queued lines (`skipTranscript`) for direct turns and `latestUser.id` for room member turns. Reason (finding 3): the dispatching message is captured before its bundle is built, so recall handed the engine its current request back as a remembered `source` line.
- **Proof scripts.** `scripts/verify-memory-dispatch.ts` pins the line grammar, handle order, the absence of provenance tokens and of a self-echoed request line, and resolves `m1` live through the held turn's capability (7 checks). `.planning/0149-memory-runtime-proof.ts` parses the current frame instead of the pre-MEMJSON1 JSON preamble and proves the imported record by words + attribution in the prompt and by id/source in the receipt.

## FUIGOTRUST1 notes — Fuigo folder trust through Murage (additive; the shipped path and why)

**The finding.** Fuigo 1.0.13 (FUIGO13) is the first release-stamped build whose folder-trust gate is live (upstream `crates/codegen/fuigo-workspace/src/folder_trust.rs` `decide`): a workspace with repo-local sources — `AGENTS.md`/`CLAUDE.md`/rules, `.mcp.json`, project `.fuigo/`/`.claude/` config, project skills/commands, hooks, plugins, agents — resolves **Untrusted, never Prompt** when stdin and stderr are not terminals, and Murage's piped `agent stdio` spawn is exactly that. Every hosted turn in a folder the user had not trusted through standalone `fuigo --trust` silently ran without those sources.

**Which upstream path shipped, and why both.** Upstream offers an interactive round-trip: a client that advertises `clientCapabilities._meta["fuigo/folderTrust"] = { interactive: true }` on `initialize` receives `_fuigo/folder_trust/request` `{sessionId, cwd, workspace, configKinds}` after `session/new`/`session/load` and answers `{outcome: "trust" | "reject"}` (anything else decodes to reject; 30-minute upstream timeout). Murage advertises it and answers it — **but that request is sent from a detached task after the session was already built, and the engine reads project instructions and skills at agent build (`AgentBuilder::build` with `project_trusted` frozen; the post-grant hot-reload covers MCP servers, hooks and plugins only).** A grant given through the request therefore reaches `AGENTS.md` only on the next spawn. Since Murage spawns a fresh Fuigo per turn, the truthful design decides trust **before the spawn** and passes it explicitly: the record (or the card's answer) becomes `--trust` on the argv (a top-level flag, before `agent`, granting the process cwd = the `session/new` cwd, and persisted by Fuigo itself into the user's own `~/.fuigo/trusted_folders.toml`, so standalone Fuigo agrees with what the person told Murage). The capability stays advertised so the engine never has to decide "untrusted" alone: if it still asks (its scan gated something Murage's scan did not name — a `~/.claude.json` project entry, a marker added since), the driver answers from the same decision or raises the same card from the engine's `configKinds`, and a late grant is noted as applying from the next turn. Proven live against the staged 1.0.13 binary on a local llama.cpp model (lane scratchpad `fuigotrust1/harness-proof/receipt.json`, `native/*.ndjson`, `trusted_folders.toml`): untrusted → canary instruction absent, the engine's request on the wire answered `reject`; trusted through the card → the same turn's reply carries the canary, `trusted_folders.toml` names the workspace, no request on the wire; picker-chosen folder → no card, canary present.

**Shared shapes (all additive).**
- `SendTurnInput.folderTrust?: FolderTrustTurnInput` (`server/contracts.ts`) — `{ key, folder, decision?: "trust" | "reject", sources: string[] }`, handed by `server/index.ts` (`folderTrustForTurn`, both the direct and the room dispatch) **only** to a driver whose `adapter.capabilities.folderTrust === true` (new optional flag; Fuigo's ACP support sets `folderTrust: true`). `undefined` for the home folder and for unrecordable roots, which Fuigo never gates.
- `RuntimeEvent request.opened` gains `folderTrust?: { key, folder, sources }` — a question whose answer decides trust; `tool` is `folder_trust`, `requestType` `question`, `questions` is the one `folderTrustQuestion` (`shared/folder-trust.ts`: id `folderTrust`, header "Folder trust", options `Trust this folder` / `Don't trust`, no free text). Never auto-answered in any mode (ASK1's rule); auto mode does not trust folders.
- `OptionCardData.folderTrust` (server + renderer mirror), persisted with the card; the card's title is "Trust this folder?".
- `DecisionKind` gains `folder-trusted` / `folder-untrusted` (source `user`): the answer IS an authorization, unlike an ordinary question's.
- `AcpSupport.folderTrust?: boolean` and `spawnArgs(config, turn, ctx?: { requestedModel?, folderTrusted? })`.
- Chip conventions (`shared/folder-trust.ts`): `untrusted folder: <sources>` (a turn ran with the folder's sources held back) and `trusted folder: <sources>` (a late grant, applies next turn), one activity message with `ok: true`, rendered as `FolderTrustRow` (neutral, `role="status"`, visible with Tool calls off) in `ChatView`/`GroupView`, spelled out by `folderTrustDisplayName`/`folderTrustLabel` in the sidebar preview, timeline, Markdown export and delegation summaries — same family as `stopped:`.
- Routes: `GET /api/folder-trust?folder=` → `{ key, folder, sources, gated, record | null }`; `DELETE` forgets (the next turn asks again). Both desktop-authority (`server/desktop-policy.ts`). A folder-trust card is answerable only from an owner surface (desktop, the paired Telegram channel); `/respond` answers 403 to a companion (`questionReply(..., owner)`).

**The record.** `server/folder-trust.ts` `FolderTrustStore` → `<DATA_DIR>/folder-trust.json` `{version: 1, folders: { [key]: { decision, decidedAt, source: "picker" | "card", folder } } }`, mode 0600, damaged file = `PersistedStateRecoveryError` like bots.json. Key = canonical git worktree root when the folder is in a repository, else the canonical folder (upstream `workspace_key` minus its managed-worktree collapse); home, a filesystem root and relative paths are unrecordable. `scanFolderTrustSources` names the upstream markers by file along the cwd → git-root chain (display for the card; Fuigo's scan stays authoritative for the gate). Written by: any folder picker (`PATCH /api/bots/:id {cwd}`, `PATCH .../tasks/:threadId {cwd}`, `PATCH /api/groups/:id {cwd}`, room setup) → `trust`/`picker` at selection time (the picker says so in one line, `FolderTrustNote`); the card's answer → once the driver has taken it (`answerRequest`). A skip records nothing.

**Driver rule (`server/drivers/acp/core.ts`).** Decision known → spawn at once (`--trust` when `trust`; a `reject` turn gets the withheld chip). No decision and `sources` non-empty → `turn.started`, then the card, **nothing spawned** (`interruptTurn` settles at once, close-confirmed); Trust → spawn `--trust`; Don't trust / skip → spawn untrusted + chip; nobody answers within `QUESTION_TIMEOUT_MS` → `stopped: nobody decided whether to trust <folder> in time; send the message again to be asked` and `turn.completed { ok: true, stopReason: "cancelled" }` (STOP2), never a hang and never a guess. The engine's own request is answered from the decision, or by a card built from its `configKinds` when there was none.

**Tests.** `server/drivers/acp/acp.test.ts` "ACP folder trust" (fake ACP CLI `folder-trust` mode, which reads `./AGENTS.md` into its reply only when spawned `--trust` — the engine's session-build timing; nine tests red before core.ts), `server/folder-trust.test.ts`, `server/folder-trust-api.test.ts` (real server, Fuigo driver on the fake), `src/components/QuestionCard.test.ts` "the folder-trust card".

## FUIGOTRUST2 notes — verifier follow-ups to FUIGOTRUST1 (additive)

**Shapes added (all optional, nothing renamed).**
- `FolderTrustTurnInput.upstreamTrusted?: true` (`server/contracts.ts`): the user's own Fuigo install already trusts the workspace (`<FUIGO_HOME>/trusted_folders.toml`, read-only by `server/folder-trust.ts` `readUpstreamTrustedFolders` + `upstreamTrustsFolder`, which mirror upstream `TrustStore::is_trusted`: longest covering key of the same git root wins, ties fail closed, home/root/relative keys ignored; a document the parser does not fully recognise is empty, like the engine's). Set by `folderTrustForTurn(instance, cwd, providerRouted)` only for a native-login Fuigo turn (`fuigoHomeForTrust`: `FUIGO_HOME` from the server env plus the instance's configured environment, else `~/.fuigo`); never for a provider-routed turn (per-turn temporary home) or another engine. The driver then raises no card, passes no `--trust` of its own, shows no `untrusted folder:` chip and answers a stray engine request `trust`. `GET /api/folder-trust` gains `upstreamTrusted: boolean`; `FolderTrustNote` shows `folderTrust.statusUpstream` for it.
- `RuntimeEvent request.resolved.folderTrustLate?: "finished" | "stopped" | "timeout"`: a folder-trust card raised from the engine's own request after the engine had started (the server's scan named nothing) closed with no decision because the turn finished, was stopped, or the ask timed out while the turn ran on — the engine ran that turn untrusted in every case. `OptionCardData.folderTrust.late` (server + renderer mirror) persists it on the expired card, and `QuestionCard` reads `folderTrust.lateFinishedNote` / `lateStoppedNote` / `lateTimeoutNote` instead of "nobody decided in time, so this turn was stopped". The driver emits the `untrusted folder: <what the engine asked about>` chip for that case too (inside the turn: a settle closes the ask before it emits `turn.completed`).
- `FolderTrustSource` gains `"upgrade"`, and `folder-trust.json` gains `seededFrom?: string`: on the first boot with no marker, `seedFolderTrustFromStore` (`server/index.ts`, right after `store.seedIfEmpty()`) records every existing bot `cwd`, task `cwd`, room `cwd`/`pinnedCwd` and room-task `pinnedCwd` as `trust`/`upgrade` (keyed on the workspace root like a picker; folders already recorded keep their decision; `<DATA_DIR>/workspaces/…` bot-created folders are never seeded), logs `[folder-trust] recorded N folders …`, and writes `seededFrom: "0.1.52"` so it never re-runs and never overrides a later Forget. Sean's rule: picker-chosen folders are trusted at selection, and these were picked by the person in Murage before the record existed.

**Rules tightened.**
- `rememberPickedFolder(cwd, desktop)` records only when the request proves desktop authority (`requestSurface(...) === "desktop"`, the card's own predicate) — at every picker site: `PATCH /api/bots/:id {cwd}`, `PATCH /api/groups/:id {cwd}`, `PATCH /api/groups/:id/setup`, `PATCH /api/bots/:id/tasks/:threadId {cwd}` and now `POST /api/teams/import?mode=project&cwd=` (which was not recording at all). Note for the verifier's premise: the task route already answers 404 to a non-desktop caller for `cwd` (since `05cce991`), so a companion never set the folder there either; the test pins both facts (404 + nothing recorded) rather than widening the companion surface.
- ACP core `settle()` calls `providerBinding?.cleanup()` when no child was ever spawned (a card that timed out, was stopped, or whose post-answer launch failed) and on a synchronous launch throw in the no-card path; the child-close handler stays the cleanup for every spawned turn.

**Tests.** `server/folder-trust.test.ts` (parser, `is_trusted` cascade, `fuigoHomeFromEnv`, `seedOnce`), `server/drivers/acp/acp.test.ts` "ACP folder trust" (+6: routed-home cleanup on Stop / timeout / failed launch, upstream-trusted turn with and over a Murage reject, late request finished-first and stopped), `server/folder-trust-api.test.ts` (+4 in the fixture suite: upstream toml over a Murage Don't trust, task-picker authority on both surfaces, project import records, late card copy through the real server; + the upgrade-seed suite that boots a 0.1.51-shaped store twice), `src/components/QuestionCard.test.ts` (late-card copy). The fake ACP CLI in `folder-trust` mode now also reads `<FUIGO_HOME|~/.fuigo>/trusted_folders.toml` (exact `[folders."<cwd>"]` match) and takes `FAKE_ACP_TRUST_PROMPT_FIRST=1` to finish the prompt while its trust request is open.

## FUIGOTRUST3 notes — verifier follow-ups to FUIGOTRUST2 (additive)

**Shapes touched (all additive, nothing renamed).**
- `RuntimeEvent request.resolved.folderTrustLate` and `OptionCardData.folderTrust.late` (server + renderer) gain `"failed"`: a late folder-trust card closed by a turn that FAILED (ok:false — `rpc_error`, `spawn_error`, `exit_before_result`) says so through `folderTrust.lateFailedNote` (all eight packs), never "The turn was stopped before anyone answered". ACP core tracks `state.failed`; `stopped` is now only a cancelled turn.
- `GET /api/folder-trust?folder=&bot=<botId>`: `bot` names the bot the picker is editing; `upstreamTrusted` is then read from THAT bot's instance (`fuigoHomeForTrust(registry.get(bot.modelSelection.instanceId), Boolean(bot.modelSelection.connectionId))` — a provider-routed selection consults no store, like its turns), an unknown bot is `404 no such bot`; without `bot`, the first `fuigoAgent` instance as before. `FolderTrustNote` takes `botId` and `SettingsPanel` passes `bot.id`; `GroupView` (a room, whose members run on their own instances) keeps the fallback.

**Rules tightened.**
- **Trust key = upstream `workspace_key` including the linked-worktree collapse.** `folderTrustKey` (and so Murage's own record, the picker, the seed, `upstreamTrustsFolder`, the card's `key`) keys a linked git worktree on its MAIN checkout's root: `linkedWorktreeMainRoot` mirrors the git2 branch — the `.git` file's gitdir must carry `gitdir` + `commondir` (libgit2 `repo_is_worktree`), the common dir is opened as the main repository with its `core.worktree`/`core.bare` read the way libgit2 infers a working tree, and the collapse fires only when `<main>/.git` IS the common dir (a bare repo's worktree and a `--separate-git-dir` checkout keep their own root; a submodule's `.git` file is not a worktree). Still not mirrored, as in FUIGOTRUST1: the `~/.fuigo/worktrees.db` managed-worktree registry (`fuigo -w`), which Murage does not read. `upstreamTrustsFolder` queries the workspace key like the engine (`is_trusted(workspace_key(cwd))`), so the main checkout's `fuigo --trust` covers every worktree, and a hand-edited record BELOW a root never covers or overrides anything (the engine never consults it) — a changed test expectation, recorded in commit 892b73ab. Proven on the staged 1.0.13 binary (lane scratchpad `fuigotrust3/harness-proof/receipt.json`): with cwd = a linked worktree, `--trust` makes the engine write `trusted_folders.toml` keyed on the MAIN checkout, never the worktree path, and that grant alone (Murage's record forgotten) runs the worktree turn with no card, no chip, no engine request and the canary on the model stream.
- **A stray engine request is never answered `trust` on `upstreamTrusted` alone.** The engine sends `_fuigo/folder_trust/request` only when ITS store did not trust the folder, so on an `upstreamTrusted` turn the request itself proves Murage's toml reading and the engine's disagree; the engine's reading is the one that runs. `handleFolderTrustRequest` answers from Murage's own record (a `reject` also emits the `untrusted folder:` chip the pre-spawn path had suppressed, since the turn runs untrusted after all; a `trust` record already put `--trust` on argv, so the engine does not ask) or raises the late card, exactly as for a turn that was not upstream-trusted.

**Fake ACP CLI (`folder-trust` mode).** `FAKE_ACP_TRUST_STORE_REJECTED=1` reads its store as empty whatever the file says (the engine's `toml` crate rejecting a hand-edited document); `FAKE_ACP_TRUST_FAIL_PROMPT=1` fails `session/prompt` with a JSON-RPC error while its trust request is open; its store lookup keys a linked worktree on the main checkout (`git rev-parse --git-common-dir`, conventional layout only) like the engine; and it runs the trust prompt after `session/load` as well as `session/new` (upstream `load_session_inner` does), so a resumed turn is gated like a new one — the missing branch made `folder-trust-api.test.ts` (2) timing-dependent (a second turn on a thread resumes through its cursor). The fixture's three decision-log reads poll (the log is an async queued append).

**Tests.** `server/folder-trust.test.ts` (real `git worktree add` fixtures: two linked worktrees + subfolder, submodule-shaped `.git`, bare-repo worktree, separate-git-dir worktree; the upstream store through a worktree cwd; the workspace-key query), `server/drivers/acp/acp.test.ts` "ACP folder trust" (+3: failed late card, upstream-trusted turn whose engine still asks, linked worktree under the main checkout's grant), `server/folder-trust-api.test.ts` (+2 in the fixture suite: a bot in a linked worktree through the real server; `?bot=` against two Fuigo instances with different `FUIGO_HOME`s, the third instance `fuigo-other-home`), `src/components/QuestionCard.test.ts` (the failed note).
