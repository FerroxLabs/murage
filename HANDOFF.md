# Murage — session handoff

**Updated:** 2026-09-01, end of a long build day · **Head:** `0b2dcea`
**Repo:** `github.com/FerroxLabs/murage` · **Local:** `/Volumes/Mando/WaylandBots/murage-app`

Murage is Ferrox Labs' multi-engine AI agent desktop app. Hard fork of
[OpenMausBot](https://github.com/milind-soni/OpenMausBot) (Apache-2.0) at `6140532`,
rebranded, now carrying the Wayland teams/skills library.

---

## START HERE — the next session's swarm

Three items, in this order. They are sequenced because 1 makes 3 look finished.

### 1. Fix the skill descriptions  (~15 min, BLOCKS the skills panel)

`scripts/import-wayland-skills.mjs` writes each description as a YAML **block scalar**
(`description: |` + indented lines). `parseSkillMd` (server/skills.ts) does not handle block
scalars, so **every one of the 2,106 imported skills has an empty description**. The skills
install and work; they just look blank.

Two failing tests pin this exactly:
`server/wayland-library.integration.test.ts` — "carries each catalog skill's real
description into the prompt index" and "finds every skill a generated assistant package
declares in the generated catalog".

Fix in the converter (emit a single-line quoted scalar), re-run it, re-run those tests.

### 2. Publish the 57 single-agent profiles  (~15 min)

`library/assistants/` (28 specialists) and `bot-library/builtins/` (29, **including
`smart-trader.json`**) were generated by an earlier swarm and **never published**.

**A single-agent bot package IS the "assign a profile to a bot" mechanism** — same import
path as a team, one agent instead of four, and it carries persona + playbook + skills. No
new concept is needed; the content simply is not in the catalog.

Publish into `FerroxLabs/murage-teams` alongside the 65 teams. Reuse
`scripts/import-wayland-teams-packages.mjs` as the model — it already emits valid packages,
validates through `parseBotPackage`, and writes catalog entries.

**Smart Trader has a playbook (11,492 chars) and ZERO skills.** Import the 11 published
`@ferroxlabs/tvcontrol` skills (`~/dev/tvcontrol/skills/`: chart-analysis, morning-prep,
multi-symbol-scan, multi-pane-analysis, learn-from-losses, replay-practice, strategy-ab-test,
strategy-report, pine-develop, porting-pine-versions, rebuild-from-screenshot) and attach.

**IP boundary, non-negotiable:** `~/dev/smarttrader` holds the unpublished 162-line Rebel
Scanner and REGIME-GATE. READ-ONLY reference. Never into this repo or any build artifact.
Ships: the generic prompt + the 11 published skills only.

### 3. A skills panel on the bot profile  (~25 min)

All four routes already exist at `server/index.ts:7284-7312`:
```
GET    /api/bots/:id/skills          list (+ staged)
POST   /api/bots/:id/skills          install from a GitHub source
GET    /api/bots/:id/skills/:name    read the SKILL.md text
PATCH  /api/bots/:id/skills/:name    { enabled: boolean }
```
Pure UI. Read-only (list + view text) is 90% of the value; the toggle is a bonus.

---

## What is DONE and verified

| | |
|---|---|
| Teams live | **65** in `FerroxLabs/murage-teams` — 59 Wayland + upstream's 6 merged back |
| Each team brings | its room, 240 playbooks, 6 routines, 1538 skill links, 0 dangling |
| Skills library | **2,194** in `skills-library/` (2,106 Wayland pack + 88 role skills) |
| Skills actually install | verified: Smith imported with 9 skills symlinked into `.claude/skills`, `.agents/skills`, `.grok/skills`, every link resolving |
| Names | `Smith (Code)` — character from the id, role from `.name` |
| Avatars | 9 call sites fixed to honour a bot's own avatar |
| Delete | typed-name confirmation for bots and rooms |
| Tests | **2,899 pass, 3 fail** (2 are item 1 above; 1 is machine-dependent) |

**Release:** macOS **green** (signed, notarized, stapled), Windows **green** (Azure Trusted
Signing), Linux **failing** at the in-place DEB upgrade (`apt-get install` of the built .deb).
Five runs; each found a real defect. All 9 GitHub secrets set and verified live.

---

## Flux Router — modules exist, nothing is wired

`server/flux-routing.ts`, `flux-surface.ts`, `flux-config.ts` + tests now exist (swarm
`wu1735bdz`, 8/8 agents). **The env strip shipped and is genuinely good**: it found FIVE
`...process.env` spawn paths, not the three Kimi named (`pi.ts` and `antigravity.ts` were
missed), and every new test was negative-control verified by neutering `stripRoutingEnv`.

**There is no key field, no onboarding step and no picker rows.** Do not turn Flux on
without reading the cross-audit first — `docs/plans/flux-onboarding-audit-codex.txt`.

Three CRITICALs in the UX plan (`docs/plans/flux-onboarding-ux.md`):
1. **"79 models unlocked" is a false success signal.** `/v1/models` returns 200 while every
   inference call 402s. It congratulates the user immediately before their first turn fails.
2. **"every bot / every engine" is false** — only claude, qwen, goose, codex have a surface.
3. The 79 count includes `flux-image` (down) and `flux-voice` (never probed).

Also wrong in that plan: packaged Murage uses **Electron's OS-encrypted credential store**,
not `config.json`; and the settings section is **Connections**, not "API keys".

**The key itself is fine** — verified live at end of session, 200 with a real completion,
spend 171 against a 1e9 budget. The recurring 402 is a Flux-side billing reconciliation
defect (reported to that team), not a quota problem.

---

## Live infrastructure

| | |
|---|---|
| Releases | `FerroxLabs/murage-releases` (public) |
| Teams | `FerroxLabs/murage-teams` (public) |
| Composio broker | `murage-composio.patient-meadow-1a11.workers.dev` |
| Cloudflare acct | `b83123326a4b9ad76831b9cb9365b33b` (admin@imsuccesscenter.com — **not Ferrox**) |
| Azure signing | `ferrox-labs-signing` / profile `ferroxlabs` / eastus, validation Active |
| Apple | Team `PX6SP9GPWJ`, Developer ID cert on this machine, API key verified |

**Composio ceiling built but NOT deployed** — `migrations/0002_call_ceiling.sql` + worker
changes need `wrangler deploy` with your Cloudflare token.

---

## Open, not urgent

1. **Credential-list drift has bitten twice in one day.** `electron/diagnostics.mjs` keeps a
   hand copy of `WORKSPACE_CREDENTIAL_ENV`; both Sendlane and Flux keys shipped unredacted
   until the parity test caught them. Derive it at build time.
2. **Linux release** — the DEB in-place upgrade check.
3. **`book-publishing-house`** — 1 of 60 teams skipped; its five members are `book-*` roles
   absent from the 28 specialists.
4. **Two Quiet Money rituals dropped** — `RoutineSchedule` is only `once|daily`
   (routines.ts:12-14) and cannot express quarterly or annual.
5. **Cloudflare account** is not a Ferrox account. Cheapest to move before launch.
6. **No team-wide skill.** Skills are per-bot; a shared "brand voice" is N assignments.

---

## Running it

```bash
nvm use 24
node --experimental-strip-types server/index.ts &   # :8799
npx vite &                                          # :5199
npx electron .
```
Order matters. Electron's `DEV_URL` defaults to **5199** and gives up fast; a stale vite
squatting on 5199 gets loaded instead of yours. That cost an hour once.

---

## Gotchas that cost real time

- **The rebrand split TEN contracts**, renaming one side and not the other: the AOS schema
  id, the catalog format string, `HERMES_OPENMAUS_*`, the team-library URL, the update feed
  owner, the SBOM property, the DEB maintainer, a sha256 fixture, `runOn: maus`, and the
  credential list. **Assume an eleventh.** Sweep for identifiers another process reads,
  separately from cosmetic renaming.
- **A v2 team manifest deliberately creates no room.** Bot packages do. Use packages.
- **Chromium honours EITHER `scrollbar-width` OR `::-webkit-scrollbar`, never both.**
- **`win.publisherName` does not exist in electron-builder 26** — it lives inside
  `azureSignOptions`, and a stray one fails the config for every platform.
- **Validate `electron-builder.yml` against `app-builder-lib/scheme.json` locally.** A CI
  round trip to learn one field name costs 8 minutes.
- Dev Electron is `com.github.Electron`, unsigned — macOS cannot attribute permissions to
  "Murage" until it is the packaged signed app.
- Signature gates must allowlist vendor signers: we bundle `cloudflared.exe`, signed by
  Cloudflare, and a gate demanding our subject everywhere rejects it.
