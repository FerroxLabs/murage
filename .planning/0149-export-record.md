# 0.1.49 contextual exporter

Baseline 6c32500b. Outcome: selected bot/team starts export; grouped searchable bot choices with actual roles and selected instructions/assets; importable ZIP default and truthful Markdown limits; contents/exclusions preview; preserved selected supported definitions and relationships through import. Credentials, conversations, private memory, runtime permissions excluded; routines paused. Existing scan/hash/review and inert-import boundaries retained. No live profile mutation, marketplace, new file format, dependency install or publication. Existing Murage semantic tokens and component patterns govern this corrective UI; no new visual theme selection.

Ownership: server/bot-package.ts, package-export*.ts, bot-package-import.ts, bot-package-manifest.ts and focused tests; TeamExportDialog.tsx and isolated human spec. Root owns Sidebar active-scope seam, Store/shared types/routes and separate conversation action. Role intent vs runtime authority seam reported before implementation: current Store explicitly refuses imported Chief authority. Root decision pending while independent export work proceeds.

Frozen checks: `pnpm exec vitest run server/bot-package.test.ts server/package-export.test.ts server/package-export-bundle.test.ts server/bot-package-manifest.test.ts server/bot-package-import.test.ts server/package-export-files.test.ts`; `pnpm typecheck`; isolated Playwright team-export.human.spec.ts with dedicated config (no global app harness), desktop/narrow and light/dark screenshots inspected; `git diff --check`. Disposable archive round-trip fixture must use write/read/preview/import with temporary data root and retain originals, prove fresh identities, selected roles/relationships/assets and disabled permissions/routines. UI fixture uses real dialog + mocked HTTP for exact request/selection/download behavior; it is not a live route proof. Root integration owns live isolated HTTP acceptance.

Root clarified: preserve role/team as inert provenance; active Chief/leader restoration remains explicit existing assignment and an integration acceptance seam, never automatic elevation. Shared metadata seam supplied by root commit 543fec0e (local cherry-pick 672439ae). Source export roles reflect runtime flags; source role/team is retained on imported metadata. Fresh import-specific section labels preserve grouping without joining an existing workspace leader. Selected room members are reconstructed with fresh IDs and removed default responders fall back to mentions.

Frozen check set additionally names the new `server/package-export-roundtrip.test.ts`: it writes/reads a real archive, previews/imports it and uses the real package file transaction into a temporary data directory. Browser config: `.planning/0149-export.playwright.config.ts`; real dialog + mocked transport, 121-bot search, selected scope, asset pruning, desktop/narrow/light/dark screenshots. No global harness or live server is launched by this browser config.

Round 1: seven focused files / 63 tests passed; isolated Playwright 8/8 passed in 42.3s, including all four light/dark/width combinations. Screenshots inspected: semantic skin colors, text, selected-role details and controls legible with no horizontal overflow; narrow modal scrolls vertically. Typecheck found one change-induced fixture typing error (required BotRecord.title omitted in new round-trip fixture), not a product runtime failure. Correction 1 added the fixture's title. `git diff --check` passed. Round 2 runs typecheck and only the invalidated round-trip test; browser and other focused passes retained. No third cycle.

Round 2: `pnpm typecheck` passed; round-trip 2/2 passed (1.63s). All other Round 1 passes retained. Two rounds consumed, one fixture correction. Implementation verified at component/definition/ZIP/durable-file-transaction level on Darwin arm64, Node v25.8.1. Browser transport is mocked; no actual profile/server calls made. Test teardown removed fixture temporary roots and stopped its own Vite server. Screenshots retained in this worktree's `.planning/0149-export-ui-results/` (not source commit): `team-export.human-contextu-18779-ncy-pruning-at-1440px-light/context-export-1440-light.png`, `team-export.human-contextu-b6c3c-ency-pruning-at-1440px-dark/context-export-1440-dark.png`, `team-export.human-contextu-e9e8b-ency-pruning-at-390px-light/context-export-390-light.png`, `team-export.human-contextu-322ce-dency-pruning-at-390px-dark/context-export-390-dark.png`.

Integration status: PENDING ROOT SEAMS, not full exporter acceptance. `server/index.ts` legacy Markdown import still automatically calls `store.setChiefOfStaff` for `pkg.chiefOfStaff` (~9180) and drops portable role/team metadata. This is an evidenced conflict with inert-import copy and role acceptance; root owns the correction before safe integration. Root also owns Sidebar initialBotIds wiring and imported intended-role display/explicit role assignment. No new role authority protocol implemented; active role restoration remains existing user assignment, not an automatic export/import effect. Separate conversation Markdown action must live at the active-conversation menu and export only that task's messages; do not reuse bot-definition export or include it in packages. Worker made no shared route/store/menu changes. Candidate commit is for root to integrate together with its required seam corrections, not ship independently.
