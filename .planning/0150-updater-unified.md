# Unified native Fuigo updater integration

Baseline f320e846fe4ad49a277f7b8a14c361cce8d51e32. Outcome: integrate exact proven Windows AppContainer launcher/probe adapter, Windows resource/signing manifest wiring, and shared Linux/Windows x64 manager admission, retaining accepted Linux private Unix-pair policy and current macOS behavior. No application-core, owner-state, VM, publication or unrelated feature work. Original failed synthetic Windows token context and outer PowerShell ExitCode=null wrapper result remain recorded separately; neither substitutes for the passed genuine-Limited leaf receipts.

Native evidence reused: Windows D2 candidate4dc37b21 original canaries and full default EngineManager install1.0.9/update1.0.10/rollback/bundled/fullACP passed as genuine Limited; guestowner exports and cleans. Linux fullmanager run34468176872/source576035a1 passed; earlier Linux primitive/fullprobe/package evidence retained. Existing Mac passes remain. No full native rerun is planned.

Frozen U1 integration checks: source-section equivalence of Windows launcher/canary against1ea3f9fc/D2 and Linux against1b90f210; qualified probe/adapter behavior equivalence and explicit unsupported-architecture guard; focused manager/native probe/resource/afterPack tests on Node24.20; server types; changed build/resource scripts syntax and diff validation. Windows resource tests cover required PE target/hash/inventory plus signed-hash restamp and signing failures using clear test doubles. Native compile/real resource/Authenticode stage belongs to the already-planned subsequent Windows private package build and remains pending until that actual artifact is built. U2 only eligible induced correction/confirmation if needed; existing native counters are not reset. Root waits for exported native receipts, reviews one squash-ready commit, then decides integration/private build source. No private app build or guest operation from this worker.

U1 integration PASS: Node24.20.0 focused6files62passed/1existing platform skip; resource tests6passed/1real-Linux-compile skip; server tsc exit0; four changed script syntax/diff checks passed. No U2 needed. Windows and Linux launcher/canary preprocessor sections exactly match proven1ea3f9fc and1b90f210 respectively. EngineManagerSHA810b00bd1fcb99c0c89565b88200cbc6d2e29e02330c213c5d81bfbbe0ece8a9 and adapterSHAe74e13bd8c15747e3ce576d052389002ee68ce4487d96602aa67e7ff57823e8e match D2 receipt exactly. CodexWindowsSHAaa340dddb20ae6356bc4182a75c540eb8b03ce6a257ce2775110c99349ac2d35 and envPathSHA9bd548fcc25ea44a1485014f08218f2824790b44dfc74cf6e67a2ad7eba97952 unchanged. Probe matches D2SHAacde23e8f71547d0f02533fe232fdd09a2a5b51a83bc9696b45ca1359f18a821 after removing only the new explicit FUIGO_TARGETS early guard; qualified-platform path is unchanged, unsupported Windowsarm64 refuses before scratch.

Packaging implementation: existing Linux routes/resources unchanged; Windows package:win now builds the helper and includes only launcher.exe+manifest.json as single-file resources. Existing toolchain discovery reused; synthetic restricted-user wrapper is not added. afterPack validates raw target/hash/inventory, signs via existing signer, verifies final Ferrox Authenticode with the existing signature checker, then stamps manifest to the signed helper bytes. Test doubles establish call ordering/failure/refusal/rehash only. Real native build/signature stage is still required in the private Windows artifact workflow: package:win invokes actual compile/afterPack; after packaging run validateFuigoProbeResources(release/win-unpacked/resources, win32), require helper+manifest inventory and valid Ferrox signature for helper, retain final helper SHA and artifact checksums. Do not call this unexecuted stage passed. Original five-file packaging preparation and all earlier native/platform receipts remain preserved.
