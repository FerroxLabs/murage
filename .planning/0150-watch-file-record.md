# 0150 file watch adapter

Contract: existing watch foundation extension only, adapterId=file, opaque scopeId resolved by trusted caller to current bot/workspace binding, sourceId exact relative user-selected file. No scheduler, index routes, UI, model, network, publication or live profile access. Acceptance: bounded regular file hash observations; invalid/private paths, symlinks/hardlinks, races, missing/revoked access and cancellation cannot produce unchanged observations. Source content never leaves adapter. Parent owns integration and user workflow proof.

Base verified: integration 684c60ae; isolated worktree `.planning/swarm-0150-watch-file`, branch `work/0150-watch-file`. Files: server/routine-watch-file.ts and server/routine-watch-file.test.ts. Shared types unchanged. Existing artifacts.ts snapshot/descriptor/recheck pattern followed. This record was written at handoff, not before implementation; parent task message supplied initial scope/check constraints.

Checks: Node24.20.0 explicit executable; vitest run server/routine-watch-file.test.ts server/routine-watch-state.test.ts; tsc -p tsconfig.server.json --noEmit. Round1: 29/30 tests passed, ESM namespace spy fixture failed before product execution; types failed never-return helper narrowing. Corrections: mocked fs namespace for deterministic race fixture; function declaration for never-return narrowing. Round2: 30/30 tests passed, two files; server types exit0. No additional rounds. git diff --check exit0 (new files still untracked).

Status: adapter verified locally on darwin arm64; not integrated/released, not full watch feature or native Windows proof. Fixture owns temporary files and removes each after test; shared node_modules linked only for tool resolution. Stable opaque workspace mapping and current authorization resolver remain required trusted caller responsibilities. Permission-denied OS case is mapped to unavailable but not independently exercised with an ACL fixture. No task-owned runtime process remains. Next authorized action: parent reviews/transfers these two files and wires caller separately. No commit made.
