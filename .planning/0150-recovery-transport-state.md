# 0150 production recovery transport

Joined native run34451475681/source452e167b failed before compile: build wrapper passed literal backslash-escaped quotes to cmd around existing VsDevCmd.bat. Full failed output inspected; C++ and pipeline never ran. Supported build correction uses windowsVerbatimArguments for the controlled/validated cmd string, rejects percent/delayed-expansion path characters, and moves Node24 setup before compile (failed receipt used host22). Continuation6 repeats joined workflow; native/desktop source and assertions unchanged, prior36/UI6 retained.

Joined local checks PASS:36 controller/main/window/selector/transport tests, no skips;6 actual recovery-renderer screenshots at390/820/1440 light/dark inspected, keyboard capture action, cancellation feedback, disabled owned-only actions and no overflow pass. .planning/0150-recovery-joined-ui/result.json + six PNGs retained. Windows joined native pipeline/build script confirmation pending next branch run. Source keeps347a3f4d startup catch/Flux code; integrating8da3e20f remote-menu/updater changes before native run. Local node_modules is a task-owned symlink to the existing parent installation, no dependencies changed.

Scoped native transport ACCEPTED at5108046c in continuation4 run34450505204/job102784929425. Production broker and separate emulated-elevation fixture compiled cleanly; Windows desktop protocol6/6 PASS/no skips. Native peerPID/SID/nonce/first-instance/DACL checks pass; real pinned-root rename refused Win32 32 after read-access correction. Pre-confirm cancellation, actual parent-process death and one caller-token-guarded real VSS capture pass; exact snapshot0f222334-7110-45f4-a004-e29f435d6a02 reported copyComplete/snapshotReleased true. Downloaded .planning/0150-recovery-transport-native-r4/result.json records source5108046c and explicitly actualUac:not-exercised. Four runs retained (R1 opaquefixture, R2diagnostic, C3fixturecorrection exposedlockdefect, C4lockfixPASS); two corrections (fixturecapacity, nativepinreadaccess). No further transport re-audit. Actual UAC and signed/packaged elevation remain unproven.

Next authorized existing-feature step: join accepted capture/transport to4da archive/separate-installation selector, preserve current347a3f4d Flux/startup behavior, main/controller/window/selector seams exclusively assigned by root. No liveowner profiles or liveUAC; targeted worker/pipeline/native fixture integration only under recorded user continuation authority.

Joined acceptance/check set: pathless Windows foreign-owner controller action; native identity-bound consent; original/anchors never leased or mutated; normal clone lease/real archive/hash-bound separate restore/fresh-process selector; actual retained history, fresh connections, credentials excluded and engines/schedules/memory paused until exact review/activation. Cancellation cannot publish selector. Focused controller/main/window/selector checks, one disposable Windows native-to-archive-to-selector/activation run, and existing renderer at390/820/1440 in both skins with button/disabled/error flow. Windows package script must build/include production helper while preserving signing settings. Actual interactive UAC/signing/full app package remain separate unproven gates. Native transport and old capture proofs reused; no unrelated broad suite. These extend existing recovery under user continuation authority; no attempt counters reset.

Continuation3 run34450352428/source38b241cf: first-instance refusal PASS(status5), caller access denial PASS(0x80070005), then pinned-source MoveFileEx unexpectedly succeeded. This is a native locking defect, not a stale assertion: FILE_READ_ATTRIBUTES-only directory handles did not block rename. Supported correction adds FILE_LIST_DIRECTORY access to held directory handles in bridge and accepted capture dependency; exact rename-refusal assertion retained. No source/owner writes or permissions change introduced. Continuation4 confirms this correction and remaining native transport; prior desktop6/6 and compile evidence retained, no oldcapture11 rerun.

User extension relayed by root: "Blitz all, no limits, no new features, maximum effort" authorizes further targeted corrections/confirmations of this existing package beyond old caps. Preserve counts and evidence; no weakened assertions, speculation or live UAC. R2 run34450220147/source567e93c4 identified exact fixture error: first-instance probe used maxInstances1 and received Win32 231 ERROR_PIPE_BUSY before first-instance policy could run. Authorized continuation3 changes ONLY native policy fixture capacity to2 in both calls, keeping ERROR_ACCESS_DENIED assertion; production remains maxInstances1. Product source unchanged. Rounds2 completed, correction1 (fixture only), continuation3 pending.

R1 native run34449887507/source091f96e2: production+fixture compile PASS; Windows desktop protocol6/6 PASS; native policy fixture stops before broker/capture with unlabelled E_INVALIDARG(0x80070057). Full failed output read; source inspection does not identify a supported correction. Root authorized reserved R2 with checkpoint/HRESULT instrumentation ONLY, same assertions and product code. No speculative source fix. R2 is final package round and may provide diagnosis rather than acceptance; no extra run inferred. Rounds1/2, source corrections0/2.

Base 34b90c58. Outcome: add one-shot native broker and trusted desktop adapter for the existing no-backup recovery feature. Scope includes documented named-pipe peer PID/SID binding, source/destination identity binding, caller-token read-access checks, nonce/confirmation, UAC launcher, cancellation/parent death, bounded private receipt and reuse of the accepted capture function. Excludes live profile/elevation runs, service/privilege changes, renderer paths, generic privileged file operations, publishing and full archive-selector UI integration owned by root. Accepted capture package remains 2/2 and is not restarted.

Frozen transport checks: native MSVC build; disposable exact-peer pipe exchange with emulated elevation explicitly labelled; unauthorized peer PID, nonce/SID mismatch, pre-confirm cancellation, parent death, source-identity mismatch and caller-access denial; focused desktop adapter tests for confirmation and bounded schema/output. One guarded real capture may exercise the new access guard; no wholesale old 11-case rerun. Actual UAC approval/denial on user desktop stays pending until an owned interactive runner is authorized. New transport rounds 0/2, corrections 0/2. Round 2 only eligible corrections and invalidated transport checks; no third run. Stop on supported API/platform failure or remaining authority gate, preserve exact receipts.

Transport chosen: Electron main spawns unelevated bridge with fixed helper path and parent PID, sends bounded paths over inherited stdin, receives native identity preview, obtains native owner confirmation, then sends nonce-bound confirmation. Bridge owns current-SID+SYSTEM, first-instance, remote-rejecting named pipe; ShellExecuteExW runas returns the exact elevated process handle. Mutual named-pipe PID checks and live pinned process/token SID checks precede the single native request. Elevated argv contains only bridge PID and nonce, never paths. Different-SID elevation is refused. Captured object DACL checks use the unelevated bridge token before copied reads. No Node renderer reaches this transport directly.

Implementation prepared: native broker.cpp, separate test-only emulated-elevation transport-fixture.cpp, desktop adapter + six focused tests, caller-token access and destination-parent identity guard in capture.h/cpp, native driver and workflow. R1 desktop Node24 tests 6/6 passed with no skips; driver syntax and whitespace pass. Native part of R1 pending root's exact branch workflow. No production UAC/native helper execution locally. Workflow compiles both production and fixture executables, but executes only the explicit emulated-elevation fixture. Caller identity/native policy tests, pre-confirm cancellation, real parent death and one caller-guarded VSS capture are selected; old capture11 not rerun. Production app wiring/packaging and actual UAC remain unproven.
