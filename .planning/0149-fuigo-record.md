# Fuigo discovery and browser PATH — 0.1.49

Contract: diagnose the shared bundled-engine discovery/browser PATH path and correct evidenced diagnostic loss. Preserve the retained Electron browser architecture and user-installed Fuigo precedence. Exclude customer-host root-cause claims (app version/package format unknown), installer changes, credentials, publishing and unrelated refactors. Acceptance: absent/non-executable/failing declared bundle produces actionable engine repair diagnostics; missing user-installed CLI remains distinguishable; clean PATH fixture reaches default bundled CLI without Node/npm lookup; browser MCP mounting retains engine PATH. Checks: focused env-path/Fuigo/ACP tests and server typecheck on macOS; fixtures prove process/driver behavior, not native Linux customer operation. Upstream OpenMausBot #925 inspected: its browser environment PATH overwrite does not exist in our unified-browser proxy integration. Maximum two verification rounds; stop after passing scoped checks and commit, or name failed criterion and bounded remedy.

State: scoped driver correction ACCEPTED. Round 1/2 passed; corrections 0/2. Base 2a957b67. No customer-host root cause established.

Implementation: ACP preserves ENOENT, permission error, timeout, exit code and empty-version distinctions without copying stderr/error.message. Fuigo failed default-binary probes preserve declared bundle resolution errors and advise repairing/reinstalling Murage, explicitly stating Node/npm is unnecessary. Custom/user-PATH engine failures retain their own diagnostics. No PATH architecture change was warranted.

Evidence: `pnpm exec vitest run server/env-path.test.ts server/drivers/acp/fuigo.test.ts server/drivers/acp/acp.test.ts` — 3 files passed, 105 tests passed, 7 platform skips (30.63 seconds). `pnpm exec tsc -p tsconfig.server.json --noEmit` — exit 0. Tests cover default bundled executable lookup with empty PATH using /bin/sh fixture, missing resource, missing executable permission, exit 7 without stderr disclosure, missing custom CLI, and successful ACP turn with browser MCP PATH isolated from engine PATH. Browser test uses existing fake ACP engine and does not claim a real browser launch.

Native local check: `env -i PATH=/tmp/murage-fuigo-version-sZAprG HOME=/tmp/murage-fuigo-version-sZAprG FUIGO_HOME=/tmp/murage-fuigo-version-sZAprG /Volumes/Mando/WaylandBots/murage-astra/dist-native/fuigo/darwin-arm64/fuigo --version` — exit 0, `fuigo 1.0.8 (ba7ad5430c61)`. Empty task-owned home/PATH provides native no-Node/npm lookup evidence on macOS only. This staged binary is NOT the released 1.0.9 producer artifact; release identity/Linux host proof remains with root. Temporary directory removed after check.

Root handoff: UI EngineSetup.tsx always renders static install.needsNode/npm command; root owns suppressing that misleading CTA for declared default packaged Fuigo. This driver package cannot claim customer-visible repair guidance complete until that seam is handled. Dependency symlink node_modules points at main candidate dependencies and is untracked/ignored. No push/publication authorized.
