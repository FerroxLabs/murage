# N7 independent direct threads — runtime contract

Base: 4b7dbb2c, isolated work/0149-threads. Outcome: existing task IDs become independently running direct threads, maximum three per bot. Each owns model/account/effort, approval policy, cwd, unread/activity and exact Stop target. Bot identity, skills and audience-scoped Murage memory remain shared. Groups remain serialized. Shared browser/computer/overlapping explicit workdir resources remain exclusively leased through terminal cleanup; a cancellation does not release a resource before teardown is confirmed. No ambiguous old-client targeting or sender permission inheritance.

Upstream inspected at exact revisions: #981 merge c5a8d104a3e896c07a1afa4d86fc39ae663aac84 (feature bdffd35794738a92c451a4ddfca9f9fdd3aa0cfe), #982 d0046e9e8db9cd0ceab26407e87259e3263115bf, #983 e014babe6fe162e9a7397dc37e0f025725f334c2. Upstream and Murage LICENSE contain Apache 2.0. #981 changed-file API contains no package/lock changes; resource helper uses Node built-ins. No wholesale merge, native mobile source, folder UI, source notebook memory_update system or new dependencies selected.

Concrete differences: Murage has existing TaskRecord threadId/history/cursors/cwd, but no task model/approval/unread/activity snapshot; Store.setActivity changes one bot slot. server/index.ts direct claims and generation maps are keyed by botId; dispatch rejects bot.busy, picks bot.modelSelection and changes bot unread; settlement sets the whole bot idle. Interrupt resolves bot.modelSelection and a selected thread fallback. Task creation/switch revoke all bot capabilities. Unattended state is keyed by bot. Browser is Murage's retained unified Electron proxy, distinct from upstream's replacement browser implementation. All these seams must move together before enabling concurrency.

Minimal dependency order:
1. Adopt the small generation-owned resource lease helper with canonical workspace aliases/ancestor collisions. Add a direct-thread run registry that owns at most three snapshots per bot and fences stale dispatch/cancellation/settlement.
2. Root-coordinated Store task fields/default migration/projection and per-task activity/unread, preserving original IDs/transcripts/cursors and ordinary profile defaults. Runtime-only state does not survive restart. Per-thread settings changes use existing owner validation; no inherited request-sender elevation.
3. Root-coordinated dispatch/queue/event/Stop/approval routes: explicit target, provider snapshot, thread-keyed generation/unattended state, per-thread queue eligibility; native resource claims held until teardown/final screenshot settles. Default new task workspace is isolated. Legacy multi-task requests without threadId reject with guidance; an explicit wrong thread never falls through to another run. Inbox consumes stable threadId+messageId+runId; source messages remain authoritative.
4. Root UI ChatView/Sidebar wires selected-thread controls and later navigation; do not change their current concurrent edits here.

Ownership: new server/turn-resources.ts and independent-thread helpers/tests. Store/index modifications are a proposed seam until coordinated with root. Root owns final integration/UI/schema decisions. No cloud, live profile or native dependency installation. Existing native coverage remains unproven unless exercised; coordination is not a sandbox against arbitrary same-user shell commands.

Frozen checks (same package, max two rounds): resource/registry tests; affected Store/task/queue tests; client/server types; isolated HTTP fixture with two concurrent distinct model/account snapshots, Stop-one leaves sibling running, cap three/refuse fourth, stale cancellation and completion-before-handshake, shared resource refusal, exact approval target, missing/wrong thread refusal and existing ID/history restart persistence. Root integration and UI proof are part of acceptance, not assumed from helper tests. Current: runtime checks ACCEPTED at round 2/2; final selected-thread UI remains root-owned and pending. No third runtime round authorized. Stop with integrated verified N7 or explicitly pending named requirement and concrete seam, without weakening current gates.

R1 initial helper evidence: `pnpm exec vitest run server/turn-resources.test.ts server/independent-thread-runs.test.ts` passed 7 tests / 2 files (5.34s). Classes provide atomic multi-resource admission, thread generation fencing, snapshot detachment, three-run capacity including stopping/settling, and explicit old-client target refusal. Root then granted Store/index ownership.

Runtime implements task snapshots/activity/unread, guarded owner task settings, targeted message/Stop/approval/edit/read/branch routes, thread-keyed unattended state, separate default desks and shared computer/browser claims. Existing ProjectTurnLeases protects checkpoint restores and is retained; additional TurnResources claims supply direct writer-vs-writer exclusion. Shared screen preview remains bot-owned and is serialized between that bot's screen turns; terminal handling checks exact thread. Provider reload disposes all retained runs before clearing their leases. Store publication stages settings before persistence so failed saves cannot publish partial settings.

R1 evidence so far: resource helpers 7 pass; `vitest run server/independent-task-store.test.ts server/tasks.test.ts server/store.test.ts server/steer-queue.test.ts` 141 pass (16.21s). First server typecheck found three introduced typing issues; corrected (unused sender parameter, browser-owner UUID inference, local-computer destination union). Confirmation is part of combined R2. Runtime HTTP fixture prepared for concurrent fake engines, cap/Stop/approval isolation and workdir conflict. Root authorized merging d9b70154 before HTTP proof; candidate commit is isolated and explicitly UNVERIFIED, not integration approval. Full N7 acceptance still pending.

R1 HTTP against merged root d9b70154: concurrent engine/process/Stop/cap and two real mounted permission-broker socket cases passed; shared workdir refusal failed. Source diagnosis: ProjectFolderLeases deliberately permits writer/writer overlap and conflicts only with restores. Initial assumption that it serialized writers was wrong. Added the dedicated TurnResources workspace claim; no restore lease semantics weakened. Fixture asserts the new explicit working-folder refusal and zero competing provider spawn.

R2 final runtime check: `pnpm exec vitest run server/turn-resources.test.ts server/independent-thread-runs.test.ts server/independent-task-store.test.ts server/independent-threads-api.test.ts server/tasks.test.ts server/store.test.ts server/steer-queue.test.ts server/project-turn-leases.test.ts server/project-folder-leases.test.ts` passed 162 tests / 9 files (30.94s). `pnpm exec tsc -b --pretty false` and `pnpm exec tsc -p tsconfig.server.json --noEmit --pretty false` both passed. Native fake Claude child processes use distinct configured provider instances/models, isolated per-thread workdirs, true simultaneous held turns and the mounted permission broker. No live accounts, browsers/computers or OS sandbox claims. Alias/shared-browser/computer lease mechanics are helper-tested; live native resource operation remains unproven.

Root handoff: selected task controls must use task.modelSelection/autoApprove/busy/unread and send threadId on read/messages/respond/always-allow/edit/branch/Stop. /bots/:id/tasks/:threadId PATCH supports title/modelSelection/autoApprove/cwd/unread; settings require desktop proof and idle target. Task modelSelection carries connectionId and effort. Existing bot fields remain shared defaults/aggregate activity. API rejects ambiguous old clients once multiple tasks exist. Whole N7 is not complete until root UI integration is verified. Task-owned fixtures cleaned up by launcher; dependency symlink removed after verification. No push.
