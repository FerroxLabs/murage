# 0.1.50 watch-state foundation — approved implementation contract

Coordinator supersedes the proposal below: implement ONLY new `shared/routine-watch.ts`, `server/routine-watch-state.ts`, `server/routine-watch-state.test.ts` and this record. No actual source chosen; no routines/index/Inbox/UI edits and no provider/background activity. Outcome: pure, serializable state transitions with an opaque typed source-adapter contract, checkpoint baseline/change detection, duplicate wake suppression, bounded checks, expiry and pause. This is a foundation, not an end-to-end proactive watch. The prior routine-watching-routine source proposal is rejected and inactive.

Acceptance: deterministic fake-clock tests establish reserve-before-read protocol, duplicate suppression after JSON roundtrip, one in-flight check, caps/expiry/pause, failure charging, stale completion rejection, and checkpoint baseline/changed/unchanged behavior. State inputs fail closed; transitions do not mutate input. Persistence remains the future caller's responsibility; no claim of actual crash-safe integration. Source authority must be revalidated by a future adapter on each read; an adapter reference carries no permission.

Frozen checks: R1 `rtk pnpm exec vitest run server/routine-watch-state.test.ts`; isolated strict TypeScript check of the two new modules using existing local tsc (ES2023/NodeNext/noEmit/allowImportingTsExtensions/skipLibCheck). R2 only eligible corrections to these checks. Counters start 0/2; no prior watch verification ran. Stop at verified foundation and coordinator handoff; broader integration/source selection pending.

Implementation complete in the three approved new source/test files. R1 PASSED on shell Node v25.8.1: Vitest 4.1.10, 1 file / 10 tests, reported start 00:30:48; targeted strict tsc exited 0. This is not Node24 qualification; coordinator was informed for integration disposition. Rounds used 1/2, corrections 0/2; no code correction warranted. Tests use supplied numeric clocks and synthetic SHA-256-shaped fingerprints, with JSON serialization roundtrips, never files/providers/credentials or source-adapter calls. MaxChecks is strictly 1–10,000 to bound the retained deduplication ledger; no default cadence/source/expiry selected by this foundation. Failed and abandoned reads stay charged; pause abandons pending checks, and resume cannot revive them. Only committed changed outcomes are eligible for future publication. Pure foundation implemented and focused checks passed; coordinator integration pending. Actual adapter authority, atomic storage, scheduler wake handling, quiet-hour delivery, Inbox links and UI remain unimplemented and unverified. No end-to-end watch completion claim.

## Accepted foundation closure — 2026-09-10

Coordinator authorized committing only this record and the three foundation files in `work/0150-integration`. Coordinator independently confirmed the same 10 tests PASS on Node24, also recorded in parent `.planning/STATE.md`; reuse that confirmation and the prior strict TypeScript pass. No checks repeated and no new audit opened during closure. Verification budget consumed: original R1 plus coordinator Node24 confirmation; corrections 0/2. Disposition: ACCEPTED for the pure foundation only. Source files already export their public types/transitions directly; no application imports, barrel exports or scheduler wiring are required for this scope. This commit integrates the accepted standalone foundation; it does not implement an actual source, routine execution, Inbox delivery or an end-to-end proactive watch. No push or publication authorized here.

Next source selection remains pending. The approved parent plan promises Chief-owned, user-approved read-only watches with bounded usage; it does not identify the first backend. An external-research watch needs an exact user-selected resource and observable fields plus per-read authority and fetch limits before an adapter can be implemented. An internal routine-result watch has existing Store/routine/Inbox seams, but the earlier routine-watching-routine proposal was explicitly rejected; it cannot be substituted for the intended first source. Neither option is implemented or newly authorized by this closure.

## Historical proposal — not authorized implementation scope

Authority: parent `.planning/MURAGE-PLAN.md:49` and latest parent `STATE.md`; inspected integration checkout `work/0150-integration`. This is the watch package contract, not a competing programme. Current ownership: this file only; no production edits or checks run. Rounds 0/2, corrections 0/2. Stop after contract handoff until coordinator assigns source/UI ownership.

**Outcome and scope.** A Chief conversation proposes a read-only watch, the owner confirms its exact source, cadence, expiry and usage cap in the existing routine card, and subsequent changes appear once in persistent Inbox with their original result link. Reuse RoutineManager, its atomic file and receipt-based confirmations; no new scheduler. Proposed first vertical source is an explicitly selected existing routine or room-goal result in an owner-accessible task (typed Store read, no engine dispatch). This source choice requires coordinator disposition before implementation: the approved programme does not name a first source. It proves the watch lifecycle without claiming support for arbitrary websites, connectors or agentic research. Those source adapters remain pending programme work if required, never silently treated as delivered. No cloud, external sources, credentials, paid calls, general tools, sends, task delegation, memory promotion, or new responsibilities. Scoped memory may help Chief formulate a proposal; it cannot activate or resurrect one.

**Current seams and proposed change.** `server/routine-requests.ts` schemas/normalizeDefinition/inputFromDefinition/cardCopy/resolve and `shared/routine-request.ts` own strict confirmation payload, digest, optimistic revision and durable receipt. Add optional strict `watch` definition: `{version:1, source:{kind:'routine-result'|'goal-result',threadId,sourceId}, expiresAt, maxChecks}`; initial proposal defaults: existing interval cadence >=5min, expiry 7 days, maxChecks 100, explicitly visible/editable before confirmation. `server/routines.ts` Routine/Run/Input/load/sanitize/newRun/tick/commitMutation own persistence and admission. Add manager-owned watch state `{checksUsed,checkpointHash?,lastCheckedAt?,lastPublishedHash?}` and per-run `{watchOutcome:'baseline'|'unchanged'|'changed',sourceLink?}`. Validate safe integers/future expiry, source identity and ownership at confirmation and each read; malformed saved watch fails closed, never falls back to normal prompt execution. Reserve each check atomically before reading, bound one active run, cap all triggers including manual/webhook, and stop at expiry/cap. Existing enabled pause/resume and global automaticPaused apply; neither pause nor restart resets checkpoints or counters. An update to source/bounds requires the existing new confirmation and revision checks.

`server/index.ts:4181` constructs RoutineManager: add a narrow `readWatchSource` callback backed by authorized `store.messagesFor(threadId)` and a specific existing `routine.run`/`goal.run` identity, returning allowlisted status/summary/link only. Branch watch dispatch before createTask/startTurn/startGoal: it must never reach provider, arbitrary MCP or goal orchestration. This provides read-only enforcement structurally; current EventActionBudget's four creates/four handoffs is not a read-only or monetary cap. Hash canonical allowlisted snapshot, ignoring volatile timestamps; first read establishes baseline, equal reads remain quiet, changed result persists checkpoint and receipt together in routines.json before projection. A restart replays only the committed changed receipt; `syncRoutineRunToSource` already upserts by runId and must leave baseline/unchanged out of unread and Inbox. Preserve source identity in `shared/routine-run.ts`; original task/message link is carried with result and existing Inbox navigation. Inbox currently hides empty completed summaries (`server/inbox.ts`), but explicit watch outcome exclusion is clearer than relying on blank text. Reuse `shared/notification-preferences.ts` quiet hours as delivery suppression (durable results stay available, no delayed alert replay), not a second scheduler. No semantic-memory or raw-transcript hashes.

**Ownership requested.** Watch worker: `shared/routine-watch.ts` (new strict types/helpers), `server/routine-watches.test.ts` (new), watch-only portions of `server/routines.ts`, `server/routine-requests.ts`, `shared/routine-request.ts`, `shared/routine-run.ts`, `src/lib/routines.ts`. Coordinator or explicit exclusive grant: `server/index.ts`, `server/inbox.ts`, existing routine confirmation/result rendering components and a focused human fixture. No modifications to goals, permission providers or scoped-memory storage. Identify renderer files before assigning that lane; no parallel edits to these shared seams.

**Acceptance and frozen checks (before implementation).** Node24/macOS isolated synthetic profile only. R1: `rtk pnpm exec vitest run server/routine-watches.test.ts server/routines.test.ts server/routine-requests.test.ts server/inbox.test.ts server/routines-background-pause.test.ts server/routine-write-gate.e2e.test.ts`; `rtk pnpm typecheck`. Focused assertions: unconfirmed/nonowner/wrong-thread source refused; normal routines unchanged; baseline/no-change quiet; one changed result with correct original link; persisted checkpoint+counter across restart and crash-between-save/projection yields exactly one result; duplicate schedule/event wake coalesces; pause/global pause/expiry/cap all forbid further reads; resume preserves usage; unknown/corrupt source fails closed; watch never calls startTurn/startGoal or provider/tools; quiet hours suppress external notification while preserving Inbox result. Isolated real server HTTP fixture uses desktop proof, synthetic source message, confirmation, trigger, restart and Inbox retrieval (extend existing write-gate protocol, not live app). UI fixture must verify proposal bounds/pause/resume and result navigation at 390/1440 plus keyboard, including source removed. Freeze its concrete path/command after renderer owner maps existing components, before R1 starts. R2 only invalidated checks and required integration after eligible correction; no third round. ACCEPTED only with implementation, these gates and authorized integration complete. This document is planning evidence only, not runtime or full watch-programme qualification.
