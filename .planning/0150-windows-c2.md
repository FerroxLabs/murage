# Windows C2 bounded diagnostic

Contract: from6ff812bb, determine whether inherited stdio is necessary for the observed CreateProcessW error5. Diagnostic only; production qualification remains BLOCKED. Preserve the exact zero-capability AppContainer SID, synthetic image/cwd ACL setup and standard-user wrapper. No launcher/server/Linux edits, host policy, network allowance, credentials or existing recovery VM. One disposable windows-2022 contents-read/no-secrets CI invocation, Node24. Children are created suspended and terminated without executing; these outcomes never substitute for isolation canaries/full Fuigo session/rollback acceptance.

Prior evidence: C1 run34460533833 successfully created/deleted the random profile but failed CreateProcessW(error5). H1 inherited pipe access is the discriminating candidate; H2 image/cwd ACL intersection; H3 token/process security. Capture image/cwd ancestors, process and stdio SDDL, token groups/restricted SIDs/integrity/default DACL/elevation. Single paired test retains identical AppContainer capabilities: inherited stdio baseline, then no inherited handles/std handles. No permissions are granted by the discriminator. If baseline5/no-inheritance0, inherited handles are causally required for this launch failure; if both5, pipe inheritance alone is not sufficient and ACL/token evidence informs next remedy without another run.

Checks frozen: Node24 syntax; git diff whitespace; native MSVC compile in the single invocation; bounded receipt and exact profile cleanup. Local source prepared; explicit Node24.20.0 syntax PASS and new-file whitespace checks PASS (git no-index returns1 for a new-file difference with no whitespace diagnostics). Native compilation/runtime pending; native C2 invocation0. Stop after C2 evidence/classification; root owns push. No production correction or integration authorized by diagnostic success.

## C2 result — BLOCKED

Run34462166681, source67cc88872d1b4c29bc52ed6cf12584f57847dd30, native windows-2022, Node24.20.0. MSVC compilation and diagnostic collection PASS. The one paired native test returned CreateProcessW(error5), pid0 both with inherited stdio and with no inherited handles/std handles. Therefore removing pipe inheritance alone does not resolve this failure. Neither suspended child was created; no child executed and no AppContainer canary/full Fuigo session passed.

Exact receipt: .planning/0150-windows-c2-receipt/launch.json, artifact fuigo-windows-c2. Image SHA256832df28beb538663aac5e1026b0d5a20bbe3f621548b19efd0022099c7de65ac. Parent image read/execute and cwd traverse/list checks succeeded. Image and cwd DACL explicitly grant the exact AppContainer SID full access; ancestors above the probe directory do not. Parent integrity is medium8192 and Administrators SID is deny-only16. Additional token observations: TokenElevation1, IsTokenRestricted0, default DACL grants generic-all only to Administrators/SYSTEM and generic-read/execute to the logon SID. These observations identify remaining token/object-security and filesystem-traversal hypotheses; they do not identify which Windows check produced error5. IsTokenRestricted0 alone is not proof the administrator disablement failed, since disabled groups and restricting SIDs are distinct token properties.

Profile murage-fuigo-probe-2d4560c1-c4b3-4c19-83c7-50d765a056cf was deleted successfully; receipt profileDeleted=true. Child count0, no network-capability or host-policy mutation. C2 invocation1 consumed, no rerun/correction. Production Windows remains BLOCKED/unqualified. A further bounded diagnostic grant would be needed to distinguish explicit task-owned process/thread security descriptors from the retained default DACL before considering any production change; do not grant access to owner-home ancestors or relax AppContainer isolation on this evidence.

## Approved extension — C3 contract

User approved the pending Windows process-security diagnostic and evidenced correction extension. Preserve R1/R2/C1/C2 counts and results. C3 is one paired native invocation: inherited-stdio/default process-thread security baseline versus identical launch with explicit protected DACLs on the new task-owned child process/thread, granting generic-all only to current TokenUser, SYSTEM and the exact AppContainer SID. No token, integrity label, capability, handle-inheritance, image/cwd/ancestor ACL or host-policy changes. Both children remain suspended and are terminated; success is launch diagnosis, not canary/session qualification.

Frozen C3 checks: explicit Node24 syntax, whitespace, native MSVC compile, paired result receipt and exact profile cleanup in one disposable contents-read/no-secrets Windows CI job. If baseline5/explicit0, new-object security is causally implicated and an evidenced minimal launcher correction may be proposed to root for C4 confirmation. Both5 leaves the hypothesis unsupported and stops this diagnostic cycle; no new discriminator or rerun. C3 invocation0; C4 unused. Production launcher untouched; root owns push and integration. No Linux changes/check repeats, global-policy changes, owner-folder grants, network exemptions or recovery VM use.

C3 preparation: Node24.20.0 syntax and git diff whitespace PASS. Native compile/runtime pending; C3 invocation0. Receipt destination .planning/0150-windows-c3/launch.json, artifact fuigo-windows-c3.
