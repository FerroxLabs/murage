# 0.1.50 file watch integration

Contract/root grant: continue the EXISTING watch requirement in isolated `work/0150-watch-flow` from8da3e20f. Foundation8bc409c8 and bounded file adapterb8dcaac2 already present; preserve their accepted evidence. Root granted exclusive edits to watch seams in routines.ts/index.ts, routine requests/shared types, strict agent proposal schema and source-picker controls. Not alone in the codebase; no other worktree edits.

Outcome: explicit user selection of an existing relative file inside a current approved bot working folder, then existing durable routine confirmation. Server binds bot/workspace identity and revalidates at confirmation/every read; missing workspace stays unavailable and is never created. Existing timer, atomic routines.json and run receipts own cadence, reserve-before-read, checkpoint-before-publication, expiry, maxChecks<=10000, per-watch pause/resume, global automatic pause, dedupe and restart recovery. Watch branch never calls provider/createTask/startGoal. First baseline and unchanged reads stay quiet; changed/failed results link from persistent Inbox to original source conversation, upserted by runId. Existing notification preferences own quiet alerts. Chief optional propose_routine watch schema remains proposal-only. No URLs/network adapter, sends/spends/writes, owner files, paid model calls, new scheduler or automatic activation.

Frozen checks: joined fake-clock RoutineManager + real bounded file adapter + RoutineRequestService tests covering confirmation/restart/dedupe/caps/expiry/pause/revocation and unchanged ordinary routines; isolated actual owner HTTP confirm→baseline→modify→Inbox source link→restart/no duplicate; server/frontend types; picker visual/interaction at390/820/1440 using existing tokens. Preserve all failures and correct only observed acceptance failures under latest user continuation. Rounds0; corrections0. Stop at verified isolated commit and exact remaining platform/combined candidate limitations, not at old counters.

Current: typed watch binding and source scopes, atomic manager read branch, durable routine confirmation/MCP payload, current-workspace root adapter, changed-only Inbox projection, owner file-picker/proposal endpoints and existing-token UI controls implemented. Joined tests now written; R1 focused ordinary/watch suites and frontend/server types next, followed by actual owner HTTP/restart and picker browser checks. No watch started on owner data. Skill inspection: design-system describes marketing-site layout; root explicitly directs existing Murage application tokens/forms, no redesign. No source/expiry/max-check reset on ordinary edits or resume; source/bounds changes require a fresh confirmed watch and retained old history.

R1 results: focused52233 =109PASS/2FAIL of111. Ordinary routines54, requests46, global pause3 all pass. Two new watch assertions used oldest instead of newest `listRuns` row; source confirms descending scheduledFor order, assertions corrected without changing outcomes. Server types92056 found optional notification summary and closure mutation narrowing; corrected summary fallback and check outcome predicate. Frontend types17625 passed. Required Node24 direct adapter import failed ERR_UNSUPPORTED_TYPESCRIPT_SYNTAX on preexisting constructor parameter property (plain --check had passed and is not runtime proof); corrected to explicit readonly field plus assignment. Original receipts retained.

## Accepted file-watch slice

Implementation and the frozen file-watch checks now PASS on macOS arm64/Node24.20.0. This is the useful user-selected FILE slice, not arbitrary web/connector monitoring or all-platform packaged acceptance. Ordinary routines retain their existing provider execution behavior; watch runs use no execution task, provider turn or open action budget. Source selection does not create a working folder. Chief-owned cross-bot watches retain their reporting/management owner, while each read is bound to the watched bot's current folder identity.

Evidence and counters (historical failures are not reset):

- `watch-flow-focused-r1.json`, session52233:103 unchanged ordinary checks PASS (54 routines,46 confirmations,3 global-pause), six initial watch checks PASS and two fixture-order failures recorded above. Those ordinary passes remain unaffected.
- `watch-flow-focused-r2.json`, session67914:8 joined watch checks plus20 bounded file-adapter checks PASS. Covers confirmation, durable reserve/completion, real file hashes, failure charging, cap/expiry, pause/global pause, revoked scope, interrupted restart, failed atomic saves and malformed saved run refusal.
- Completion of the named Chief workflow revealed the internal list route still filtered on the watched bot. Two watch-specific owner expressions corrected; ordinary ownership unchanged. `watch-flow-chief-confirm.json`, session78963:all9 joined cases PASS including Chief pause/resume of another bot's watch and watched-bot management refusal.
- `watch-proxy-confirm.json`, session57587:9 selected routine/MCP checks PASS,24 filtered. Strict file-watch payload reaches confirmation-only request; unknown account-permission field refused. Not the whole MCP suite.
- Actual server/file/Inbox process check22781 passed initial5 cases; confirmation29998 added actual scoped-agent list ownership plus pause/resume and all6 cases PASS (`watch-flow-http-confirm.json`). Real file baseline/unchanged produce zero Inbox results; changed file produces exactly one linked result in original Chief thread. Existing quiet preferences suppress notifications. Actual process restart preserves four charged checks/checkpoint with no duplicate publication. Fixture uses disabled engines, supplied RoutineManager clock, notification observer and production capability functions to admit synthetic local agent callers; no real engine/model/account call. The real file adapter, owner/scoped-agent HTTP guards, routine service, atomic state and Inbox are loaded unchanged.
- Browser94838:390/820/1440 file choice and confirmation handoff PASS with no browser errors/overflow, screenshots visually inspected (dark390/1440, light820). Negative missing-folder case correctly refused activation and native Escape closed the dialog, but the fixture's built-in `window.closed` flag assertion failed. Custom fixture flag + eventual callback observation corrected; confirmation18538 passed the exact negative case (`watch-picker-confirm.json`). All4 scoped browser cases now pass; no UI product correction. Prior trace/error/snapshots retained in `watch-picker-r1/`.
- Final server types15228 and frontend types70148 both exit0. Earlier server confirmation39892 also passed before the two-line owner-list completion. No full build/package/native Windows/Linux claim.

Cleanup: HTTP check roots `murage-watch-http-0dN8Ib` and `murage-watch-http-uqs5zV` removed only after passes; children52287/52373 and59999/60023 exited0. Browser servers and task-owned Vite caches close in fixture teardown. Failed and passed receipts/screenshots remain in this worktree. No owner files, credentials, normal sessions or other worktrees changed.

Next authorized action: commit this scoped source/test/evidence package for root integration, then parent combined private candidate/platform verification. No release/publication. Existing state/file foundation receipts remain accepted and unchanged except the demonstrated Node24 syntax correction in the file adapter.
