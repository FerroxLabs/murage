# 0.1.51 macOS shutdown investigation

Contract (2026-09-10): diagnose the intermittent private 0.1.50 macOS shutdown timeout from preserved R1/R2/C1/C2 receipts and exact release-source credential lifecycle. Scope is source/receipt inspection and a proposed isolated discriminating check; no native app launch, test loop, product changes, owner Keychain/profile/process changes, paid/network registration, or release mutation. Acceptance: distinguish confirmed queue coupling from unproven native/fixture causes, preserve historical counters and name the next bounded check. Stop after evidence classification and recommendation. Owner: mac_shutdown_0151; other agents own the repository and release work.

State: diagnosis complete within read-only scope; root-cause attribution remains unresolved. Original R1/R2 and approved C1/C2 exhausted; zero production corrections. This investigation does not reset or extend those budgets. No new execution checks authorized or run.

Source identity: signing-fix HEAD 15c3cbd64056a7288777739f5dd244ca4365ea0b. Historical native receipts use f320e846fe4ad49a277f7b8a14c361cce8d51e32 / app.asar 28cc67406293a47f7e24cdb527538350edf37ccb90983aee9d610a2b0b30ba7c. `git diff f320e846fe4ad49a277f7b8a14c361cce8d51e32 HEAD -- electron/main.mjs electron/secure-credential-state.mjs electron/managed-composio.mjs` returned empty: the relevant production files are unchanged.

## Confirmed evidence

- Historical source record: `/Volumes/Mando/WaylandBots/murage-astra/.planning/0150-private-mac-f320e846/.planning/PRIVATE-BUILD-F320E846.md`; raw receipts below are in its sibling `current-private-evidence/`.
- R2 `native-smoke-r2/desktop-server.log`: registration logged 10:10:19.161Z, server exit0 at 10:10:21.848Z, cleanup incomplete at credential writes 10:10:31.857Z. Lifecycle record reports quit started 10:10:21.843Z, no will-quit/quit, 20s fixture deadline followed by exact-owned fallback. This is a real unsettled credential queue, not merely a Playwright transport timeout. No per-native-method observation exists for R2.
- C1 `native-shutdown-c1/quit-lifecycle.jsonl`: `saveSecureCredentials:402` availability call ran 10:17:04.589Z–10:17:07.298Z (2709ms), overlapping before-quit 10:17:04.933Z; encryption then rejected and normal quit0 followed. C2 corresponding file: availability 10:20:46.528Z–10:20:48.714Z (2186ms), overlapping quit 10:20:46.876Z; encryption rejected with generic Error at 10:20:48.715Z, quit0 10:20:48.745Z. Neither repeats the 10s timeout, and neither proves persistence. Reaching encryption after the availability check implies its result was truthy; availability alone did not guarantee usable encryption.
- Main source `main.mjs:628–640`: every runtime credential mutation enters `secureCredentialState.update` and `credentialWrites`; removal occurs only when the operation settles. `secure-credential-state.mjs:58–85` serializes the entire asynchronous derive plus persist; a blocked optional derive/persist also delays later user credential mutations.
- `main.mjs:2720–2738`: optional connected-app registration is one such queued derive. Its shutdown signal is forwarded to fetch. `managed-composio.mjs:40–105` catches request errors/cancellation and returns credentials; `secure-credential-state.mjs:76–77` persists unconditionally, even for an unchanged document. Therefore cancelling optional network work does not guarantee bypass of native encryption. This is confirmed source behavior; it is not established as the trigger of R2, whose registration had already succeeded.
- `secure-credentials.mjs:30`: a fresh profile with no credentials file returns empty without probing native availability. In this fixture, optional registration is consequently the first observed native credential initialization, after the initial window becomes usable.
- `main.mjs:2811–2869` aborts registration, stops owned server children, waits startup, drains credentials, then releases the installation lease only after all cleanup succeeds. `server-child-lifecycle.mjs:37` supplies the 10s deadline. Timeout reports unresolved work and retains ownership; dropping that guard or extending the deadline would not establish safe completion.
- The original smoke runner lines58–62 uses fresh HOME/data/userData and `--disable-background-networking`, but supplies no broker override and no explicit native Keychain fixture. `main.mjs:474–478` therefore selects the packaged default broker. Registration log and source establish this network path was exercised; the Chromium flag did not isolate application fetch. HOME isolation does not itself supply evidence of which native credential store was reached. No owner Keychain was inspected now.
- Registration's log occurs inside `ensureManagedComposioCredentials` after the supplied no-op save callback; the real queue persistence follows later. That log proves server registration, not encrypted custody. C1/C2 demonstrate this distinction directly.
- Retained `/private/tmp/murage-0150-build-recovery-fdatWt/private-cohort/.planning/cohort-mac-r1-{arm64,x64}-evidence/receipt.json` each names exact15c, workflow6dae8ec222739d64583a3c7b575d4ab9ed79be94/run34473310308, Node24.20.0, native window/quit success, published=false. These preserve later basic native acceptance; they contain no credential timing/error/custody observations and do not prove the intermittent cause was corrected.

## Classification

H1: native encryption initialization/persistence latency, exposed by optional startup work sharing the durable-write queue. Queue coupling is confirmed; native delay and rejection are confirmed for C1/C2; extrapolation to the R2 10s stall remains a hypothesis. No native error message/code, Keychain identity, prompt state, or access status was retained, so OS/backend cause cannot be resolved from these receipts.

H2: fixture/environment contribution (fresh redirected HOME plus ad-hoc private package and uncontrolled broker timing). Those conditions are confirmed, causality is not. Later signed CI success changes host/signature/timing at once, so it is not a controlled discriminator.

H3: outstanding registration network request caused R2. Less supported: registration was already logged before quit, and both diagnostic runs entered native availability before quit. Network abort is nevertheless insufficient as a general guarantee because the unchanged-document path still persists.

Impact: observed intermittent failure of graceful shutdown in the historical private fixture; source also permits optional work to delay explicit credential saves. No lost credentials, customer-profile failure, or current signed-release regression has been demonstrated. Existing queue serialization and lease retention protect consistency and must remain intact. Reliability lane owns further diagnosis; no severity inflation or adjacent audit.

## Exact next bounded check, not executed

Request a new explicit execution extension for one deterministic, Node24-only fixture against the exact three credential modules; no Electron app or native Keychain access. Inject fetch and persist providers and use deferred promises, not time-based guessing. Preconditions: initial fake document, explicit fake broker token/installation response, no real network provider, and a test-owned abort controller; retain source identity and one receipt. The triggering operation is the production optional-registration derive wrapped in `createSecureCredentialState.update`, followed by abort and an attempted second fake user-credential mutation.

Two cases within one frozen check set: (A) registration fetch rejects on abort and leaves the initial document unchanged; assert whether persist is invoked anyway, then release it and verify unchanged state. (B) registration succeeds with fake credentials and persist is deliberately pending; abort, assert the second mutation and queue drain remain pending, then release persist and verify both documents commit in serialized order without overwriting earlier fields. This distinguishes product queue semantics independently of native environment, without reproducing or claiming the macOS backend defect. These are exact source-based predictions, not results.

If A confirms unnecessary persistence, a narrowly scoped optional-registration cancellation/no-change correction can be proposed separately; it must retain durability for a completed registration and all user writes. It will not by itself resolve the historical C1/C2 path, which had registered and entered native encryption already. Do not remove optional work from tracking or force lease release while persistence remains active.

Only a further explicitly authorized native gate on a disposable macOS user/VM with its own credential store can discriminate native environment next: loopback broker with fake registration, unchanged exact signed app, trigger quit on the observed availability-call entry, retain method timestamps plus sanitized native error code/category and owned-store setup state. Use the same 20s bound and owned-process/lease checks. No normal-user Keychain search-list/default-store mutation or credential import. A native success without reproducing the failure narrows uncertainty but is not a fix claim. This gate is not authorized or started here.

Final disposition: scoped investigation ACCEPTED as source/receipt diagnosis; root cause and product remedy unproven, execution extension pending. No native launch, tests, process changes, network calls, product edits, integration or release actions performed. Only this record was created/updated.
