# P04 recovery record coverage

Source: working tree based on `03342b462c6b8d69b5cf1ff3c89ccdcbcb656238`, 2026-09-06. This is a source inventory, not a repeat of the accepted historical recovery proof. Controlling scope: MURAGE-PLAN.md P04 and RECOVERY-CLOSURE.md.

## Current bounded correction and frozen contract

Outcome: section-context briefs accepted for backup/restore retain their identities and contents; records the runtime would discard or merge are refused. Scope: installation-record-validation.ts, its test, restore-preparation.ts validation list and its test. Acceptance: v1, finite timestamps, canonical section keys including empty General, at most 24,000 UTF-8 bytes per brief; unknown metadata preserved. Invalid/future/malformed/oversized/colliding-key data must not publish backup or prepare restore; source/archive bytes remain intact. No runtime section-context normalization changes, no additional message audit. Round budget: maximum two; round 1 passed, no correction or repeat required. Planned checks: Node24 Vitest installation-record-validation, installation-restore-preparation and installation-state-snapshot tests; server TypeScript. Stop: scoped acceptance or precise failed check. Root owns programme integration. No commits or external writes authorized here.

Round 1 evidence: Node `/Users/seandonahoe/.nvm/versions/node/v24.20.0/bin/node`, `pnpm exec vitest run server/installation-record-validation.test.ts server/installation-restore-preparation.test.ts server/installation-state-snapshot.test.ts`: exit 0, 3 files / 32 tests passed, 2026-09-06 19:36:32 local, session 39929. `pnpm exec tsc -p tsconfig.server.json --noEmit`: exit 0, session 71616. Both commands prefixed `rtk proxy env PATH=/Users/seandonahoe/.nvm/versions/node/v24.20.0/bin:/opt/homebrew/bin:/usr/bin:/bin`. Tests use disposable local installations and hash-valid crafted archives. Scope accepted locally; combined integration remains root-owned, and full P04 is incomplete.

## Declared data and actual treatment

| Persisted component and source | Archive/restore treatment | Remaining coverage |
| --- | --- | --- |
| messages.db: messages, thread_state (`message-db.ts:38`) | SQLite backup in offline lease; strict two-table schema, integrity, indexed column/JSON identity, branch references/cycles; restore expires pending approvals and queued messages, preserves terminal receipts | Message.kind and nested payload validation incomplete; source schema/app provenance absent |
| messages-THREAD.json (`store.ts:552`) | Legacy array or messages/activeLeaf envelope copied; restore graph validation and expiration | Explicit legacy migration receipt and complete message-kind policy absent |
| config.json (`config.ts`) | Allowlisted safe projection; omitted fields recorded; engine credentials/cursors excluded, explicit discovery and all engines off | Source version/platform and complete configuration migration accounting absent; reauthentication remains intentional |
| bots.json, groups.json (`store.ts:550`) | Bot/task cursors removed; restore unique IDs/thread ownership/member existence checks; automatic authority cleared | Full roster/task shapes, duplicate membership and broader relationship policies not declared |
| routines.json (`routines.ts:473`) | v1 routines/runs/request-receipts schemas + ID uniqueness; disabled definitions, pending runs cancelled, terminal records retained | Historical references may legitimately outlive targets; active references/attachments need explicit policy |
| calendar-calls.json (`calendar-calls.ts:184`) | v1 call schema, distinct bot IDs; nextRunAt null on restore | Reference and attachment ownership policy incomplete |
| webhooks.json (`webhooks.ts:285`) | v1 hook/delivery/attempt schemas and uniqueness; dedup evidence preserved; secret removed/replaced, hook disabled/pending verification | Complete relationship/provenance policy incomplete |
| delegation-receipts.json (`delegations.ts:95`) | Terminal schema and unique IDs; historical target absence allowed | Complete declared historical-reference policy pending |
| delegations.json (`delegations.ts:94`) | Generic JSON copied; whole pending queue quarantined | No record shape schema; explicit quarantine disposition exists |
| browser-cleanups.json (`index.ts:385`) | Generic JSON copied; whole queue quarantined | No record shape schema; no replay intended |
| section-contexts.json (`section-context.ts:15`) | Before correction: generic JSON copied; runtime loader silently empties invalid files, drops oversized entries, trims/merges labels | Current bounded correction validates backup and external restore; preserves original unknown metadata |
| skill-state/BOT/skills.json, managed-links.json, staged.json (`skills.ts:449`) | Entire directory copied; manifest entries disabled; staged writes quarantined | Manifest receives only object checks; managed-links has no recovery shape/ownership checks |
| skills/** (`index.ts:353`) | User skill files copied as opaque bytes | Record manifest/provenance schemas not validated; bundled external skill root intentionally distinct |
| attachments/** (`attachments.ts:19`), workspaces/** (`workspace.ts:18`) | Recursively copied; no symlink following; per-file hashes | Attachment/workspace references, external-path migration/refusal need explicit inventory |
| checkpoints/** (`checkpoints.ts:34`) | Shadow Git directories copied as opaque files | Checkpoint metadata/Git integrity and restored path association not validated; project-folder restoration remains P20 |
| events/THREAD.ndjson (`harness/bus.ts:71`), decisions.ndjson and .1 (`decision-log.ts:68`) | Opaque file copies | No event/decision record schema, truncated-tail or provenance treatment |
| skill-index.db (`skill-search.ts:47`) | Omitted derived cache; defines skills/teams FTS5 tables, skill_terms, meta plus FTS internals | Explicit named rebuild policy should replace generic omission description |
| team-library/catalog.json (`team-library.ts:59`) | Remote cache omitted | Local package roots are outside installation, not implicitly exported |
| vm-home/** and vm-homes/** (`container-computer.ts:48,92`) | Omitted by snapshot allowlist | These are persistent VM workspace directories, not demonstrated disposable cache; needs explicit preservation/export/refusal decision within original inventory |
| Native diagnostic logs, provider scoped homes, connector state, connection-profiles/companion | Generally omitted by unknown-component rule; fresh restored connection realm generated | Provider global config remains outside scope; connector receipts/snapshots and app-owned homes need named provenance/credential disposition, not broad raw credential copying |

Snapshot allowlist is `installation-state-snapshot.ts:10-11`: ten root JSON components, six directory components, legacy message JSON and two decision logs. Unknown top-level components are recorded as omitted. Archive manifest (`installation-archive.ts:15`) contains format v1, snapshot ID/time, hashes, missing/omitted paths, DB counts and paused policy; it does not contain source app/schema/platform identity or per-kind migration/preservation policy. Archive hashes are integrity checks, not authenticated authorship. Portable external paths are accepted unless reserved recovery paths; therefore an explicit supported-component/refusal policy remains open.

Messages declare eight kinds in `store.ts:114`: text, options, activity, screen, connector, secret, routine.run and goal.run. `InstallationTranscriptGraph.add` currently accepts any string kind and checks identity/role/time/parent graph only. Nested card/tool/run, sender, attachment, reply/send/turn and comm provenance is copied, not comprehensively validated. Existing restoration does expire active requests and preserves completed data; this inventory does not overturn accepted no-replay evidence.

## Remaining P04 disposition

The section correction is the smallest evidenced record loss prevention patch: the runtime's `section-context.ts:53-62` would otherwise silently discard or merge accepted archive content. Other rows are original P04 coverage work, not permission to widen this correction. Damaged-state preservation export remains separate: ordinary backup currently refuses malformed JSON/database and preserves source in place, which is not a usable damaged-byte export. Fault-injection/disk transaction work is root-coordinated. Abrupt owned-VM power-off is P04-B after P03 host availability. Physical power loss, native dialogs and packaged macOS/Windows/Linux startup/relaunch remain unproved by this local slice. No new claim of complete Task15/P04 acceptance.

## Next authorized slice: VM workspace inventory decision

Initial intent: recover vm-home/vm-homes workspace data through existing bounded offline snapshot without restoring native credential realms. Source inspection found a concrete boundary: `container-computer.ts:147-165` stores full Google Chrome/Chromium profiles in each workspace's `.browser-profiles` and symlinks the guest native profile locations to them. The host workspace is mounted at `/home/cua/workspace` (`:882-885`). Blanket directory inclusion would carry browser credentials into the next VM session and conflict with credential reauthentication.

Root authorized a narrower immediate correction: refuse normal backup whenever either VM workspace root exists until quiescence and credential-safe preservation can be proved. Frozen outcome: `VM_WORKSPACE_BACKUP_UNSUPPORTED`, no published archive, source user files and fake browser credentials unchanged, owned scratch cleaned and lease released. Only snapshot source/test changed; root owns CLI fixed safe guidance. Snapshot/archive focused tests and server TypeScript were the named check set. Round 1 passed: Node24.20.0, `pnpm exec vitest run server/installation-state-snapshot.test.ts server/installation-archive.test.ts`, exit 0, 2 files / 47 tests, session 42959, 2026-09-06 19:40:19 local; `pnpm exec tsc -p tsconfig.server.json --noEmit`, exit 0, session 7980. Commands use the same `rtk proxy env PATH=...` prefix recorded above. No additional correction/round required.

This refusal prevents a successful backup that silently excludes known user data. It is not full VM recovery. Original P04 still requires quiesced workspace preservation/export plus browser-profile quarantine or explicit credential-safe migration; an installation process lease does not demonstrate a bind-mounted guest has stopped writing. No VM lifecycle changes, profile copying or inclusion patch performed. Full P04 cannot close on this refusal.
