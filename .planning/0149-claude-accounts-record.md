# Named native Claude accounts — 0.1.49

Base 0da7dde9, isolated work/0149-claude-accounts. Outcome: named additional Claude accounts reuse provider instances, each with explicit configDir and selected instanceId. Default Claude login/config/HOME/Claude-Mem remain unchanged. Claude CLI owns authentication and credential storage; no copying/import/migration, custom OAuth, proxy, hosted login, automatic switching or new connectionId credential type. No owner profile/Keychain/login mutation during this package's verification.

Upstream #960 inspected at feature 67f6ed2978b86b6d2e08113d5758805a54c816d6 + fix fef5542f462ca95843524f279936b9f4d13f615c, merge da2f9cdf8f43ff016972960c68367e6632a785dc. Apache-2.0 matches Murage; no package dependency additions. Reuse the small account/helper and driver concepts, not the upstream index/UI wholesale. Existing Murage already supports environment.CLAUDE_CONFIG_DIR, native `auth status --json`, per-instance driver sessions and per-task resume cursors. Missing: managed named-account configDir, inherited OAuth override isolation, consistent account environment for catalog/auth/turn/review, account-specific terminal recipe, and individual registry/bus replacement.

Important preservation difference: upstream normalizes an inherited CLAUDE_CONFIG_DIR even without a named account. This implementation must not rewrite the default environment string or HOME: current Claude credential namespaces can depend on that exact string. Only newly explicit configDir accounts get normalized directories and inherited credential/secure-store overrides stripped. Murage's existing explicit provider routing remains authoritative after stripping.

Ownership: agent owns server/claude-accounts.ts (+focused helpers/tests), server/drivers/claude.ts and its related tests. Shared config persistence/registry/bus/index/account CRUD/engine setup/UI metadata/picker are root-coordinated seams, not edited without ownership. Root must preserve unrelated running provider instances, reject changes to assigned/history-referenced account roots and protect default account deletion. Removal removes only registry configuration; never files or credentials. N7 task.modelSelection.instanceId remains the native account identity, with connectionId reserved for existing explicit provider connections.

Frozen checks: scoped helper/driver/catalog/auth tests using synthetic homes, config roots and credentials; POSIX sign-in wrapper execution against a fake native-protocol CLI and deterministic PowerShell quoting/restoration; client/server types. Integrated owner CRUD and two-account fake-process isolation checks once root seams are available, preserving a running sibling. Actual OAuth/Keychain isolation and native Windows auth require separately authorized live checks and are not claimed by these fixtures. Maximum two rounds including integration; 0/2 started, corrections 0/2. Stop with scoped verified candidate and exact remaining integration/auth evidence, or a named acceptance failure and bounded remedy.

Approved seam amendment: use the existing global providerConfigBusy + fleet-idle mutation gate and reloadProviders; no individual registry/bus disposal framework. Refuse CRUD while fleet work is active, preserving running siblings. Agent additionally owns server/claude-account-config.ts: materialize fleet without runtime env/config injection, atomically replace only instances, preserve unrelated raw config/retained unknown fields, return internal-only secret-bearing rollback receipt, verify exact restoration without overwriting a subsequent writer. Root owns reload/rollback status and owner CRUD/UI. The earlier individual-disposal suggestion is superseded, not an acceptance requirement.

R1 starts with helper + driver-account + existing Claude driver/catalog suites and client/server types. R2 reserved for root-integrated CRUD/UI and any eligible R1 correction; no live authentication. Existing test setup isolates DATA_DIR; new fixtures use synthetic homes and auth-status replies. Native fake stream-json child exercises real auth/catalog/turn/review process paths; fixture dumps whitelist environment only.

Current official Claude authentication documentation states that CLAUDE_CONFIG_DIR scopes .credentials.json and the macOS Keychain entry (https://code.claude.com/docs/en/authentication#credential-management). Earlier vendor issue reports describe exceptions and an undocumented secure-storage override; current docs outrank those reports for the supported contract, but version-specific live isolation remains unproven. No raw auth response or credential values will be returned/logged. Source/seam report sent to root before implementation.

R1 outcome: 91 passed, 1 skipped, 1 fixture failure across four files. Helpers, existing Claude driver and catalog passed. New driver-account fixture incorrectly inspected snapshot.models; actual contract stores models on ProviderInstance. Types identified the same two references, no other diagnostics. Corrected those two fixture references only (correction 1/2); auth first-account assertion passed before the fixture stopped. Concurrent process/catalog/review assertions remain unproven until R2. No production defect demonstrated. Preserve the 91 passes; R2 must run corrected driver-account fixture + types and root-integrated owner CRUD/UI checks. No rerun started yet.
