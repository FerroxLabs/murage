# Scoped agents MCP lifecycle fixture

Base 34b90c58. Outcome: distinguish a live proxy with revoked per-turn authority from HTTP transport loss, and prove fresh-turn metadata recovery using the existing fake-Claude held-turn fixture and actual harness/proxy. No production changes, image generation, provider keys, live profiles or engine-retention claims. Owns one index.test.ts case and test-only persistent stdio helper.

Acceptance/check set frozen: Node 24 targeted Vitest case; git diff --check. Assert metadata success before normal completion, actual HTTP 401 and MCP unauthorized afterward with original proxy still alive, new capability/fresh proxy recovery, old proxy remaining denied, and an owned connection-drop endpoint yielding MURAGE_AGENTS_UNAVAILABLE while local ping/tools/list still work. Cleanup all fixture-owned proxies/endpoint/bot. No broader suite requested. Two verification rounds maximum. Stop on accepted evidence or classify a failed criterion. No customer root-cause claim.

Round 1 ACCEPTED (2026-09-10): Node v24.20.0, Vitest 4.1.10, targeted `server/index.test.ts -t 'distinguishes persistent agents capability revocation'`: 1 passed, 229 skipped, duration 7.16 seconds; terminal process exit 0. `git diff --check` passed. 1 round / 0 corrections used; round 2 not needed. No production defect reproduced: the intentionally revoked live proxy returned unauthorized, a fresh scoped proxy recovered, and induced connection loss returned its distinct transport marker. This is synthetic lifecycle/HTTP evidence on macOS, not Sable's incident or real-engine resumed-session proof. All owned clients, HTTP negative-control endpoint and bot are closed/deleted in finally; existing suite teardown owns isolated harness/home. Dependency-only node_modules symlink points to the integration checkout and is not part of the candidate.
