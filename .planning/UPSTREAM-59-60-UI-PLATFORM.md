# Upstream 0.1.59–0.1.60 UI/platform reconciliation

Read-only source reconciliation, 2026-09-07. Range: `upstream/releases/v0.1.58..upstream/releases/v0.1.60`. Inspected current working tree at baseline `b6bcb808d3c811be257f1f0c925d0af242e763c8`, including existing fork changes. No production edits, tests, deployments, permission changes, or commits performed for this task. This file is the only new reconciliation artifact.

Scope excludes root's highlighted-code CSS review and the other agent's Codex/Composio/VM review. Browser engine changes are assessed only for architectural compatibility. Source applicability is not runtime verification.

| Upstream change | Current source evidence | Disposition |
| --- | --- | --- |
| `351506e0` delayed thinking-row bottom follow | `src/lib/bottom-follow.ts` contains only threshold/resume helper. ChatView imports it at 81, keeps follow state at 1233, and scrolls on parent dependencies at 1270; GroupView has analogous one-shot effect near 1046. Neither observes transcript content size. Upstream adds `useBottomFollowResize` and a transcript ref to both views. | **Narrow applicable fix.** Port observer/helper and both call sites while preserving fork keyboard-anchor, focus/windowing, and user-scroll behavior. Validate pinned late content follows, unpinned scrollback stays fixed, and observer cleanup/rebinding. |
| `d2336731` bot title pill squeezes name | Current `src/components/Sidebar.tsx:1025` has name/rename span but no upstream title pill; next sibling is timestamp. Fork has its own hidden-role indicator and later role/preview row. | **Already covered by different layout / trigger absent.** Do not add upstream `grow` or reintroduce its pill merely for patch parity. This is source compatibility evidence, not a visual claim about every sidebar width. |
| `dbfad94e` / duplicate `d6c9674e` held card names guard | Current `server/index.ts:2360` labels any permission held in `asker.autoApprove` as "This looked destructive, so auto mode stopped to ask." Current `server/auto-approve.ts:180–221` already distinguishes `sensitive-guard`, `destructive-guard`, `unattended-block`, `local-computer-block`, and `no-grant`. Upstream helper's `approvalMode`/Full/native modes do not exist in current shared approval-mode file (file absent). | **Narrow applicable adapted fix.** Derive text from current verdict/context; preserve current permission decision and give unattended context appropriate precedence. Do not transplant newer permission modes. Count duplicate commits once. |
| `9b9e99eb` held-note codes/translations + `f23f5d01` inherited-key fix | Current server/store.ts:60 and src/state/store.tsx:79 have `held?: string` only. ApprovalCard.tsx:99–101 and PendingApproval.tsx:144 render raw held text. `src/lib/i18n.ts` has no `tFromServer`. Server index also puts application errors in held, e.g. 5643. | **Narrow optional localization package with dependencies.** Add optional heldCode beside backward-compatible free text, catalog keys and fallback translation; unknown/legacy/apply-error text must survive. Use `Object.hasOwn` from the outset if adopting lookup. `f23f5d01` alone has no current target. Follow current approvals, not upstream mode promises. |
| `ff99faa8`, `f5ef5a19` / duplicate Portuguese variants | Current ApprovalCard/PendingApproval still have English strings rather than the upstream approval catalog seam. Upstream first extracts approval copy (German), then translates Portuguese and held notes. | **Optional bounded UI localization package.** Do not treat locale-only commit as sufficient when extracting call sites is prerequisite. Preserve user text and spoken-template semantics. |
| `93fd6abc` dictated segment preservation | Current tracked top-level tree has no `ios/` or `android/`; this commit touches exclusively Swift/Kotlin native recognizers, core helpers, and tests. | **Defer absent platform work.** Do not introduce native projects or claim web companion has the same defect from this patch. |
| `20226a3f`, `2e493c87`, `d1d9e166`, `d0751ae0` iOS language seam/catalog/call sites; `339149d3` iOS/Android keyboard dismissal | Same absent native directories. Locale selection requires SwiftUI root/environment/catalog work; chrome-refresh intentionally recreates navigation state. Keyboard dismissal targets native gesture APIs. | **Defer absent platform work.** These are upstream native features, not ports into current React companion. |
| `480c137f` Docker Compose deployment | Current tree has no `deploy/` and no root compose.yaml. Upstream adds new deployment/Caddy/bootstrap/docs rather than fixing a current matching file. | **Defer new deployment architecture.** No deployment or public endpoint changes in this reconciliation. |
| `6aa058eb` rootless Podman deployment | Deployment files absent. Independent runtime hunk does match: current `server/container-computer.ts:884` uses Podman `relabel=private,U=true`; upstream uses keep-id uid/gid 1000 with namespace root supervisor, removes U=true, and admits only precise keep-id namespace form. | **Defer deployment; flag narrow VM ownership fix to VM owner.** It prevents workspace ownership changes from recursive Podman chown. Requires mapping/hardening tests and native rootless Linux proof before claiming solved; don't weaken namespace acceptance broadly. |
| `3ec6dbcd` Antigravity Auto/full access | Current shared/approval-mode.ts and ApprovalModeSelector upstream seam are absent. Upstream explicitly removes Antigravity from native-required Full residual asks and labels explicit full grant "Auto" in composer. | **Defer permission-model change.** This is material auto-approval behavior, not a display-only compatibility fix. Do not import under a no-permission-weakening task. |
| `b3c2f74b` one browser engine everywhere | Upstream deletes native Electron browser surface, host, proxy, capability wiring and associated tests (9564 lines deleted), changes tool vocabulary, moves sessions/deletion to agent-browser engine, and replaces live panel with install/status UI. Current fork actively retains native surface/control and verified native fixtures. | **Defer major architecture.** It removes live browser/takeover until upstream step 3, so is incompatible with treating current native browser preservation as a narrow update. It is not an early-empty readiness fix for the retained surface. |
| `e5a3bde4`, `3e0e0e94` browser upgrade/install recovery | Depend on the replaced agent-browser panel/engine install endpoint. They restore failed-erasure retry, install reachability before ready, reset request latch, publish install progress, and preserve Chrome setup errors/retry. | **Defer with engine migration.** Valuable acceptance requirements for a future engine migration, but not direct patches to retained Electron BrowserPanel. Preserve cleanup truthfulness as a requirement; do not pull the migration merely to gain these fixes. |

## Suggested root selection

Select delayed-thinking bottom follow and adapted held-note wording as the immediate narrow candidates. Sidebar title-pill patch is unnecessary in this layout. Treat approval localization as an explicitly bounded presentation package if desired, since it spans persisted cards, server note keys and catalogs. Send Podman keep-id hunk to the VM owner for assessment alongside their existing scope. Defer absent native projects, deployment additions, Antigravity permission semantics, and native-browser replacement.

## Evidence commands

Used `rtk proxy git log --oneline upstream/releases/v0.1.58..upstream/releases/v0.1.60`, focused `git show --stat`/`git show` for commits named above, `git ls-tree --name-only HEAD`, and focused `rg`/`sed` on the current files cited. No full audit or verification run; root chooses ports and freezes acceptance before implementation.
