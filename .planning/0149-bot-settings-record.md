# Sectioned bot settings

Contract: native modal around existing bot controls, desktop left navigation and narrow full-height section picker. Sections: Overview, Identity & instructions, Skills, Memory, Routines, Access, Model, Permissions, Voice & alerts, History, Usage. Search finds sections by control keywords. Keep section content mounted to preserve local edits; existing save/authority/API behavior stays intact. Close/Escape restores opener focus and guards unsaved local drafts. Imported source role/team is visible as intent, compared with active role; never grants a role. Existing MemoryLauncher remains the managed-memory door. Existing tokens only; no new UI library or new em-dash copy.

Ownership: SettingsPanel.tsx, new BotSettingsDialog/section/draft helpers and focused tests. Root owns App replacement seam and merging N7 model default/task-scope edits; other worktrees untouched. Requested narrow dirty-report hooks for existing Voice/Access controls, pending root disposition; no permission changes. No live profiles, new grants, API replacement, credential import or publication.

Freeze checks before edits: focused section-search/role-intent tests; pnpm typecheck; isolated real controls browser fixture at desktop/narrow/light/dark covering section/search/keyboard/focus, local draft retention and close guard, unchanged privilege state. Round1 standalone dialog fixture; Round2 reserved for root App/N7 integration. Existing failed old fixtures are not restarted. Maximum2 rounds, counters survive handoff. Stop accepted/blocked with exact evidence, not extra audit. Base0da7dde9; rounds0/2, corrections0. Next: preserve controls while grouping presentation and implementing wrapper.

Root granted narrow dirty-state reporting in VoiceSettings.tsx and BotAccessSettings.tsx only. Hook passes boolean dirty/saving flags, never draft text or secrets; original save and permission behavior unchanged. BotSkills has no editable draft, so it is untouched. Working-folder/notebook drafts also report flags; all sections remain mounted; Calendar/Library/Usage navigation uses the same pending-save/unsaved-close guard. Imported role uses existing provenance compared with actual runtime flags. Legacy SettingsPanel default presentation remains supported until root replaces App's callsite with keyed BotSettingsDialog.

Exact Round1 commands: `pnpm exec vitest run src/components/bot-settings-sections.test.ts`; `pnpm typecheck`; `pnpm exec playwright test --config src/e2e/bot-settings.config.ts`; git diff --check. Browser fixture uses actual StoreProvider/DesktopCapabilitiesProvider, real controls and isolated verification server; seeded bot has inactive imported leader intent and all grants off. Four skin/width cases verify search, narrow picker, working-folder/notebook draft retention, declined Escape discard, a real notebook save with delayed response to prove close blocking while saving, focus restoration and unchanged role/app/Auto authority. App callsite/N7 model-scope integration remains reserved Round2. No native user profile touched.

Round1 live handles for continuation: focused66550, typecheck88687, Playwright99521. Inspect these before any restart. Worktree `/Volumes/Mando/WaylandBots/murage-astra/.planning/swarm-0149-bot-settings`, branch work/0149-bot-settings, base0da7dde9 with current uncommitted implementation/fixtures. Fixture owns its verification server, Vite and temp data; shared node_modules link is task-owned and retained until checks complete. Current round1/2 in progress, no correction yet. Root shared App/N7 seam still pending.

Round1 completed:2 focused tests passed(12.22s), typecheck passed,4 real-controls browser cases passed(1.2m), diffcheck passed. All handles66550/88687/99521 exited0. Eight overview/identity screenshots inspected in `.planning/0149-bot-settings-browser-results/`: desktop navigation and narrow full-height picker, labels/inputs/footers readable in actual light/dark with no horizontal overflow. Source role stayed inactive, grants/Auto unchanged, local drafts survived section changes/declined Escape, pending save blocked close, actual notebook save completed and opener focus restored. Fixture server/Vite/temp data cleaned by awaited teardown; no live profile touched.

Visual acceptance correction1 before reservedRound2: expanded avatar editor filled the Overview viewport, leaving imported-role intent below the fold. Appearance is now collapsible by default only in the embedded modal; underlying controls remain mounted and legacy standalone SettingsPanel retains expanded appearance. This change awaits R2 visual confirmation, no extra run. Counter1/2 consumed.

Pending integration seams reported to root: App uses keyed BotSettingsDialog; merge N7 SettingsPanel/ModelPicker default-model scope without rewriting policy. ModelPicker manage-models navigation and embedded BotIntakeCard browse-library navigation must enter useBotSettingsNavigation to avoid bypassing hidden-draft close guard. Avatar control also owns imageKey/direction draft and pending upload/save/generate flags; narrow dirty hook requested, not applied without ownership. No broader control audit requested. Candidate remains pending complete integration confirmation rather than release-ready.

Root subsequently granted exact avatar dirty/busy hook and ModelPicker.manage/BotIntakeCard.browseLibrary navigation wrappers. Implemented without changing save, generation, grant or model policy. Pending upload/save/generation blocks close/navigation with "Wait for the current operation to finish before closing." It never offers Discard as cancellation of a paid/in-flight request; Discard applies only to local unsaved text. This integration addendum awaits the already reservedRound2; original rounds1/2 unchanged. N7 model-scope code still must be merged by root before confirmation.

Root authorized reservedRound2 on7a31c477; merged cleanly as28593702 retaining Files/Avatar/Accounts/Memory hooks and actual App BotSettingsDialog callsite. N7 UI remains blocked/unmerged by explicit root disposition; do not claim N7 default-model scope proof from this run. R2 browser fixture now opens the actual App profile button, using the production native dialog/overflow containers. Same four skin/width cases also confirm guarded Manage models navigation with a hidden unsaved folder draft. One desktop case delays an actual tiny fixture-avatar upload response and verifies close remains blocked until completion; no paid generation or credential is used. Prior notebook pending-save, local draft/focus and authority assertions remain.

R2 named checks: pnpm typecheck; `pnpm exec playwright test --config src/e2e/bot-settings.config.ts --output=.planning/0149-bot-settings-browser-results-round2`; git diff --check. Unchanged two section-search/role helper passes fromR1 retained. This is the final planned round, not a reset or broad audit. Original R1 screenshots remain untouched. Awaited fixture teardown owns only its verification server, Vite and temporary profile.

R2 active handles: typecheck68212, Playwright40635 (functions write_stdin session IDs); inspect them before restarting anything. Source merge28593702(root7a31c477), candidate commits834f8a51/9ecbe86b, branchwork/0149-bot-settings. Current uncommitted R2 actual-App fixture/record must be preserved across account/capacity switches. No third round authorized; no N7 acceptance claimed.

R2 result: BLOCKED on exact opener focus restoration. Typecheck68212 exited0. Browser40635 exited1: all4cases reached final Escape/close, then `expect(opener).toBeFocused()` failed at spec line92 with opener inactive. Earlier checks in every case passed: actual App native layout, search/section navigation, hidden draft preservation, declined discard, Manage models route guard, pending notebook save, close/focus containment. Desktop-dark also passed pending actual avatar upload close-blocking. Final API authority/viewport assertions after focus failure were not reached and are not claimed as R2 passes; R1 authority proof remains retained.

Diagnosis: actual src/main.tsx mounts StrictMode, unlike R1's standalone host. BotSettingsDialog's effect captures document.activeElement in each setup; StrictMode's simulated cleanup/setup repeats after modal autofocus, so the second closure remembers an element inside the dialog rather than the opener. On final unmount that element is disconnected. Concrete bounded remedy proposed to root: stable opener ref captured only outside the dialog, plus deferred cleanup that restores it only after the actual dialog node is disconnected (ignore StrictMode simulated cleanup). No source correction or third run applied; root must choose an explicit bounded continuation disposition. This is a change-induced App focus acceptance failure, not a claimed permissions or save failure.

All eight R2 overview/identity screenshots inspected and retained under `.planning/0149-bot-settings-browser-results-round2/` with four viewport/skin directories matching R1. Appearance correction is visually confirmed: imported role/team and inactive-intent explanation now visible in initial Overview; narrow full-height picker and desktop nav are legible. Each directory also retains failure screenshot/error-context.md/trace.zip. Awaited teardown completed without a hook failure and cleaned each fixture server/Vite/temp profile; no live tool job remains. Original R1 outputs preserved. Counters2/2 consumed, one completed appearance correction; focus remedy is unexecuted. Source commits remain834f8a51/9ecbe86b integrated by root; actual-App fixture is ready to commit for continuation. N7 remains separate blocked/unmerged work, not silently accepted.

2026-09-09 approved focus continuation: root STATE records the user's explicit approval for one targeted correction and confirmation, preserving original2/2. Worker recovery_ui owns only BotSettingsDialog focus code and the existing exact fixture/evidence. Apply retained proposal: capture opener once outside the dialog; guard showModal; defer cleanup restoration until dialog disconnection, ignoring StrictMode simulated cleanup. Named checks frozen: unchanged four previously failed actual-App opener-focus cases with every existing draft/pending-save/avatar/authority/viewport assertion intact, plus pnpm typecheck and diff check. Command uses output `.planning/0149-bot-settings-focus-confirm`. No full suite, new audits, N7 changes, profile mutations, or further correction/run on failure. On pass commit only this fix/record; root serial integrates. Prior evidence9458fd76 retained. Confirmation not yet run.

Approved focus confirmation ACCEPTED: unchanged browser fixture4/4passed(37.6s), process15675 exited0; pnpm typecheck52135 exited0; git diff --check passed. Actual App/StrictMode opener restoration now passes in390/1440px light/dark. All original guards remain intact and passed: retained folder/notebook drafts, declined Escape/Manage models discard, pending real notebook save, desktop-dark pending fixture-avatar upload, focus containment, final unchanged chiefOfStaff/autoApprove/composio state and no horizontal overflow. All eight Overview/Identity screenshots under `.planning/0149-bot-settings-focus-confirm/` were opened and inspected: labels, section navigation/picker, content and footer remain legible. Browser fixture awaited teardown completed; no live owner profile/credentials or paid operation. Only BotSettingsDialog focus handling changed; fixture unchanged. Counters: original2/2 plus one approved correction/confirmation consumed, no additional cycle. Root serial integration pending; no N7, native or release claim. Commit uses conventional format; automatic background critique omitted because explicit scope prohibits new audits.
