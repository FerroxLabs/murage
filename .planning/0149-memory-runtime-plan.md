# Outstanding memory runtime gate — frozen proposal

Root STATE.md owns acceptance/attempt authority. This is the outstanding runtime acceptance from the 0.1.49 amendment, not a new importer/UI round. Prior importer/UI two rounds and narrow UI confirmation, plus P07 rounds, remain consumed and reusable. No runtime command has been executed under this proposal. Root must record the runtime invocation/correction budget before execution; no failed Fuigo fixture or capped UI cycle is reopened here.

Root authorized the minimal scripts/control-murage.ts restart seam and one remaining runtime verification invocation: one round, stop/report on failure, no silent reset. Implementation prepared before first run. Original consumed rounds unchanged. Frozen positive recall uses unpinned imported records and actual lexical projection; no owner-pin substitution. Two actual transport implementations are the existing ClaudeDriver (`claudeAgent`) and CodexDriver (`codex`), with fake CLI protocol endpoints configured inside the internally owned profile. Existing launcher callers retain launch/close behavior; restart does not accept a profile path.

Outcome: imported bot knowledge reaches two actual registered driver transports through server HTTP dispatch across a complete stop/restart of the same isolated profile. Scope is local fake transport endpoints, real Murage server/worker/database/drivers. No owner data, ClaudeMem, real credentials, model calls, semantic downloads, UI rechecks or platform/native model claims. All writes confined to new fixture/evidence plus an explicitly assigned test-launcher seam.

Frozen command after seam agreement: `rtk proxy node --experimental-strip-types .planning/0149-memory-runtime-proof.ts` from integration on Node24. One sequential scenario; readiness waits are bounded, no automatic scenario retry. Existing launcher startup20s, each turn30s, worker indexing30s and exact process shutdown bounds retained. Checks:

1. Use launchVerificationServer and its allowlisted child environment/private mkdtemp profile. Actual ClaudeDriver (`claudeAgent`) uses existing fake-claude-cli.ts; actual CodexDriver (`codex`) uses existing fake-codex-app-server.ts. No synthetic registry kinds or mocked provider registry. Configure fake Codex via private instance config; observe actual stream-json and turn/start transport requests.
2. Create bots A/B and one room through desktop-authorized HTTP. Write a short unique private notebook for each bot via existing notebook API. Import selected bot notebooks via actual owner import-preview/import-commit actions, allowing harmless already-imported outcome if the real idle migration won the race. Record original file hashes and authoritative source/record/version/scope lineage. Keep imported assertions unverified-import and source-backed. Prefer unpinned lexical recall after real projection; do not substitute fake search hits. Owner pin is only an additional continuity assertion if root approves; not a replacement for indexed recall.
3. After real index receipt, send lexical queries through HTTP to A and B on Claude and inspect the actual provider input. Require own exact notebook text plus source-backed memory framing; reject other-bot canary. Send room query naming the same private facts; require both canaries absent. Query text itself must not include the full canary/answer, so absence/presence cannot be satisfied by echoing the request. Verify delivered disclosure lineage (not just an assistant canned answer).
4. Close all DB observation handles. Stop the whole server and its real memory worker/fake CLI children, confirm exact PIDs closed, retain the same private profile. Select Codex through ordinary API before stop so persisted selection itself is checked at next startup. Restart server using the same profile with no config reseed. Require a new server PID, same profile/bot IDs and unchanged source/notebook identities.
5. Repeat A/B/room recall/exclusion assertions through Codex's actual thread/start + turn/start transport; require fresh transport process/session and delivered current source lineage after restart. No cross-provider native session reuse. Require source import identities/version counts unchanged (new ordinary conversation capture records are legitimate and excluded from import-duplicate counts). Original notebook bytes/hash unchanged. Record keyword-only/model-missing status truthfully.
6. Close entire owned runtime, verify observed PIDs and listeners gone, preserve sanitized transport extracts/SQL lineage and logs before task-owned cleanup. Do not dump whole fake Codex environment or capability tokens into durable review evidence.

Launcher gap: current launchVerificationServer always creates dataDir and seeds config; close() deletes it. Do not pass arbitrary user paths into a retention option. Smallest safe seam proposed to root: fixture-owned stop()/restart() (or equivalent) closes exact child, preserves its internally allocated profile, respawns without reseeding, and keeps close() as full owned cleanup. Expose no external dataDir selection. Existing callers unchanged. Existing instrumentationSource can configure fake-only per-process observation variables; private config can add the second instance, avoiding a general environment/config hook.

Potential precondition: server shutdown must actually close memory worker and fake CLI descendants before restart. An exit of the parent alone is insufficient. If existing shutdown does not meet that, stop with exact evidence and let root decide smallest test lifecycle seam; do not terminate processes by name or treat surviving descendants as a valid restart.

Success means this scoped actual-driver/fake-transport runtime gate passes. It does not prove actual vendor binaries/model quality, every engine, UI/native platforms, final package readiness, or capacity. Those boundaries remain from canonical plan and prior evidence.

## Single invocation result

PASS,2026-09-09T10:48:18.194Z–10:48:29.600Z. Command exactly as frozen above. Receipt `.planning/0149-memory-runtime-1788950898194/receipt.json`; six sanitized actual prompt/lineage files and two server logs retained in that directory. Six HTTP/actual-driver transport assertions passed: Claude A/B/room, then Codex A/B/room. Both unpinned imported records remained `unverified-import`, revision1, source-backed, and lexically indexed; own record present in actual bounded reference payload, other bot canary absent, both private canaries absent in room. No pin substitution or synthetic search bridge.

Integration base identity at handoff: ae5d2fe1986353cf695b5082ce61c6eb221e78c6, plus the scoped launcher/fixture changes and preserved unrelated concurrent work. Root accepted this runtime gate and authorized committing only the launcher plus actual runtime fixture/plan/receipt and supporting transport evidence. No additional verification run or automatic background audit is included in that acceptance.

Server27760 and observed worker27857/fake Claude28313/28314/28315 closed. Server28317 restarted on the SAME internally allocated profile and port21413, with identical persisted config hash; model selections had been changed through the existing HTTP API. Codex fake transport PIDs28340/28345/28349 each used actual thread/start+turn/start via CodexDriver, with delivered source lineage. Original source IDs/revisions/import counts and notebook SHA256 remained identical. Imported notebook hashes:3673873f4bdbeee9c155f25c7648f8063df255ea7f52ea4db33450d014a8135d and d17f41443fbb9b116bdb1be8d2de8eabccf05db70128491241bd35e38cf72e53. Final server28317/worker28326/all captured transports closed; temporary profile removed only after retained evidence. Follow-up exact listener cleanup check for21413/21414 found no listeners.

Status reported model missing/bytesDownloaded0, extractorInstanceId null, memory worker ready/no error, zero extractor reserved input/output and calls. This proves local keyword continuity through actual Murage drivers and fake protocol endpoints; it does not claim real vendor inference, every engine/platform or semantic quality. Runtime round1/1 consumed successfully, no corrections/retries. Prior importer/UI/P07 counters unchanged. Small launcher restart seam ready for root integration; no production memory implementation changes made and no commit performed by worker.
