# 0.1.50 Flux native IPC proof contract

Status: ACCEPTED for the bounded macOS arm64 native IPC proof below. Product source unchanged. Root authorized fixture implementation/run after reviewing this contract. Root's integration is `3194784a`; this isolated fixture tested Flux candidate `6407ee4862df1db232c1ca723b0b8fa81718b649`, whose accepted changes root integrated. Receipt source hashes identify the actual tested preload/production handler functions. This package does not reopen the accepted Models browser workflow or prior 71/18/51/type-check passes.

## Outcome and exact proof boundary

Run a real Electron BrowserWindow with the unchanged `electron/preload.cjs`, then invoke `window.muragebox.mutateFluxConnection(...)` from its isolated renderer. Register the exact current Flux IPC handler from `electron/main.mjs`, using the existing source-extraction pattern in `electron/credential-authority.node-test.mjs`; fail if its boundaries do not match exactly. Use the production `mutateFluxCredentials`, `createSecureCredentialState`, `updateSecureCredentialDocument`, and `saveSecureCredentials` functions. The last two can use the same exact-source extraction, with fixture-owned lifecycle/path bindings, without importing normal main startup. Record source identity in the receipt.

The server is the actual isolated harness from `launchVerificationServer` in `scripts/control-murage.ts`, reached over its allocated loopback port and real owner proof/private commit token. Do not replace the server transaction, copy its behavior into the fixture, or synthesize a preload bridge. Select the packaged controller branch through the extracted handler's `app.isPackaged` binding; explicitly record that this is not a shipped packaged-app startup proof.

Safe-storage boundary is intentionally injected: use a temporary in-memory random AES-GCM key for the fixture's `safeStorage.encryptStringAsync`/decrypt adapter and availability switch. Actual production atomic file writes and document serialization run against only the owned temporary credentials file. Never call Electron safeStorage or a system keychain. This proves native IPC, nonplaintext fixture persistence, atomic replacement and rollback; it does **not** prove OS-backed encryption, real keychain access, packaged migration/startup, or another platform. An unavailable injected store must refuse without replacing the file.

## Existing mechanisms to reuse

- `scripts/test-desktop-recovery.mjs`: exact Electron executable child, owned temporary directory, timeout, terminal exit and cleanup. Use its launch/cleanup pattern, not a new process-manager abstraction.
- `scripts/smoke-desktop-recovery.mjs`: set userData before readiness, sandboxed context-isolated native window, network denial, explicit window teardown and evidence receipt. Canonicalize owned temporary-path comparisons to avoid `/var` versus `/private/var` fixture mistakes.
- `electron/credential-authority.node-test.mjs`: exact handler extraction with dependency bindings. Real `ipcMain` must register the extracted Flux handler here; no mocked `invoke`.
- `electron/server-child-lifecycle.mjs` or the verification launcher's existing `close()`: observe and stop only the exact owned process.
- `server/testing/fake-claude-cli.ts`: `__fixture_hold_authority__` prompt produces held synthetic work; use the existing control commands and explicit task targeting to establish and stop a genuine busy task. Do not assume onboarding appears: the verification server already seeds Ember.
- Accepted Flux HTTP instrumentation: fake credentials and private commit token, all external transport denied. This native proof does not invoke catalog Test, so expected provider requests are zero.

Implementation, if authorized, is limited to two fixture files under `.planning/` plus this record and evidence. No product refactor or production test seam is required. The outer Node 24 runner launches the existing isolated server and Electron child, passes only fixture connection metadata, and waits for both exact exit handles. The Electron fixture loads the unchanged preload and extracted production functions. All credential comparisons happen in the privileged fixture and emit booleans/counts only.

## Frozen acceptance/check set

1. **Actual native bridge:** real Electron versions/platform recorded; `window.muragebox.mutateFluxConnection` exists; renderer cannot access `require`, Node, the credential document or any saved key. Missing owner proof refuses before persistence. No ordinary Murage entry/startup or owner data is opened.
2. **Atomic successful mutation:** seed a synthetic canonical key, secretless alias, and unrelated provider credential in the fixture encrypted document and matching server environment. Invoke Replace through the actual preload. Read/decrypt the owned file in main and query actual server status. Both slots/aliases represent the same committed revision; unrelated values and historical alias IDs/enabled flags survive; public IPC output has no saved key. Persisted config has no Flux or bank plaintext credential after packaged commit.
3. **Busy prewrite fence:** hold a synthetic task using the established fake engine. Attempt native Replace and verify rejection, unchanged credentials bytes/revision, and zero persistence calls for that attempt. Stop only that owned task through the existing task-targeted control path; require its terminal receipt before continuing. During a reserved write, a concurrent credential mutation must also refuse before its write.
4. **Lost-ack compensation:** inject exactly one transport failure in main after the real `/api/flux-connection/replace` commit response arrives. The real controller must restore the original encrypted document and actual runtime snapshot under the lease; aliases/unrelated provider unchanged. Subsequent native mutation succeeds, proving the known rollback released the fence. Do not replace the transaction with a mock response.
5. **Unavailable custody and disconnect:** injected unavailable safeStorage refuses without a file/server change. Restore availability, invoke Disconnect through preload, and verify one blank managed canonical slot, no Flux key remaining in the bank, historical aliases unavailable, unrelated provider retained, and managed environment projection remains blank. This is source/function restart projection proof, not a full app relaunch claim.
6. **Isolation and terminal evidence:** no provider/network request, no saved key in IPC results/receipt/logs, no owner profile/keychain access, clean native renderer with context isolation and sandbox enabled. Exact Electron/server children exit and only validated fixture-owned temporary data is removed after evidence is saved.

## Execution budget and stopping

This is a newly named outstanding native-platform package, not a reset of the previous Flux package. Maximum two rounds and two unsuccessful corrections to the same issue. Round 1 is the above complete native check set with classification. Correct only fixture unsuitability, change-induced acceptance failure, or an evidenced safe-integration blocker within this contract. Round 2 reruns only invalidated checks plus the required single joined native candidate confirmation. No old UI, full test suite, Lighthouse, provider calls, publication or release checks. A product defect outside the fixture boundary returns to root for a scope decision.

Proposed one-invocation command after fixture creation: `/Users/seandonahoe/.nvm/versions/node/v24.20.0/bin/node .planning/flux-native-runner.mjs` (through `rtk proxy`). The runner uses the repository-installed Electron executable; it must not rely on an ambient Node or load the normal app. Bound each child to 60 seconds; allow at most 120 seconds for the joined fixture. On timeout, inspect and stop the exact handles; never start a duplicate while exit is unknown.

Receipt destination: `.planning/flux-native-evidence/result.json` plus bounded redacted logs. Include candidate SHA, Node/Electron/platform, extracted-source hashes, checks/status, mocked safeStorage boundary, providerCalls=0, owned process exits, and limitations. Never persist synthetic key literals in reports or dump the encrypted document contents. At success record native IPC/transaction ACCEPTED on the tested platform only. At limit record the failed criterion, evidence, and one exact bounded remedy; do not relabel a fixture failure or mock encryption as a native keychain pass.

## Terminal result

Round 1 passed with no corrections. Node 24 parser checks passed before invocation. `/Users/seandonahoe/.nvm/versions/node/v24.20.0/bin/node .planning/flux-native-runner.mjs` exited 0. Native runtime: Electron 43.4.0, embedded Node 24.18.1, darwin arm64; outer runner Node 24.20.0.

Actual sandboxed preload/IPC, missing-proof refusal, production atomic two-slot/alias persistence, reserved-write contention, actual held fake-task busy refusal before encryption, task-targeted interruption, lost real commit acknowledgement with document/runtime rollback, successful post-rollback mutation, unavailable injected custody, and native Disconnect passed. Unrelated provider/alias flags retained. Fixture credentials were ciphertext on disk, with no key reflected in the returned status or saved report. No provider request was issued. Native error log contains the four intentionally injected refusal/failure messages; these are expected test branches, not unclassified failures.

`flux-native-evidence/result.json` records source hashes and scope; `lifecycle.json` records exact Electron exit and server closure. Owned temporary storage was removed after both children closed. SafeStorage was an injected AES-GCM fixture adapter; no real safeStorage/keychain API was called. This closes native dispatch/persistence proof only, not full packaged startup, actual OS keychain encryption, or Windows/Linux qualification.

Rounds used: 1/2. Corrections used: 0/2. No round 2 is needed. Next authorized action: commit fixture/contract/evidence only and hand the accepted proof to root for integration.
