# Sender avatar correction

Explicit independent user package, baseline b7e65937, work/0149-sender-avatar. Frozen scope: communication activity chip resolves comm.withBotId only against current authorized state.bots, renders existing BotAvatar custom image/crop with static inline motion and existing image sanitization/failure fallback. Missing/deleted sender uses historical comm name/color mascot, never recipient or name-based identity. Existing channel navigation, permission/delegation behavior unchanged. Own narrow ActivityChip span, new CommAvatar and focused tests/visual fixture; root coordinates other ChatView work. No owner data/provider calls. Capped polish2/2 and Fuigo native gate unchanged.

Acceptance/checks max2rounds,0used: SSR stable-ID/custom sender vs recipient/duplicate name, missing/deleted and rejected external URL; isolated browser actual component shows custom image and image-failure fallback with reduced motion, light/dark visual snapshot; typecheck. R2 only eligible correction/confirmation. Commit accepted scoped fix, no push. Source cause: server/comms-visibility.ts received note sets withBotId=from.id; ChatView ActivityChip ignores it and renders EmberAvatar withColor directly.

R1: SSR3PASS and typecheckPASS. Initial commands before dependency-link admission did not discover tests (vitest/playwright missing); shared node_modules link restored, no install. Browser2FAIL before component load: retained trace explicitly reports module __avatar.js served text/html because fixture middleware matched its prefix. Correct fixture document route to exact path/query, preserving module handling. R2 only those2 browser cases; unchanged source/SSR/types passes retained. No acceptance weakening.

R2:2/2 browserPASS in10.5s, session72441 terminal exit0; screenshots inspected for light/dark custom16px image and mascot fallback, no alignment/label issue. Fixture uses synthetic white PNG from local attachment URL, verifies successful decode and exact sender URL, then dispatches image error to exercise existing fallback under reduced motion. This is component visual/runtime evidence, not an owner-profile or delegation E2E claim. Source integration is the three-line ActivityChip change, channel dispatch unchanged. SSR3PASS session44855 and typecheckPASS session83087 retained. All check handles terminal, fixture server/cache closed by afterAll. ACCEPTED scoped sender-avatar correction;2rounds used. Root authorized exact ChatView span, ready for integration with N7 preserving CommAvatar import/use. Polish2/2 blocked and Fuigo approval pending remain unchanged. No push/publication.
