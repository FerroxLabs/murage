# Windows 0.1.50 private delivery preparation

Baseline f320e846fe4ad49a277f7b8a14c361cce8d51e32; final source awaits the active Windows updater D1 outcome and root's source decision. Preparation only: no build, native invocation, VM access, signing call, install or publication occurred. Other worker windows_launch_followup owns the sole Windows guest.

Outcome: produce an exact-source private Windows x64 NSIS installer and portable ZIP suitable for later install/uninstall/bundled-engine qualification. Existing package:win runs package:prepare, build:recovery and electron-builder --win --publish never. No product-source edits, bypasses, owner credentials/profile, new features, or broad audit.

Counter audit: parent .planning/STATE.md identifies final native Windows 0.1.50 package/delivery open; no full Windows 0.1.50 package invocation is recorded. Existing 0.1.49 publication/native-install/cleanup evidence is retained and does not qualify 0.1.50. Updater primitive R1/R2/C1/C2 and active D1/D2 are distinct existing counters owned by its worker. Historical P17 round2 capped packaged-server fresh-start smoke at old395d790b is not rerun by this preparation. Proposed package R1 remains uninvoked; at most R2 for eligible corrections/confirmation after root admission. No attempt budget is reset.

Frozen proposed build checks: windows-2022 x64, exact approved full source SHA, Node24.20.0/pnpm10.33.0, pnpm install --frozen-lockfile, existing pnpm package:win and its afterPack/recovery/Fuigo/browser/memory validators; actual installer+ZIP presence, required bundled files, updater owner/repo/publisher and valid required signatures; retain source/version/toolchain/outcomes/artifact SHA256 and build logs. Existing build-recovery-helper uses installed MSVC/SDK through vswhere, and does not execute the helper. No install/uninstall, packaged-server, provider-auth or UI qualification runs in the build job; those are later guest-owner checks against the retained exact artifact. No public update feed, signing-service settings, credential extraction/new credentials, id-token permission or publication binding is added here.

Signing prerequisite found and preserved: electron-builder.yml win.azureSignOptions selects Azure Trusted Signing. scripts/after-pack.mjs196–210 requires signIf=true for Fuigo/browser, signed original browser images, and valid Ferrox browser Authenticode signatures. Existing .github/workflows/package-win.yml and release.yml supply AZURE_TENANT_ID, AZURE_CLIENT_ID and AZURE_CLIENT_SECRET. This baseline provides no unsigned private mode. Clearing Azure config, mocking signIf, disabling afterPack or relaxing browser admission would change the acceptance contract and is not authorized.

Coordinator explicitly clarified the earlier no-secrets constraint: signing this private installer through the existing CI identity is authorized normal build work; the prohibition covers credential extraction/new handling, not established CI references. Prepared .github/workflows/windows-private-candidate.yml has contents:read and exactly the three existing Azure secret references confined to the package step. No values are read, copied or printed by the agent, and no RELEASES_PAT/releaseworkflow/publication path exists. Package preflight checks presence only and fails closed if CI bindings are unavailable. All existing afterPack/signature validators remain. Root must resolve final source after updater D1 before invocation; no attempt has been consumed.

Launch mechanics: only a push of isolated work/0150-windows-private-prep starts the first run; the branch is still local/unpushed. Root must retarget both source_sha default and PRIVATE_SOURCE_SHA fallback to the approved final SHA before that deferred push if it differs from f320e846. The manual trigger is also present for an explicitly authorized later run, but a new manual-only workflow initially requires default-branch/first-run registration, so that is not assumed. No main-branch workflow registration or extra run is needed. Reference: https://docs.github.com/en/actions/reference/workflows-and-actions/events-that-trigger-workflows#workflow_dispatch.

Later native delivery prerequisites: guest owner has exclusive lease; clean disposable user/install location; exact downloaded installer/ZIP hashes match receipt; install/uninstall and required bundled Fuigo/browser/recovery behavior each get explicit scope and remaining counters before invocation. Preserve prior 0.1.49 passes and state separately. SmartScreen/trust, installed lifecycle and full native bundled runtime are not proven by compile/inventory checks.

Current status: workflow/record prepared, not dispatched. Application source unchanged. Root receives branch/commit for review; no push or native work until updater D1/source decision. Established CI signing references are authorized as recorded above.

Source decision resolved after genuine-Limited Windows D2 and Linux full-manager proofs: root integrated unified updater as9eeb5e032b99edf0c93c0f651d0ddf387fae66b8. Both default and fallback now pin this exact private Windows qualification baseline. Later unrelated source changes do not silently relabel this artifact. Final package validation now requires fuigo-probe/launcher.exe and manifest.json, validates the helper's actual final Ferrox signature, re-runs existing validateFuigoProbeResources against assembled win-unpacked resources, and retains signed helper SHA/manifest and signature receipts. This closes the planned compile/resource/Authenticode stage only if it actually passes; install/uninstall/bundled runtime remain later native delivery gates. Root pushes prepared workflow; no run was started by the preparing worker and package R1 remains unconsumed here.
