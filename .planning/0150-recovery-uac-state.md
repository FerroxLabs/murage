# Isolated Windows UAC qualification

Outcome: qualify production recovery helper's real interactive same-user UAC cancellation and approval in the retained Windows11 test guest. Native reference f7ad8968d50dfdb82c9a825cd9b0a8fb360a1005; working base9a40c0d3 capture/broker/build/adapter diff against reference is empty. Build-only branch artifact contains unsigned production helper, exact SHA256 and actual adapter/lease modules. No fixture-mode helper, release, signing claim or customer profile.

Root approved exclusive retained host/guest resources and actual isolated UAC test. Host hetzner-dsm; base /var/tmp/murage-win11-guest-20260909-vj96yyyk. New uniquely named recovery-uac overlay only, original disk readonly backing, copied UEFI+TPM,4vCPU/8GiB, absolute2h deadline and exactPID/starttime watchdog. Admission requires no existing VM/TPM, KVM,32GiB hostfree disk and16GiB MemAvailable. Read-only current checks: no matching globalQEMU/TPM processes; KVM/tools present;100GiB diskfree/108GiB availableRAM; base0700, guestcredential0600; original qcow2 dirty=false/corrupt=false. No lease started before exact artifact is ready.

Acceptance: limited interactive MurageTest token/session; actual visible UAC secure desktop for exact unsigned helper; cancel leaves original fixture/anchors and startup selection unchanged; approve sameSID creates real VSS copy with exact owned cleanup and preserved source; differentSID guard refuses (native policy evidence retained unless actual second test account already exists). No account creation, privilege grant, UAC-policy change, services or owner-data use. If guest UAC preconditions or Electron RUN_AS_NODE parent capability are unavailable, report exact environment blocker instead of bypassing parent guard or changing policy.

Use retained fixture Murage.exe in a separate task-owned app-code copy with process-only ELECTRON_RUN_AS_NODE to run the actual adapter from a limited interactive task. This is real production-helper UAC transport, not signed/installed0.1.50 app qualification. Existing fixture credentials stay inside host0600 file, never printed/copied to task manifests. Preserve all disks/evidence. Stop/export before deadline; graceful Windows shutdown then exact owned process cleanup if needed. Root confirmed no competing guest actions. Prior capture/transport/joined run history survives; this is the required remaining native boundary, not a reset or feature expansion.
