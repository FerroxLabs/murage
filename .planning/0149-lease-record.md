# 0.1.49 reboot-safe lease package

Contract: port boot-session/uptime stale-owner detection from Apache-2.0 OpenMausBot PR #937 (head b6a27330560cfbab76c720b785928e3cda782606), preserving Murage's strict bounded records, sibling anchors, immutable recovery election, child delegation and sealed release. Accept legacy records; same-boot live owners must exclude; proven prior-boot primary/child/reaper owners must recover despite a live reused PID; uncertain identity must never authorize eviction. Only electron/data-dir-lease.mjs and its Node tests are in scope. No live profile, reboot, migration, dependency installation, push or publication.

Checks frozen before edits: Node syntax check and complete electron/data-dir-lease.node-test.mjs on this macOS host, git diff --check, inspect all liveness callers. Existing real-process contention/parent-child barriers remain; add disposable subprocess boot/uptime probe fixtures and old/new record validation cases. Native Windows/Linux and real reboot remain platform integration limitations for root's package gate. Maximum two verification rounds and two corrections per issue; stop ACCEPTED or BLOCKED with evidence. Commit scoped passing changes for root integration.

Status: ACCEPTED for scoped macOS protocol checks. Rounds 1/2; corrections 0. Implementation includes validated optional boot/uptime fields, all five record liveness callers, metadata-bound release identity, and legacy compatibility. Unavailable uptime is null instead of upstream's zero, preventing accidental stale proof. No additional features or error-path disclosure were imported.

Evidence: 2026-09-09, Darwin arm64, Node v25.8.1, base 2a957b67. `node --check electron/data-dir-lease.mjs`, `node --check electron/data-dir-lease.node-test.mjs`, `node --test electron/data-dir-lease.node-test.mjs`, and `git diff --check` passed. Complete Node suite: 43 tests, 42 pass, 0 fail, 1 skip (native Windows namespace aliases), 9512.6ms. Tests used temporary directories and disposable Node workers only, with simulated boot/uptime failures and EPERM plus real process election/delegation barriers. All task worker processes and temporary roots cleaned by test teardown. Source inspection confirms only ownerIsAlive calls processIsAlive; primary acquisition/recheck, reaper, child guard and delegated-parent validation all use ownerIsAlive.

Limits: no actual reboot or packaged Electron/server workflow tested here; native Linux and Windows checks remain root integration work. On platforms without boot identity, uptime proves only some reboot cases; legacy live/reused PID records retain conservative exclusion. No known scoped acceptance blocker. Next authorized action: commit this package and return hash to root for integration; do not push.
