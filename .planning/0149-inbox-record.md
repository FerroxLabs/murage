# N3 persistent Inbox

Contract (base 4b7dbb2c): owner-only Inbox projects existing durable options requests, connection/credential requests, actionable provider errors and meaningful background routine/goal results. Exclude onboarding options, ambient progress, direct-chat final answers and successful background runs without reported output. Stable source identity groups repeated requests/runs. Read and snooze metadata persist separately and never resolve/approve source actions. Source deletion/updates are authoritative, access uses explicit permitted thread IDs, lists/search are SQL-paginated with a relevant message index. Exact thread/message/run links, Needs you/Results/All, search, paging, read/unread and snooze/restore controls use existing Murage tokens. No secret-card text, connector credentials or raw provider secrets in summaries.

Ownership: server/inbox.ts, shared/inbox.ts, focused tests and src/components/Inbox.tsx with standalone isolated browser fixture/config. Root owns database initialization, HTTP routing/owner proof, permitted thread construction, Sidebar and exact-task navigation, OS notification integration. No live data, independent approval ledger, broad transcript indexing, migration of originals, package changes, push or publication.

Frozen checks: focused Vitest inbox projection/HTTP adapter tests on temporary SQLite databases (dedup, restart, source update/delete, read not approval, snooze expiry, secret sanitization, authorization, exact links, bounded pagination/search and quiet-run suppression); typecheck; standalone Playwright real Inbox/real HTTP adapter/temp SQLite fixture at desktop/narrow light/dark, keyboard controls and exact callbacks; git diff --check. Browser fixture will not launch a full app or touch a live profile. Round1 scoped implementation/UI; Round2 reserved for root integration and eligible corrections. No third cycle. Rounds0/2, corrections0. Next: source-backed projection, state metadata and narrow API seam.

Exact Round1 commands: `pnpm exec vitest run server/inbox.test.ts`; `pnpm typecheck`; `pnpm exec playwright test --config src/e2e/inbox.config.ts`; `git diff --check`. All API/browser requests use a new loopback Vite fixture plus real inboxRequest and temporary SQLite file, not mocked Inbox responses. Cleanup SQL is limited to fixture thread IDs; no production database is opened. Root requested that Round2 also visually cover its integrated Sidebar Tools/Inbox entry, without adding a third round. Implementation uses actual source state, never private secret/connector/option text in summaries. HTTP owner proof itself remains root's existing gate; fixture supplies a named fake proof.

Round1 results: Vitest9/9 passed (1.21s); browser4/4 passed (21.3s). Typecheck flagged `row` possibly undefined after helper rejection; correction1 uses explicit throw to express the unchanged runtime guard. Screenshot inspection found the fixture's reload reset light skin to default dark, so light visual evidence is invalidated; correction1 for that fixture reapplies/asserts the selected skin after reload. Two distinct corrections, each first attempt. No product assertion failed. Round2 reserved for typecheck, affected focused test, corrected four-theme/width browser fixture plus root's integrated Sidebar/Inbox/API/deep-link seams. Source-only candidate is pending integration acceptance, not ready to ship alone.

Round2 candidate: root integrated f287470f merged as 6cbb0f37; fixture c1d673e9 included. One config add/add conflict resolved to run both standalone and integration specs. Named commands: focused inbox.test.ts, pnpm typecheck, `pnpm exec playwright test --config src/e2e/inbox.config.ts --output=.planning/0149-inbox-browser-results-round2`, git diff --check. Browser set includes six cases: four corrected standalone skins/widths plus real full-harness Tools/Inbox/old-task-navigation on desktop/light and narrow/dark. Original Round1 evidence remains untouched in its prior output directory. No additional exploratory review or N7 thread redesign included.

Round2 outcome: BLOCKED. Focused tests9/9 passed (1.24s). Standalone browser4/4 passed; corrected light/dark screenshots at both widths inspected successfully in `.planning/0149-inbox-browser-results-round2/`. Integrated fixture failed before server readiness (desktop test0ms; narrow did not run), so Tools/Inbox/current-task navigation is NOT verified. Cause: `InboxError` uses a TypeScript parameter property (`constructor(readonly status...)`), which Node's production `--experimental-strip-types` launcher rejects with ERR_UNSUPPORTED_TYPESCRIPT_SYNTAX. Exact full log: `/var/folders/8h/chxws5390hx17g49p75b2xn00000gn/T/murage-verification-evidence/server-1788934364242-34190.log`; trace retained in Round2 output. This is a change-induced startup/safe-integration blocker, not a fixture-only defect. Minimal remedy: ordinary readonly status field assigned in constructor.

Typecheck also failed: new integration fixture's static control-murage import pulled scripts/mcp-server.ts into app ES2022 compilation, yielding findLastIndex TS2550/implicit-any TS7006 at line657. This is fixture-induced compiler reach, separate from startup. Bounded remedy: runtime URL import of the existing launcher and a local narrow fixture-handle type, so the browser fixture does not change app compiler scope. Original Inbox row-narrowing error is absent. `git diff --check` passes.

Both rounds consumed. No third verification or further correction has been run. Root notified immediately; candidate must remain unqualified until the minimal startup correction and named integration gates receive an explicit bounded continuation disposition. Owned test processes/temp roots were closed by fixture teardown; failed-launch log and trace preserved. Task-owned dependency symlink retained for potential authorized continuation.
