# Murage 0.1.50 account-switch handoff

Updated 2026-09-10 13:07 UTC. **Not released. Goal incomplete. User is switching accounts.** This is a continuation record, not a new plan.

**Final worker checkpoint:** post-reboot verification PASSED13:03:26. App7516/server1540(parent7516), actual console1, are deliberately left running. Workload and P2 NOT STARTED. No exporter is active. Current receipts exported to `windows-native/p1-evidence/guest-export-p1-130418.json`, SHA256 `60f50f2c5f4b9ff9fbb20476e52b6780685cfeccf028fe20798337e43af7e478`. P2 admission cutoff13:03:29 has PASSED. See `.planning/windows-native-handoff.md` for exact read-only resume and safe close/workload commands. Do not start any new phase before revalidating live state and remaining deadline.

## Read first: time-sensitive live Windows guest

- Host SSH alias: `hetzner-dsm`. Live lease: `/var/tmp/murage-win11-guest-20260909-vj96yyyk/recovery-uac-xckr66a2`.
- VM PID605370, TPM605336, watchdog605984, transfer618722:35291, WinRM58151. Verify identities/start times from the lease's `launch.json` before acting; do not trust a PID alone.
- **Immutable deadline: 2026-09-10 13:48:29 UTC.** Watchdog may stop the guest while the account is switching. Do not extend/restart this lease or start a competing VM implicitly.
- Windows P1 is partly passed. Actual reboot and post-reboot app/health/lease/canary verification passed; app7516/server1540 remain running. **Read `windows-native/EXECUTION.md` and the committed `windows-native-handoff.md` snapshot.** Do not duplicate the existing app or restart request.
- User requested handoff; worker instructed to stop new phases after its current observation/export. No new workload/P2 should be assumed started. The boot identity/post-reboot app result and workload need final confirmation from receipts.
- P2 only repairs the missing upgrade-exit receipt. It required at least45minutes remaining **after P1 completion/export/old-lease close**, so its cutoff was13:03:29UTC. If resuming after that cutoff, do NOT start P2 under the old approval; obtain a precise new resource/evidence disposition.

## Files and repository locations

Mando reads currently fail with `Operation not permitted`, despite stat showing owner/mode normally. No permission changes attempted. **Always pass an accessible explicit workdir**, e.g. `/private/tmp`; inherited Mando cwd caused `uv_cwd` failures.

- Accessible continuation repository: `/private/tmp/murage-0150-build-recovery-fdatWt/repo`, branch `work/0150-continuation-record`. This branch holds handoff/docs only and has no build trigger.
- Detailed chronological execution addendum: `.planning/0150-build-recovery-addendum.md` in that repository. Canonical parent STATE remains `/Volumes/Mando/WaylandBots/murage-astra/.planning/STATE.md`, but is inaccessible and stale relative to this addendum.
- **Exact final application source:** `15c3cbd64056a7288777739f5dd244ca4365ea0b`, pushed to `FerroxLabs/murage` branch `work/0150-integration`.
- Accessible exact-source worktree with installed dependencies: `/private/tmp/murage-0150-build-recovery-fdatWt/signing-fix`, branch `work/0150-recovery-signing`.
- Native Windows execution record and tools: `/private/tmp/murage-0150-build-recovery-fdatWt/windows-native/EXECUTION.md`, `tools/`, `fallback/`, `p1-evidence/`, `p2/`.
- Mac/Linux source-cohort worktree: `/private/tmp/murage-0150-build-recovery-fdatWt/private-cohort`; record `.planning/0150-current-private-cohort.md`.
- Complete local release stage: `/private/tmp/murage-0150-release-stage-vHGTTB`; read `PREFLIGHT.md`, `manifest.json`, `SHA256SUMS-all.txt`, and `assets/`.
- Draft release copy: continuation repo `.planning/0150-release-notes-draft.md`. Proposed README patches are in the release stage, not applied publicly.
- User's active goal text: `/Users/seandonahoe/.codex/attachments/a11d4f7d-6594-415d-a761-b123b0ed6e6f/pasted-text-1.txt` (already read). Original master/deferred plans on Mando: `.planning/MURAGE-PLAN.md`, `GOAL-0150.md`, `MURAGE-0151-DEFERRED.md`.
- Temporary files can disappear on machine cleanup/reboot. This handoff is committed/pushed to the private continuation branch for recovery; artifacts also have CI run references below. Do not rebuild just because one local path is missing.

## Approved scope and authority

0.1.50 is the reliability release, retaining completed features and **including Windows independent Fuigo updates**. User explicitly reversed the proposed Windows-updater deferral.

Deferred to0.1.51: unfinished cloud onboarding/deployment, broader image-provider editing, hosted-service expansion, remaining optional upstream ports. Keep preserved source/evidence. Telegram groups/multi-user permissions, extra channels, public marketplace/sharing and broader autonomy are still outside committed scope.

User has requested release ASAP, but **failed or unverified gates were not waived**. Two specific asynchronous questions remain unanswered: (1) accept documented older -32603/1073807364 and original Mac shutdown/custody limitations once mandatory tests pass; (2) accept missing original upgrade exit-code evidence instead of a full repeat. Do not treat preselected options or automated goal continuations as answers. No tags, drafts, uploads, public README changes or publication have occurred.

Use Astra medium, maximum three independently useful workers, `rtk` prefix, explicit Node24.20.0 `/Users/seandonahoe/.nvm/versions/node/v24.20.0/bin/node`; pnpm10.33 CLI `/Users/seandonahoe/Library/pnpm/.tools/pnpm/10.33.0/node_modules/pnpm/bin/pnpm.cjs`. Preserve original worktrees, private sessions, data and credentials. Never print/copy the host's0600 `guest-credentials.json`; retained WinRM helpers load it in place. No global Windows Node/npm/TEMP/TMP/PATH/policy edits.

## Completed engineering: do not rebuild or re-audit

| Work | Verified result / limitation |
|---|---|
| Claude background-notice tool-loss | **Actual customer trace identified the cause.** Native05f1114e...previous.ndjson had submitted user07:35:42.879, then result origin `task-notification`07:35:50.841, then Bash/Read denied `Murage: the turn ended`. Driver settled the wrong result. Commit4f35f486 guards that origin; before-fix reproduction failed; 80 Claude tests passed/1 existing skip plus types. All final15c artifacts include it. Ordinary earlier multi-turn no-repro is superseded by this causal evidence; do not keep claiming no cause found. No idle authority revival or permission bypass. |
| Independent Windows updater | Real Limited user launch succeeds; synthetic restricted-token test caused earlier CreateProcessW5. Genuine Limited canaries and full default-probe EngineManager install1.0.9→update1.0.10→rollback→bundled passed; source4dc37b21 evidence. Outer PowerShell null-exit reporting limitation retained; both leaf processes exited0. |
| Linux updater | Initial lower-level pass did not prove manager reachability; found darwin-only manager guard and corrected it. Full-manager run34468176872 passed. Unified updater integrated9eeb5e03; finalsource15c includes all supported targets. Do not conflate the earlier stage-only result with the later real manager proof. |
| Mac updater | Native arm64 and Intel full-session/update/rollback/bundled proof retained. Intel run34456401066 passed. |
| Recovery | Guarded VSS/archive/separate restore/explicit review and selector integration passed. Real same-user UAC cancellation and approval/capture passed; original/anchors unchanged, snapshots released. Different-SID guard evidence retained, no second account created. Earlier unsigned-helper/copied-Electron test is distinct from final signed packaging. |
| Recovery signing | Windows package initially omitted recoveryhelper from explicitsignIf list. PE cert directory0/0 confirmed unsigned. Final15c adds regular-file check, configured signing and finalFerrox validation; 13 focused tests passed. Confirmation34471681869 passed actual signatures. |
| Cancellation/diagnostics | Requested-cancel falseunexpectedexit fixed78ca1662, 55ACP tests; bounded sanitized error diagnostics added. These do not prove closure of all customer -32603/1073807364 reports. |
| Retained product work | Onboarding, visibleTools/Inbox, Files and exact report-source/download/restart journey, scoped cross-engine memory/migration, bot/team+conversation exports, threads, sectioned settings, canonicalFlux key, namedClaude accounts, audio consent/transcription, avatars, profileCAS and file watches retain prior scoped passes. Recipient policy conflicting-sender tests integrated5cbc9df2. No new feature work needed. |

## Final source-matched artifacts: all built, signed where applicable, retained

All below are application source15c3cbd6. Older11549fff/f320/f164 artifacts are historical, not current release artifacts.

**Windows:** run34471681869 SUCCESS. Source/toolchain/build/final signature and helper-manifest checks passed. ValidFerrox signatures: app, recovery, Fuigo, updaterlauncher, installer. Installer364070696 bytes, SHA256 `46d67a86c3e5698c1a986b08ad31fd8ef262c8217aa7c1442b652097c925e5b7`. PortableZIP505125934 bytes, SHA `7c4df032cbcc6ef2a2e362f95011c16cb734137c1e49ab281f9a61d756cf8308`. Local binaries/evidence under `windows-signing-confirm-*` in the temporary root; native `candidate.json` binds payload hashes.

**Mac:** run34473310308 SUCCESS, workflow6dae8ec. Both architectures DeveloperID signed, all four artifacts notarized Accepted, stapled, blockmaps/feed regenerated. Native arm64 and Intel jobs both passed exact source/hash/architecture/codesign/stapler/Gatekeeper, actual window and graceful exit0/owned cleanup. All11 local manifest entries matched. arm64DMG538195576 bytes SHA `372fc718eb533e15d766be3295dd9477f43271e1662e535ba30292e7a01e26bf`; IntelDMG554127928 bytes SHA `d42fd9f1a188ed9b7768a1502e269971cc3a4f2fe0b86a061a86fef6807f20ab`. Cohort artifact10151403459 and local `private-cohort/.planning/cohort-mac-r1-artifacts`.

**Linux:** run34473310353 SUCCESS. Build, metadata/resources and five existing lanes passed: unpacked, AppImage, SIGTERM cleanup, X11 renderer/CUA crash-retry/cleanup, simulated Wayland firstpaint/failclosed. Installed-DEB lifecycle NOT claimed. DEB419263076 bytes SHA `5dfcad9a70ecdc40ff9e2916c82f1a95934ea4a17dc4f6cb595316d5e02cef44`; AppImage488391387 bytes SHA `349ca667c31616812407514974b7433075fe17fbaa373e32a6919896b7e563f1`. Both local hashes matched in `private-cohort/.planning/cohort-linux-r1-artifacts`.

**Assembly:** exact21public assets staged, seven feed entries validated with existing release.yml verifiers. `manifest.json` SHA `9e26631e0b04148a89f5edf263ba75877213e249562e86a1b19ccf6f1b795847`. Windows/Linux feeds generated with existing electron-builder26.15.3 writer and actual byte sizes/SHA512; AppImage embedded blockmap506079 validated. Mac feed/maps unchanged. Windows portableZIP must NOT enter public asset set: its basename collides with Mac-x64 ZIP. No missing release assets. `releaseReady=false` remains appropriate pending gates.

## Current Windows native P1/P2

Use the worker's current `windows-native/EXECUTION.md` and receipts, not its early historical invocation0 paragraphs.

Passed:
- Genuine Limited MurageTest token: elevation0, integrity8192, actual active console binding (session2 before reboot; re-query after reboot).
- Signed0.1.49 baseline installer8908 exited0.
- 0.1.50 upgrade installed and auto-launched exact signed app/Fuigo; same registration/path, preserved bot/marker, RX/no-write code ACL, bound server health, GPU without bypass, unchanged TEMP/TMP and normal close passed12:35:38.
- **Original upgrade installer exit code was missed.** Installer1284 exceeded600s observation; later observer attached after it exited. Do NOT infer0 from postconditions. This is the remaining distinct P2 gate.
- Uninstall7652 exit0 at12:38:44, code/registration removed, all8 data+67userData hashes identical.
- Reinstall9624 exit0 captured at12:49:06, exact signed app at fresh spaces path, auto-launch/health/GPU/Fuigo availability, normal close/environment preservation.
- Explicit fresh-profile launch/lease/canary/bound health and normal close12:51:36. Screenshot was display asleep; no visual pass inferred from black frame.
- Reboot requested12:51:48; firmware boot visibly observed later, desktop returned13:00:29. New boot identity, post-reboot app/visual and workload final status require latest worker record.

Post-reboot final receipt passed and current export is hash-verified. Pending at handoff: normal close of existing reboot app; installed bundled Fuigo useful fresh-file read→unpredictable marker/final, missing-read→handled final, held turn→cancel (exact source compiled bundle, max12 scripted calls, no real paid model); final workload evidence/lease cleanup; upgrade-exit proof or explicit disposition. The earlier P2 start window has expired.

The first P1 guest (child of emergency0149 overlay) crashed Windows0xC000021A BEFORE0.1.50 installation, serviced then failed admission. It was closed cleanly, preserved, and replaced with a fresh child of gracefully closed ap_r9lkd. This was environment evidence, not a0.1.50 defect. Current lease keeps the ORIGINAL deadline. Original/base/backing disks are read-only and must remain untouched.

## Verification history and failure boundaries

- Never reset counters by renaming worktrees/phases/accounts. Current P1 target upgrade was invoked once; uninstall/reinstall are distinct frozen P1 stages. P2 reserved for missing upgrade-exit observation only, under the time condition above.
- Recovered PowerShell script had malformed parameter/duplicated suffix from JS dollar-apostrophe replacement. Two exact fixture corrections; all13 scripts parsed and18 input hashes passed before first0.1.50 execution. No more ad-hoc reconstruction cycles.
- Keep launch-time native process handles through terminal exit. Null ExitCode and command exit0 are not child-process exit0. Boot request success is not reboot proof; use guest boot identity/events, not QEMU host uptime.
- Mac original f320 credential-drain timeout remains unexplained. Follow-up unchanged-app diagnostics found optional registration→safeStorage initialization pending then encryption rejection; two clean exits. Current signed native Mac checks also pass. This does not prove credential custody or retroactively fix the original timeout.
- Customer -32603/1073807364 reports remain unconfirmed. Do not mislabel the updater-token fixture correction or Claude notice fix as fixing those unrelated incidents.
- Missing source SHA caused WindowsP1 checkout failure; corrected source was pushed/API-verified before rebuild. Verify remote commit reachability BEFORE triggering artifact workflow.
- Signature validators must stay enforced. Recoveryhelper was truly unsigned; actual15c signing corrected it. No unsigned private bypass.
- Linux/mac evidence archive may contain case-colliding directories; preserve complete ZIP and selectively inspect rather than flatten/rename fixture contents.

## Resume sequence

1. Read this handoff, `windows-native/EXECUTION.md`, and current `launch.json`/`cleanup.json`. Check UTC time immediately. Revalidate the exact live guest/task/app/process handles before sending anything; never duplicate a running phase. If deadline passed, inspect watchdog cleanup; do not reuse stale PIDs or extend.
2. Recover accessible working directory. Do not use Mando cwd until read access is restored. Use current temporary source/records or fetch private branch `work/0150-continuation-record` if needed. Source15c is on `work/0150-integration`; leave dirty/inaccessible original worktrees alone.
3. Continue only the pending P1 post-reboot/workload/export/cleanup after verifying whether any already finished during account switch. Preserve passes. Do not launch P2 after cutoff without precise new approval; no silent evidence waiver.
4. Obtain explicit user disposition on missing upgrade exit (if not reproduced) and older engine/Mac limitations. Questions were asked but no answer observed. Actual failed installation/data/engine checks cannot be waived implicitly by urgency.
5. Once required gates are passed/disposed, review final release copy and README patches using latest receipts. Do NOT change artifact bytes/re-sign/rebuild merely for docs.
6. Follow the EXISTING guarded commands in release stage `PREFLIGHT.md`: `release-guard.mjs available`, guarded draft upload, exact21 uploaded digest verification, then guarded publish only after gate clearance. Fresh-check availability/permissions first; 403 is not absence. No clobber/delete/force tags. Old RELEASES_PAT403 is historical; local authenticated CLI may work, but read permission alone is not proof of writes.
7. After authorized publication, verify public inventory/feeds/stable aliases, apply accurate source/public README updates, preserve final evidence and clean only task-owned resources. Do not claim complete until actual outcome verified.

Useful read-only commands (use explicit cwd `/private/tmp`):
```sh
rtk proxy ssh -o BatchMode=yes -o ConnectTimeout=10 hetzner-dsm 'ps -p 605370,605336,605984 -o pid=,etime=,comm='
rtk proxy gh run view 34471681869 --repo FerroxLabs/murage --json status,conclusion
rtk proxy gh run view 34473310308 --repo FerroxLabs/murage --json status,conclusion
rtk proxy gh run view 34473310353 --repo FerroxLabs/murage --json status,conclusion
```

Do not create a fresh programme or repeat the long history. The immediate next action is the **live Windows handoff**, not more feature work.
