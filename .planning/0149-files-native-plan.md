# Remaining macOS Files native action gate — proposed

2026-09-10 explicit user-approved continuation, root assigned fuigo_110_bundle: preserve prior failure and original Files2/2 counters. Apply only established app.whenReady().then callback ordering plus sanitized before-ready/ready/preload/open/reveal receipts, then ONE unchanged native confirmation under Node24.20.0. Actual preload/verbatim handler/owner HTTP/hash/native shell assertions remain frozen. No production edits, owner documents closed, associations changed, HTML or live profile. Stop after failure with exact stage/remedy; dispatch and visual rendering remain separate.

Continuation PASS one invocation2026-09-10T02:05:48.002Z–02:05:51.224Z handle31890 exit0 Node24.20.0/Electron43.4.0/macOS. Same exact handler123d4196… and preloada1a697fc… hashes. Before-ready→ready61ms, actual preload ready, Open dispatched real shell.openPath returned empty error, Reveal dispatched real showItemInFolder; both exact immutable TXT SHA0114faf3…. Nativefixture screenshot viewed and confirms Reveal passed. TextEdit frontmost PID32205 observed, but editor document rendering remains UNVERIFIED; no claim from foreground alone. Full app startup/Windows/Linux/packaged app not covered. Evidence0149-files-native-1789005948002/receipt.json + native-receipt.json + screenshot/logs. Owned native/backend/renderer PIDs50530/50689–50692 absent; TCP19825/19826/56230 have no listeners. Original foreground restored true, fixture window closed, capability file removed. Benign document/private profiles retained, no existing editor/Finder documents closed. Callback-only correction supports readiness hypothesis; does not retroactively prove original stall cause. One explicit extension consumed, no further native reruns. Original core/browser2/2 and earlier failed receipt preserved. No production source changes.

Read-only preparation only; no native action run. Parent STATE.md owns budget/acceptance. Files core/browser/MCP2/2 remain accepted and consumed; this targets the explicitly outstanding OS action acceptance. Root must confirm one native invocation and this evidence boundary before execution. No new product edits; own new fixture/evidence only.

Root approved one bounded native invocation with exactly the source-extracted-handler boundary below. Actual preload, exact handler body, real owner HTTP, real shell call-through and immutableTXT are mandatory. Validate extraction boundaries/parse before invoking Electron; no native retry. Native dispatch and visualFileOpened are separate outcomes. Do not close another user's app/document; preserve benign temporary report/profile if task-only document cleanup is not safely established. Restore previous foreground only after matching original PID/bundle. Implementation prepared as new proof/child files; no product changes.

Existing receipts: outer swarm-0149-files/.planning/0149-files-record.md finalR2 records19focused/types/6browser PASS, actual MCP/Chat/Inbox/snapshot/restart/task deletion coverage, and explicitly leaves native macOS/Windows/Linux Open/Reveal untested. Integration electron/artifact-action.node-test.mjs supplies hash/path/symlink proof only. Reuse all of it; no browsercore rerun.

Actual seam: electron/preload.cjs exposes artifactAction(id,action) through ipcRenderer.invoke. electron/main.mjs desktop:artifact-action handler checks actual BrowserWindow/mainFrame/origin+desktop secret, fetches real owner-only /api/artifacts/:id/native, calls verifiedArtifactNativePath against immutable managed bytes, then real Electron shell.openPath or showItemInFolder. A .txt file avoids activeHTML warning/browser execution and all executable formats.

Proposed command after confirmation: `rtk proxy node --experimental-strip-types .planning/0149-files-native-proof.ts`. Use existing launchVerificationServer for a fake-only isolated backend; register one newly generated benign report.txt through real owner HTTP in a task-owned workspace. Spawn repository Electron binary with new private userData/home and actual preload.cjs. Install the exact unmodified IPC registration block extracted from current electron/main.mjs (hash it in receipt), supplying real ipcMain, BrowserWindow, app, fetch, fs/path, verifiedArtifactNativePath and the fixture-owned origin/data/secret. This avoids importing unrelated whole-main startup, credentials/updater/companion state. No mocked shell success: observer wrappers must invoke original bound shell.openPath/showItemInFolder functions.

Frozen assertions: real renderer bridge exists; Open and Reveal invoke the actual IPC callback with artifact ID only; real server returns immutable snapshot metadata; verified path/hash matches registered benign bytes; native openPath resolves empty error; actual showItemInFolder returns after system dispatch; both observed original shell calls use the exact immutable snapshot, not original workspace file; native child identity/version and actual preload/handler hashes retained. Native app/OS observations distinguish accepted system dispatch from verified editor rendering. No claim of document rendering based only on openPath success. Capture fixture window/results; if inspecting OS foreground/association, use PID/bundle identity and restore original foreground. Close only task-owned report/document/window; never quit an existing user app or alter default file associations.

Cleanup: exact fixture server/Electron/child handles and owned document only, preserve receipt/logs/screenshots before removing task-owned profile. Any native failure stops the single invocation; no retry, broad audit, customer profile, owner keys, paid provider call, HTML, executable or active content. macOS only; no Windows/Linux or final packaged-app claim.

Evidence boundary requiring root confirmation: the actual handler body is installed verbatim in a native Electron fixture with real preload/HTTP/native shell, but full electron/main.mjs app startup is not exercised. If full-entry/private-packaged behavior is required, use a current-source private app artifact and its existing MURAGE_USER_DATA/MURAGE_DATA_DIR isolation instead; do not describe source-extracted-handler proof as a packaged app pass.

## Single approved invocation — FAILED, no OS proof

Preflight passed: Node syntax checks for proof.ts/child.mjs and `proof.ts --prepare-only` confirmed one exact registration block with valid start/end/parse. Handler SHA256123d41962c9c32dda93c67069b470ad77f227f8367d67b3a84b6e3a21d011cac; preload SHA256a1a697fcefba73e74ab8d7697507f12fd53e337dfac943c563e386dddd91d4eb. No native process was launched during preflight.

The single native command ran2026-09-09T10:58:20.347Z–10:59:09.084Z, exec handle48308 completed exit1. Native child40084 hit its45second timeout without writing native-receipt.json or stdout/stderr. Backend40065 registered benign TXT artifactb8701c57-2298-4c45-8a3e-8e6b401a769f and exact immutable saved SHA2560114faf33425e0b230cea4f98ce74ad0c27585d5cfb9ad68da0ee017d963881f. These backend checks do not prove preload invocation, OS dispatch or visible file opening. Both Open/Reveal remain UNVERIFIED; no native success is claimed.

Receipt/logs preserved in `.planning/0149-files-native-1788951500347/`. Failed fixture source preserved unchanged. Owned Electron/backend exited, UI listener closed, private-fixture.json capability file removed. Exact PID40084/40065 checks returned no processes; exact TCP20325/20326/50779 listener checks returned no listeners. Task-root/child-script process scan also found no survivors. Original foreground PID/bundle restored and verified. No existing editor/Finder document was closed. Benign original/snapshot retained in `/var/folders/8h/chxws5390hx17g49p75b2xn00000gn/T/murage-verify-data-PiG2u4`; native temporary userData root retained in `/var/folders/8h/chxws5390hx17g49p75b2xn00000gn/T/murage-files-native-T5Ljpc` for evidence. No user data removed.

Source-based hypothesis, NOT confirmed cause: the new native child uses top-level `await app.whenReady()`, while established scripts/testing/background-native-fixture.mjs uses `void app.whenReady().then(async()=>...)`. A main ES-module readiness dependency could stall module completion; the timeout has no stage receipt to discriminate that from another pre-receipt startup failure. Electron's official [ESM guidance](https://www.electronjs.org/docs/latest/tutorial/esm) describes asynchronous module loading/readiness ordering and uses app.whenReady().then; its [app API](https://www.electronjs.org/docs/latest/api/app) places ready after the first main event-loop tick. This supports choosing the established callback pattern, not claiming a proven root cause.

Specific bounded correction proposed for a future recorded extension: replace only the child top-level readiness await with the existing callback pattern, add a sanitized stage receipt before readiness and after ready/preload/dispatch so any failure is classified, preserve current exact source handler/native assertions and all failed receipts, then execute once if root records authority. No correction applied and no rerun performed here. Stop this cycle: one approved native invocation consumed; original Files2/2 core/browser counters remain unchanged. Only new fixture/evidence files changed; no production integration rollback is needed.
