# N4 memory import and management package

Contract: extend the existing Murage memory importer and owner panel per the 2026-09-09 amendment in the root MURAGE-MEMORY-BUILD-PLAN.md. Automatically migrate only detected Murage-owned bot notebooks/topics in active mode, with at most four source attempts per startup batch, restart-safe hashes/provenance, private audience and tombstone/conflict/stop-tracking preservation. Explicit enable-and-import handles historical Off; keep optional extractor/model download unchanged. Replace accumulating record cards with 50-item pages and Search/Important/Recent/Needs review views, source/date/audience detail and truthful workspace/keyword-search status. Reuse existing index and controls.

Ownership: server/memory/import.ts/settings.ts and directly related tests; MemorySettings/MemoryReview and related picker/tests. Root owns database/shared schemas, server/index activation/routes, ChatView/Sidebar/top-bar wiring, compatibility retirement and cross-engine integration. Exclude global stores, live migration, new backend/cloud, native memory deletion and paused stress qualification. No competing plan.

Frozen checks: focused import/owner settings/authority/UI helper tests; client and server typechecks; isolated Playwright memory fixture, 3,000 synthetic records, light/dark at desktop 1440px and narrow 390px with page/search/important/review controls. Existing full P08 browser cases reused where behavior unchanged. No production profile/provider. Maximum two rounds, counters 0/2 and corrections 0/2. Stop with scoped evidence and committed changes, or an explicit remaining acceptance failure and bounded remedy. Root retains programme integration gates.

Inspection: current notebook importer already hashes/deduplicates, preserves originals and tracks source conflicts. Existing owner list scans text and lets scopeId override botId; existing UI accumulates all loaded pages. Default fresh active mode already exists and is not rebuilt. Existing workspace notebook reader/writer compatibility remains root-owned.

Implementation accepted in round 1/2 (corrections 0/2): migration helper attempts four files for one bot per call, returns nextCursor, skips template-only notes, refuses symlink reads, respects inactive modes/revoked tracking/tombstones and records conflicts. The real correctMemory version path is now recognized as an owner decision during notebook sync. Owner lists reuse the existing FTS database, hydrate authoritative rows and explicitly label source-text fallback while projection is pending; 50-record pages, Important/Recent/Needs review, per-bot audience restriction, paged notebook conflicts and explicit enable-and-import are implemented. Original notebook writes/readers remain unchanged.

Evidence (macOS, isolated data): focused `pnpm exec vitest run server/memory/notebook-continuity.test.ts server/memory/p08.test.ts server/memory/owner-list.test.ts src/components/MemorySettings.test.ts src/components/MemoryReview.test.ts` passed 33 tests / 5 files (10.75s). `pnpm exec tsc -b --pretty false` and `pnpm exec tsc -p tsconfig.server.json --noEmit --pretty false` passed. `pnpm exec playwright test --config src/e2e/memory.config.ts` passed all 8 cases (1.8m): existing owner review/correct/share/pin/forget recall and import tracking plus 3000-record paging/Important/review at 1440px and 390px in light/dark. Screenshots viewed: memory-settings-desktop.png plus paged-memory-dark-desktop.png, paged-memory-light-mobile.png and paged-memory-dark-mobile.png under this worktree's .planning/memory-evidence/p08-browser-results. Those screenshots are retained as untracked evidence, not shipped code.

Integration dependency: root owns the exact `export const MEMORY_SEED` seam in server/workspace.ts; this worktree applies it for verification but excludes it from the agent commit. Root must integrate that export alongside the import helper, call migrateDetectedMemoryNotebooks(roster,cursor?) on its bounded startup/timer path, and mount MemorySettings from the top bar with a profile summary/link. The helper is active-mode only; historical Off changes only through the owner action. Driver runtime/engine-switch/restart/native packaging and duplicate legacy-reader retirement remain root-owned programme checks. No claim of live migration, released binary proof, or stress qualification. Model download/extractor remain separately selected. Task dependency symlink removed after checks; its target is preserved.
