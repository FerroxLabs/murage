# Claude repeated-turn broker diagnostic

CURRENT: diagnostic complete, NO REPRO in frozen current-tree broker sequence. R2 selected test PASS (1 selected,79 skipped;1.477s test/3.70s run) and servertypes PASS exit0 on macOS with Node24.20.0. Explicit cleanup assertions prove both fake child PIDs exited before temporary gate removal. R1 behavioral pass retained; its new-test type/cleanup errors corrected in one supported test/fixture correction. R1/R2 both consumed; no third round or product edits. Scoped test/fixture/evidence commit authorized on work/0150-claude-multiturn-diagnostic based5cbc9df2; parent integration pending.

Root compared published978cd221 to current claude.ts: retained broker/session reuse, pause, and argsKey rotation are unchanged; relevant intervening changes concern accounts/auth. This regression therefore does NOT imply the old published broker defect was fixed in current code. Incident remains unresolved. Next needed evidence is actual native ordering of result, permission request, and unauthorized events, plus installed version/timestamp. No paid or live turn is authorized for this package.

Result: actual local broker correctly restores active authority for Bash/WebSearch on a compatible second turn, tags requests with the new turn ID, denies idle/stale connections, rotates fake agents/Composio config into a replacement process, and serves NEW socket connections after delayed old-child close. Six permission allows and seven idle/stale denials across three completed turns asserted. The customer symptom is not fixed or explained by this pass: real Claude behavior, installed app version, agents/Composio HTTP auth and raw failure transcript were not exercised. Customer version and exact failing-tool logs are required to compare the installed path with this current source. Committed logs: r1-test-compact, r1-types, r2-test; successful r2-types emitted no output, process handle46341 exited0. Full verbose r1-test log retained locally. No live provider, owner data or credentials used.

Outcome: reproduce or bound the reported repeated-turn `Murage: the turn ended` failure against current integration5cbc9df2. Scope: server/drivers/claude.test.ts and a minimal deterministic fake-CLI exit gate only. No production edits, real CLI/provider/tool execution, credentials, owner profiles or customer-fix claim. Raw customer logs/version unavailable. This subordinate package does not restart previous capped MCP checks.

Frozen fixture/acceptance: real local permission broker, fake stream-json CLI. First happy turn completes; its retained connection denies idle Bash/WebSearch asks. Second compatible held turn must reuse the PID and accept both tools with NEW turn IDs. Finish through existing per-PID gate. Third held turn rotates fake agents/Composio capability config and must use a fresh PID/broker while old connection denies. Delayed old-process EOF/close is released only after the new broker accepts requests; a fresh connection and both tools must still work afterward. Final settle restores idle denial. This proves broker lifecycle/config handoff only, not actual agents/Composio HTTP authorization.

Frozen checks: Node24 vitest run server/drivers/claude.test.ts -t 'keeps real broker authority across retained turns and rotated process closure', plus Node24 tsc -p tsconfig.server.json. R2 only for a supported correction; reuse unaffected passes. Two rounds maximum; no full suite. Use isolated setup.ts data and instance environment for fake controls, close test sockets and release owned gates on exit. Both rounds finished, one supported correction.

H1: retained broker remains closed/inactive on second turn; current code pause only drains and reuse installs live.turn, so evidence does not yet support it. H2: old child closure disrupts the rotated broker; current closeSession detaches old broker before replacement, requiring a deterministic delayed-close check. H3: customer unauthorized MCP transport has a separate lifecycle/version cause outside this broker-only test. Stop after evidence/classification and report no-repro if all frozen checks pass.

R1: selected regression passed (1 selected,79 skipped;4.317s), disproving H1/H2 for this local fake-CLI sequence. Types failed TS2339 at new test1035: find predicate did not narrow RuntimeEvent.sessionId. Fixture cleanup also raced afterEach removing scratch before final fake child's exit-gate read; verified orphan33567 belonged to this exact worktree and terminated it. One supported test/fixture correction: narrow session event explicitly and await disposed child exits while gate files still exist. No production changes. R2 will confirm corrected selected test+types, including explicit PID cleanup assertions; no further rounds.
