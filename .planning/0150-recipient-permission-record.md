# 0150 recipient permission combined gate

Baseline 5e88cd39, isolated worktree work/0150-recipient-permission. Outcome: demonstrate that a real queued delegation dispatches the recipient's turn and that its permission request obeys recipient settings despite conflicting sender settings. Own only the new index.test.ts cases and this subordinate record; root owns integration. No production changes, live engines/accounts, native platform checks, broad suites or adjacent cleanup.

Fixture protocol established before edits: index.test.ts starts the real server in a temporary HOME with the repository fake Claude and inert other engine slots; fake engine mode hang exposes its mounted MCP configuration. A valid active source capability calls delegate-bot, then the fake source's per-PID finish file emits natural turn.completed to drain the queue. The new recipient process dump must identify the recipient and its thread. The recipient's actual mounted permission socket accepts newline JSON {t:ask,id,tool,input}; the real driver emits request.opened and returns {t:answer,id,behavior}. Read request input is synthetic and is never executed. Review off, empty alwaysAllow, peer review off and computer off remove unrelated gates. Both bots use distinct fixture-owned working folders.

Frozen acceptance: two inverse autoApprove conflicts (sender true/recipient false and sender false/recipient true). Delegation must queue and start recipient; manual recipient must hold its own card with no broker answer until owner denies it; auto recipient must receive allow and its own auto-approved activity. Neither case may create the request card/activity on sender thread. Assert source settled/recipient active and recipient mount identity. Fixture teardown stops the owned server and removes its temporary home using existing awaited cleanup.

R1 checks: pnpm exec vitest run server/index.test.ts -t 'N6 delegated recipient permission' --reporter=verbose; pnpm exec tsc -p tsconfig.server.json; git diff --check. Maximum two rounds, R1 not run, R2 only eligible failure correction/confirmation. Existing adjacent counters/passes remain unchanged. No full index suite. Host macOS only, fake-provider runtime evidence; no real provider/native OS or release claim. Stop on accepted results or exhausted eligible correction budget; root integrates accepted diff.

R1 completed: Vitest session41762 exit1, 1 pass/1 fail/230 skipped,40.80s. `N6 delegated recipient permission allows recipient request despite sender manual mode` passed8.720s. `N6 delegated recipient permission holds recipient card despite sender Auto` hit the existing20s test timeout at20.208s, with no assertion-specific failure. Typecheck session21442 exit0; diffcheck exit0. No product defect established. Existing fixture afterAll removed temporary runtime data; initial case had no step trace, so exact stalled operation is unknown. No timeout increase or weakened assertions. R2 not yet run, no correction applied. Root notified of proposed bounded diagnostic confirmation; passing inverse remains valid.

Root explicitly authorized R2 manual-only diagnostic confirmation with unchanged20s/all assertions; stage timestamps only, no product correction, no third run. Typecheck and inverse pass retained. R2 command: pnpm exec vitest run server/index.test.ts -t 'N6 delegated recipient permission.*holds recipient card' --reporter=verbose. stdout/stderr retained in .planning/0150-recipient-permission-r2.log. Limit remains2/2 once run, not a reset.

R2 ACCEPTED by root: session18782 terminal exit0, manual case1/1 PASS8.412s (231 skipped; suite15.40s). Recipient mount identified8.278s; source settled/recipient active confirmed; real recipient card held with no broker answer8.296s; owner denial accepted and broker deny received8.305s; sender isolation/all permission assertions passed8.308s; case cleanup complete8.400s. Persistent log contains the stage timestamps and terminal result. Reuse R1 inverse PASS8.720s and server typecheck exit0. Combined gate2/2 consumed, diagnostic confirmation authorized within R2, no production correction or timeout/assertion changes. Original R1 timeout remains unexplained and is recorded as a fixture/runtime timing limitation; no product cause inferred.

Final status: implemented and verified isolated combined seam; root accepted and owns integration. Both autoApprove conflict directions now have runtime fake-provider evidence through actual delegate queue drain, startTurn and recipient permission broker. Live providers, native platforms and release readiness are outside this result. No further tests or background critique: explicit bounded root instruction overrides ijfw-commit's optional workflow expansion. Commit contains test cases, this record and safe persistent R2 log. All test handles terminal and existing fixture afterAll completed awaited server/temp-home cleanup. Worktree/dependency symlink retained for root integration and task-owned cleanup.
