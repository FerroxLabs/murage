# Independent updater integration candidate — scoped macOS acceptance

## 2026-09-10 authorized closeout

Parent STATE newest user override authorizes targeted observed-failure correction and confirmation for existing 0.1.50 packages beyond the historical caps. This supersedes the old wait-for-verification disposition below; all prior rounds, failures and receipts remain preserved. Own only the existing eight updater source/test files, narrowly necessary verification fixtures and this execution record in this isolated worktree. No new features, broader audit, owner profiles/keys, paid requests, publication or other worktrees.

Frozen outcome/checks: run Node24 focused native-helper/manager/general-manager tests and server/frontend typechecks; exercise current owner HTTP updater action routes, stale-selection/busy refusals and activation rollback; run pinned Fuigo1.0.10 through the production Darwin probe with a nonempty full ACP session, then isolated managed A→B→rollback A→use bundled, preserving prior executable bytes. Reuse prior UI and sandbox allow/deny receipts where unchanged. Classify product/fixture/environment failures, correct only observed acceptance failures and confirm affected checks. Windows/Linux and Darwin x64 remain required native gaps if unavailable, never complete by macOS arm64 proof. Stop at accepted scoped commit plus exact platform gaps or a concrete safety/authority/external blocker. Historical counters remain below/in the linked sibling record.

Closeout R1 completed: all named checks PASS, zero product corrections. Implementation commits ba16c0ef + ca190343 remain unchanged. No second check round was needed. Accepted only for the scoped macOS arm64 updater implementation and isolated activation/custody behavior; not all-platform or packaged UI acceptance.

- Node24.20.0 focused helper/Fuigo manager/general manager: 32 passed, 1 expected non-Darwin branch skip, 3 files passed (session83401). Receipt `updater-closeout-focused-r1.json`.
- Server `tsc -p tsconfig.server.json --pretty false` session35545 and frontend `tsc -b --pretty false` session19240 both exit0 without diagnostics. Explicit Node24 and repository TypeScript entrypoint; no full-suite claim.
- Production `probeNativeFuigo` on pinned native Fuigo1.0.10 passed ACP1, loadSession, synthetic authenticate and nonempty session/new. The default production probe also admitted version1.0.9 A, version1.0.10 B, rollback A and explicit bundled1.0.10. Actual native executable SHA256 values remained `753533a987637e5b249afca3a1f628a7af4d101f73431919d4b3e8f8500c282d` (A) and `10ef84065521868e5ae6122941c3318394b9eae4bcc7450cc61ea8ccd36097cd` (B/bundled), matching prior fixed registry pins. No injected probe in this run, no owner credentials or prompt. Session24090 exit0; receipt `updater-closeout-native-r1.json`. Selection callback in this native fixture persists owned temporary config; the real HTTP callback is separately exercised below.
- Real `server/index.ts` owner HTTP routes, actual withInstanceCli/save/load/reload callback: nine cases PASS. Check preserves custom CLI; ordinary install refuses custom takeover; strict action-body/content-type checks pass; use-managed A→update B→rollback A→use-bundled persists correct paths. Independent direct-thread activity with primary bot busy=false blocks downloading. Busy and stale guards hold after the probe and at the final root callback. Injected provider reload failure restores prior persisted CLI, clears providerConfigBusy, and a subsequent update succeeds. All prior managed/custom bytes remain unchanged. Engine fleet was explicitly disabled; fixed fake package bytes and an injected probe isolate this HTTP check. No native/full-provider reload-health claim from this fixture. Session82915 exit0; receipt `updater-closeout-http-r1.json`.
- Cleanup: native production probes return only after child/listener close. Both new task-owned temporary roots were removed after passed assertions. HTTP child90033 exited0; exact PID and ports21144/21145 follow-up found no process/listener. Earlier retained native1.0.10 source/root/receipts remain untouched. No live profile or publication.

Remaining requirements: Windows x64 and Linux x64 native isolation are explicitly UNQUALIFIED in production and their selection actions are refused. Both require implementation/qualification and native full-session plus activation/rollback evidence; simulated header tests do not close them. Darwin x64 has no native execution result. Combined packaged Electron/renderer integration remains a parent candidate gate; this closeout does not claim a fresh UI visual pass. Older UI and allow/deny sandbox receipts are retained as their original scoped evidence. Current UI source is identical to checkpoint eae7492f; its historical earlier hash differs, so no stronger UI provenance is inferred.

Next authorized action: root may integrate the accepted macOS code commits ba16c0ef/ca190343 with this closeout evidence, preserving the explicit other-platform refusal and open requirements. No push/publication was performed.

## Original checkpoint disposition (historical, superseded above)

Root authorized implementing the existing production delta and current integration seams, but no new verification run. Old updater checkpoint eae7492f9c00a8a0ecdcf74afc81ba56e10fa413 records the five unverified implementation/test files while preserving other preexisting uncommitted work. New isolated worktree is `.planning/swarm-0150-updater-integrated`, based on integration1a439db5cde3dc1f55be15782875788752e1b4bc; checkpoint cherry-picked asba16c0ef. Read-only comparison confirmed engine-management.ts and EngineManagement.tsx were identical between oldbase d9b70154 and integration1a439db5 before applying the delta. Current Flux, main, report and localisation changes remain based on1a439.

Implemented: Darwin syntheticmodel/loopback/sandbox/fullACP probe and cleanup; Windows/Linux explicit unqualified refusal; manager rawCLI capture/recheck/propagation; unsupported UI actions withheld. Rootindex callback now checks independent directRuns.forBot arrays and compares expectedCli while providerConfigBusy is held, before persistence. Existing withInstanceCli/save/load/reload rollback retained. Existing owner endpoint now dispatches check/install/update/use-managed/rollback/use-bundled. No renderer path/token parameters added. Test fixture and three-line preexisting general manager test adaptation transplanted without dropping prior checks.

Exact candidate source/test surface: server/fuigo-native-update.ts, server/engine-management.ts, server/index.ts, src/components/EngineManagement.tsx, server/fuigo-native-update.test.ts, server/engine-management-fuigo.test.ts, server/engine-management.test.ts, server/testing/fuigo-native-fixture.ts. No additional source feature or framework.

Historical evidence/counters remain in sibling `swarm-0149-fuigo-update/.planning/FUIGO-UPDATE-EXECUTION.md`. The accepted1.0.10 fixture demonstrated initialize/authenticate/fullsession under verified loopback-only sandbox, not this now-changed productionprobe or actual activation. Original2/2 plus diagnostic/synthetic/inert and the separately approved closeout are not reset. Windows/Linux and all-platform independent updater acceptance remain incomplete requirements, not an accepted substitute. Darwinx64 has no new native result either.

Current verification: only Node24 parser checks/source comparison allowed. No unit suite, typecheck, native engine invocation, updater activation, ownerprofile/servicekey access or provider request has run on this candidate. Do not label it accepted or merge it into release based on parser success.

Frozen proposed check set for root disposition: focused helper/manager tests plus server typecheck; one isolated owner-HTTP action/selection/busy/activation-failure/rollback fixture; one native1.0.10 check through the PRODUCTION probe and isolated A→B→A/use-bundled selection sequence. Require nonempty fullsession proof and unchanged prior selection/bytes on refusal; use fake provider and owned temporary data. No original UI/native cap rerun or broad suite. Root must allocate the remaining confirmation budget before any run; no extra attempts implied by this new worktree/checkpoint.

Next authorized action: hand off this unverified candidate and exact diff; await root verification disposition. No push/publication.
