# Single Flux entry closure

Source3194784a, isolated work/0150-flux-entrypoints. Outcome: every Flux-specific setup/Tools/default path navigates to the canonical Models card; no second Flux writer; non-Flux ambiguous keys and authentication retain current behaviour. Preserve onboarding state when Settings temporarily covers it. No redesign, provider credentials, paid calls, owner profile, push or publication.

Frozen checks: baseline ambiguity reproduction; Node24 focused FluxKeyCard/FluxInvite/PasteKeys/EngineSetup/shared flux-invite regressions plus new exact entrypoint tests; frontend tsc-b; existing onboarding-save browser cases where affected; one isolated source-rendered fixture with real Onboarding, EngineSetup, SettingsModal/Models and PasteKeys covering onboarding->Models->back state, Tools Flux navigation without implicit write/copy, nonFlux ambiguous choices, Models failure without second key field, desktop+narrow light/dark screenshots and keyboard focus. Fixtures explicitly provide empty workspace for onboarding; established Ember bootstraps must not be assumed empty. DOM-ready and named condition waits; no networkidle/SSE wait. Existing canonical backend flow evidence retained rather than rerunning provider/credential suite. Max2 rounds; corrections only within contract. Rounds0/2, corrections0.

Baseline reproduction before edits: modelProviderCandidates('sk-'+32 synthetic a characters) returns openai/openrouter/deepseek/mistral/flux; current candidates.includes('flux') wrongly redirects true. Recorded by direct Node24 import, no network/key use. UI skills already read: design-system (app tokens), React best practices (event handlers), webapp-testing, self-review. Reuse checked actual card appearance; verify changed entrypoint layout/interactions only, no broader Lighthouse audit.

Implemented pointer-only FluxKeyCard/EngineSetup; Models removes legacy Flux patch/input and always excludes Flux from generic writer while ambiguous nonFlux providers remain selectable; canonical status failure preserves labelled saved connections without a duplicate writer; Tools Flux row uses explicit Models navigation and never generic key writer; onboarding returns no overlay during Settings while component/selection remain; Flux invitation/avatar hint target Models. Added multi-paste preservation guard because switching sections unmounts PasteKeys: require explicit save/dismiss of other pending rows before navigating the selected Flux row; clicked Flux copy is then explicitly discarded without transport. No existing owner key/profile or paid request used.

R1: focused71/71 PASS, frontend tsc-b PASS. Browser9/10 PASS: new onboarding->Models->back, Tools singleFlux handoff, ambiguous nonFlux provider write + canonical status failure all passed; original Fuigo account/status and four onboarding profile cases passed. Existing generic engine fixture failed because newly mounted ClaudeAccountsSettings requested an unstubbed /api/claude-accounts and added a JSON parse alert; added exact neutral accounts[] fixture response, preserving original Terminal failure assertion. R2 corrected generic-engine test1/1 PASS; reused other passes. Playwright cleans its output directory between invocations, so required visual evidence was recaptured into separate output folders, not treated as a fresh package.

During final source check, the multi-paste unmount loss described above was found and corrected within no-key-loss acceptance. R2 affected PasteKeys25/25 and frontend tsc-b PASS; onboarding final visual/navigation1/1 PASS, light/dark390/1440 setup pointer screenshots inspected in .planning/flux-entrypoint-final-evidence/flux-entrypoints.human-onb-391f8-resumes-its-selected-engine/. Multi-paste browser case reached and passed the guard-warning assertion, then FAILED only because getByText('2 keys found',{exact:true}) did not match actual '2 keys found. Confirm each one.' (PasteKeys.tsx171). Later explicit Ignore->Models step was not reached. Screenshot/trace retained in .planning/flux-entrypoint-final-evidence/flux-entrypoints.human-Too-bb6d3--or-carrying-the-pasted-key/. No third round or silent selector relaxation.

Disposition BLOCKED pending one explicitly approved fixture confirmation. Exact bounded remedy: replace that text assertion with getByTestId('paste-key-row').toHaveCount(2), which asserts the required two retained rows directly; rerun only Tools Flux case using Node24 and a distinct output directory. No production defect is evidenced by this last failure. Candidate remains isolated and uncommitted; root may request exact extension, not integrate as accepted. Product/UI changes otherwise have preserved targeted passes; canonical live-provider/native readiness is not claimed.

User/root explicitly approved one FluxTools confirmation. Applied only stronger row-count selector and ran only Tools Flux case with Node24, separate .planning/flux-entrypoint-approved-confirmation output. FAIL before paste,45s: config fixture lacks box:{configured:false}; after Tools mounts, ApiKeys.tsx:19 reads c.box.configured and throws. Exact pageError/stack preserved in trace.zip (0-trace.trace). This explains blank prior failure screenshot; earlier Tools passage was timing-dependent against incomplete fixture and is not sufficient final evidence. No product ApiKeys source changed; no owner credentials or paid calls. The approved selector was not reached on this run. Original2rounds+oneexplicitextension consumed. No further run or commit. Next bounded remedy must complete required ConfigStatus fixture fields and assert no pageErrors before one Tools-only confirmation; root approval required. Cloud continuation remains owned by watch_state_close and was not touched.

2026-09-10 continuation: user "blitz all, no limits, no new features" explicitly extends the prior attempt limits for closing these existing gates. Prior rounds and failures above remain recorded. Scope is the already identified fixture precondition: use the actual ConfigStatus required fields (including box.configured, composio.configured, vps, rooms and localVm), assert zero page errors, retain the two-row preservation guard, then explicitly Ignore the Composio row and navigate to Models. No product changes planned. Frozen confirmation: Node24 Tools Flux browser case only, distinct evidence directory, inspect its narrow screenshot; reuse unaffected focused/unit/type/browser passes. Acceptance is full Tools journey with zero writes, retained pending row before explicit dismissal, empty canonical key editor and no copied Flux key. On pass commit the existing accepted entrypoint candidate and report SHA; on failure diagnose the specific failed gate before any further correction. No root integration, owner profile, paid call or publication.

Continuation confirmation ACCEPTED: Node v24.20.0, work/0150-flux-entrypoints, Tools Flux case 1/1 PASS (5.8s runner, 3.6s case). The two pending rows remain after blocked navigation; explicit Ignore of COMPOSIO_API_KEY leaves one row; Models opens with an empty Flux field, no key copied in DOM, zero API writes and zero page errors. Narrow screenshot tools-pending-keys-390.png inspected: both rows, the explicit actions and the preservation warning are visible without horizontal overflow. Evidence: .planning/flux-entrypoint-schema-confirmation/. Prior unit/type/onboarding passes remain valid because this continuation changes only the fixture schema and its assertions. git diff --check PASS. Fixture shut down normally; no owner resources touched. Existing entrypoint implementation now accepted for root integration. No provider/native/release claim. No background cross-model audit: the authorized confirmation is Tools-only and excludes paid calls or re-auditing accepted work.
