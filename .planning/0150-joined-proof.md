# Final joined report proof

Base655c1d4211157512b5941f3a5f8b7f5118025e49; isolated work/0150-joined-proof.
User/root grants one invocation only, no automatic retry or production change.
Own new candidate-report-integration fixture/config and evidence/this record.
Existing Files2/2, thread/Inbox/account/memory proofs remain retained, not rerun.

Frozen check: explicit Node24 version, then explicit Node24 Playwright CLI with
candidate-report-integration.config.ts (one desktop journey,retries0). Preconditions:
real launcher fake engine; two new empty tasks via HTTP201 (not intake); desktop
proof kept private; app readiness by controls, never networkidle/SSE. Select A,
send held fake turn with explicit A, register bytes through actual mounted MCP;
interrupt exact A; select B; Inbox→Files→source A original message; download hash;
same fixture restart→repeat route/hash/one result/source integrity. Synthetic
account/model only, no real credentials/model calls, native actions or review
preview access. Inspect pre/post screenshots and retain safe IDs/hash/PIDs.
Stop on first failed requirement, no rerun/counter reset. Rounds0,invocations0/1.

Invocation1/1 process71058 exit1, explicit Node24.20.0. Real app selected report
A, verified model, and sent explicit-thread held turn with202; actual MCP mount
was observed. FAILED before report write/register at textual workspace containment
assertion. No navigation/download/restart proof yet. Fixture teardown completed.
Trace/error-context retained under `.planning/candidate-report-integration-evidence/`.

Diagnosis: launcher dataDir uses `tmpdir()` alias `/var/folders/...`; server/config
canonicalizes DATA_DIR through dataDirLeasePaths and workspace.ts derives task
cwd from it. Read-only host check confirmed tmpdir realpath is `/private/var/...`.
Fixture compared noncanonical root with canonical task path. This supports a
fixture alias defect, not an escaped product workspace; exact failed path was not
logged. Proposed remedy: realpathSync BOTH existing root and task workspace, then
retain strict relative-path containment (reject parent/absolute escape), no
guard weakening. No correction or second invocation made; root disposition
required by one-invocation limit. No production files changed or owner data read.

Root now records latest user continuation as ONE correction-confirmation grant.
Canonicalize both existing directories; retain exact-parent, parent-prefix and
absolute-relative escape rejection. Same joined check only, explicit Node24,
no product changes/owner preview. Original invocation retained; extension0/1
before run, correction1. Stop on any further failure, no automatic retry.

Confirmation40732 exit1 (one test failed10.0s), extension1/1 consumed. Canonical
containment passed. Actual mounted MCP registered report, exact thread A and hash
passed; one Inbox result, selected B→Inbox→Files and byte-exact UI download passed.
Files source action returned to A (server thread and visible Report A heading),
but required original `[data-mid="80f1de35-9d58-456c-a71e-acdabb2e6a26"]` was absent.
Error snapshot shows “Saved file: Joined report” but no artifact card. Retained
trace HTTP response resource92d90aeba3d7c9968886977cbc1cebf2110cb4f8.json includes
that message ID and artifactIds[97828dea-2a7a-4520-87ff-7c47b08b7ba5], so source
identity is present in the server response. Renderer identity/card projection
versus fixture selector remains to diagnose; no established product correction.
Restart stage was NOT reached. Evidence retained separately under
`.planning/candidate-report-integration-confirmation/`; original failure retained.
No production edits or retry. Fixture teardown completed. Disposition BLOCKED at
exact-source rendering acceptance; root informed with bounded diagnosis needed.

2026-09-10 resumed closeout: user "blitz all, no limits, no new features" permits this existing targeted confirmation beyond recorded limits. Parent explicitly assigns integration of verified ChatView fix dd875828 (interrupted reply transient state) and completion of this same journey. Preserve original invocations and fixture correction above; no competing fixture processes are running. Scope: cherry-pick the already accepted product fix and its evidence into the isolated joined worktree, preserving its untracked joined fixture/record; run only candidate-report-integration with explicit Node24 and separate output. Retain strict containment, actual mounted MCP registration, exact source message/artifact IDs, byte hash, empty sibling/model preservation, one Inbox result and same-profile restart assertions. Reuse prior focused ChatView 3/3/type passes; no broad tests or owner/native/provider access. Capture and inspect pre/post source-card screenshots and safe IDs/hash/PID/log receipt. On pass commit joined fixture/config/record and report accepted SHA; failure receives exact diagnosis before correction.

Verified ChatView commit integrated cleanly as51ec7ecd; joined fixture/record preserved. Receipt source updated to this actual product candidate. The accepted report-render record establishes the same static VerificationServer type import pulls server-only ES2023 code into this ES2022 frontend project; current tsconfig includes this joined fixture. Before confirmation, replace only its type import with a local structural interface matching the actual launcher members it uses (info, fixtureDumpPath, restart, close), keeping runtime URL import unchanged. Add explicit Node24 frontend tsc-b to the frozen confirmation for this new fixture integration; no core suites rerun. This is the same evidenced fixture typing precondition, not a product change or new feature.

Integrated confirmation PASS 1/1 (13.1s case/22.0s runner) and frontend tsc-b PASS. Full source-card/download/restart assertions completed; pre/post source-report screenshots inspected in candidate-report-integrated-confirmation and both show the exact report card alongside the intentional interrupted-engine error. The list reporter does not persist the inline receipt attachment, leaving the promised safe IDs/hash/PIDs/log receipt unavailable on disk. Fixture-only evidence correction: write the same receipt JSON to test output and attach its path, preserving all assertions. Run only this same journey into a new candidate-report-receipt-confirmation directory to preserve the complete agreed receipt. Reuse the typecheck pass; no product or acceptance changes.

Final disposition ACCEPTED. Node24 single joined journey PASS1/1 (28.7s case/44.1s runner), all source identity/card, Inbox uniqueness, download hash, sibling task/model and same-profile restart assertions retained. Frontend tsc-b pass reused; git diff --check PASS. Final before/after screenshots inspected; the saved report card is visible next to the deliberately interrupted fake-engine activity error in both. Durable receipt saved in candidate-report-receipt-confirmation/candidate-report-integrati-50897-ad-and-same-profile-restart/joined-receipt.json. Source51ec7ecd; original message05c4117b-a16b-491d-b0c3-051307b045fd; artifactbce79f16-5942-443e-b502-9814ec4f3711; SHA2569959a8332ef78f238934f0fc21c9db73b45c6ac0903902aa2c22c755e0c61e75. Same isolated profile restarted from PID12479 to13410; both now exited. Receipt retains bot/thread IDs and both server log paths. Teardown stopped the mounted MCP child and Vite/server fixtures. No real model/provider, live profile, other platform or release claim. Root can integrate the joined fixture commit with the already accepted dd875828 product fix; no core rerun or background cross-model audit.
