# Upstream 0.1.59–0.1.60 runtime reconciliation

Contract: read-only comparison of the nine assigned upstream commits against Murage candidate c33860e9; classify bounded ports and architectural dependencies. Only this record is written. No implementation, tests, live calls, or claims of runtime verification. Main agent owns selection and integration.

| Commit | Disposition | Current evidence and minimal recommendation |
|---|---|---|
| dd7e4f16 | Applicable minimal port | `server/procs.ts:128` returns void from killCliTree; `server/drivers/codex.ts:294–309` emits completion/deletes active state before process shutdown. Retry at 657 and stopAll/dispose at 736 also do not await shutdown. Port bounded shutdown Promise and Codex settlement/retry ownership handling, adapting all existing killCliTree consumers whose declared void return would become incompatible. Files: `server/procs.ts`, `server/drivers/codex.ts`, focused tests `server/kill-tree.test.ts`, `server/drivers/codex.test.ts`, fake Codex fixture; inspect Antigravity and Claude call signatures as integration dependencies. Preserve local driver functionality. |
| b7133b3b | Applicable minimal port with dd7e4f16 | Current Codex stdout reader processes buffered lines after settlement; upstream checks both callback entry and each newline-loop iteration. Port both guards plus late-output fake/test in `server/drivers/codex.ts`, `server/drivers/codex.test.ts`, `server/testing/fake-codex-app-server.ts`. Must cover late lines in the same chunk as completion, not just a later callback. |
| 96574fd6 | Deferred architecture/compatibility decision | Actual defect exists: `server/drivers/codex.ts:627` concatenates bot system instructions into user input, and `server/drivers/codex.test.ts:126` explicitly expects it. But upstream bundles effective config/read in all modes, a persistent instruction receipt, experimental thread/inject_items on resumed sessions, instruction redaction, and removal of fresh-thread fallback. Current local driver lacks upstream named/custom approval-mode machinery and explicitly tests resume-failure fallback at test line 533. Do not cherry-pick. A selected port needs a separate frozen contract for minimum supported Codex protocol, preserving native configured instructions, first-adoption/update/removal semantics, native logs, and resume-failure behavior. Files: local Codex driver/tests/fake; upstream new `server/drivers/codex-instructions.ts` and `.test.ts`; upstream live script/docs only after adapting branding and fixture tooling. |
| 45cc2cf0 | Applicable minimal port | `server/composio.ts:1094` still loads a single 500-toolkit marketplace page; broker catalog also discards the caller cursor. Current connected-service pagination/tests (`server/composio.test.ts:445–486`) concern connected inventory, not marketplace completeness. Port bounded cursor traversal/deduplication, broker cursor validation/forwarding, and lazy icons. Files: `server/composio.ts`, `server/composio.test.ts`, `cloudflare/composio-broker/src/index.ts`, its test, `src/components/PluginsPanel.tsx:173`. Preserve current multi-account/session logic. Upstream catch still loses accumulated pages on thrown fetch/JSON errors; do not promise partial results for those cases unless deliberately included in selected acceptance. Broker code integration is distinct from broker deployment. |
| a9ec061c | Deferred architecture; feature absent | Local VM mounting currently exists only in direct turns (`server/index.ts:3302–3322`), while runGroupMemberTurn has no VM mount. Upstream depends on readyLocalVmForTurn, absent locally; local direct turns use containerComputerStatus. A safe room VM implementation must bring the follow-on fencing/cancellation cleanup as one package, not the initial mount alone. Files: `server/index.ts`, local capability authority, room/VM fixtures. |
| 27a3b622 | Deferred with a9ec061c | Upstream improves speaker/setup identity, VM lease-specific token checks, pre-dispatch cleanup and cancellation. Local capability authority is a dedicated `server/internal-capabilities.ts` registry, unlike upstream inline token map; direct application would collide with current authority design. Adapt lease identity checks at the local registry/caller boundary without replacing it. Upstream `server/group-local-vm.e2e.test.ts` and `server/testing/group-local-vm-hooks.mjs` are useful scenario references, not proof or drop-in local fixtures. |
| 0531f457 | Deferred with room VM package | Follow-up restricts watchdog VM grace cleanup to group-speaker claims; other hunk adjusts verification cancellation sequencing. Its referenced room cleanup and readyLocalVmForTurn machinery are absent locally, so no standalone patch is applicable. Include its ownership distinction and wait-for-mutation-before-cancellation scenario if room VM work is selected. |
| 01b972da | Applicable minimal port; native verification needed | `server/container-computer.ts:769,799` accepts exactly setgid/setuid and no SYS_CHROOT. Port Podman-only SYS_CHROOT addition and matching exact effective/bounding-cap validation with upstream focused cases in `server/container-computer.test.ts`. Keep Docker unchanged, preserve no-privileged/no-unconfined constraints. Live rootless Podman Firefox sandbox proof is required to claim the reported native defect fixed; local command-generation tests alone only verify configuration. Existing two-capability Podman containers become incompatible and require recreation, so record this behavior. |
| f73c8096 | Applicable minimal port; image/visual verification needed | Current `server/container-computer.ts:39` image layer is 4 and managed Dockerfile has no Noto CJK install. Port pinned font+license checksums, fc-cache before desktop startup, layer bump to 5, and focused Dockerfile test. Keep Murage repositories/labels unchanged. This intentionally invalidates prior managed images/containers; native rebuilt-image Japanese text rendering and recreation proof remain separate from source/unit checks. Do not copy upstream screenshots as local evidence. |

No assigned change is fully already covered. Connected-inventory pagination and generation-bound internal claims are existing related protections, but do not establish marketplace paging or room VM authority.

Recommended bounded selection: Codex process lifetime plus late-output guards as one package; marketplace pagination as one package; Podman sandbox and Japanese-font changes only with explicit native/image verification disposition. Keep instruction-history and room-VM work separate because they introduce behavior/compatibility and authority dependencies, not isolated patch hunks.

Status: source comparison complete; verification rounds not started. Next action: main selects packages and records frozen acceptance/checks before implementation.

## Selected Codex package execution

Provenance: selected ports of dd7e4f16 and b7133b3b. Outcome: defer turn completion and retry until the owned app-server closes, retain ownership on a bounded shutdown timeout, and ignore output after settlement. Scope: `server/drivers/codex.ts`, `server/drivers/codex.test.ts`, necessary fake mode in `server/testing/fake-codex-app-server.ts`. Shared killCliTree API, approval protocol, native instruction history, and other drivers remain unchanged. The driver wraps existing tree termination in a memoized five-second close wait; interrupt/stopAll/dispose await it. Required checks: focused Codex test file, scoped lint, whitespace check. Two rounds maximum; no live app mutations.

- Implementation present; not accepted. Round 1 `pnpm exec vitest run server/drivers/codex.test.ts`: 36 passed, one new test failed because its predicate expected an agent-message itemId that canonical events omit. Corrected the predicate and assertion to canonical assistant text. Scoped `pnpm exec oxlint server/drivers/codex.ts server/drivers/codex.test.ts server/testing/fake-codex-app-server.ts` exited 0 with pre-existing warnings. `git diff --check` passed.
- Round 2 `pnpm exec vitest run server/drivers/codex.test.ts -t 'waits for child exit'`: one failed, 36 skipped. Required regression hit ENOENT reading its PID dump immediately after receiving assistant text: fake finishTurn writes that dump after emitting the message, so concurrent parent delivery may win. This is a test-fixture ordering failure; child-exit acceptance remains unverified. Unchanged prior 36-pass evidence remains valid but is not a full passing suite.
- BLOCKED at two-round limit. No further correction or rerun performed. Proposed next bounded correction, requiring root/user disposition under the shared budget: publish the fake PID dump before emitting the observed assistant event (or emit a dedicated ready notification after dump), then confirm the new regression. Keep candidate unintegrated until this check passes. Native real-Codex and other-platform behavior are not claimed.
