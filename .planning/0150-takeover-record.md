# Computer takeover with unavailable native browser

Contract: new reported regression, user explicitly authorizes diagnosis/fix. Isolated work/0150-takeover from78ca1662; own electron/main.mjs exact set-human-control branch, electron/browser-control-sync.test.mjs and this record. Keep retained browser/platform policy, IPC ownership and bot-wide gate; no live Box/provider/profile changes. Expected outcome: absent surface records native hold so Computer can acquire server lease; future surface inherits it before host exposure; release stays server-first. No browser-available claim.

Frozen checks: actual main-handler/startup source VM + real surface manager + transition helper tests for absent take, future/recreated hold before host startup, absent release, failed durable transitions retaining hold, existing surface and rejected foreign sender/destroyed owner/invalid id. Existing ComputerPanel ordering tests and browser host/surface suites verify retained safety behavior. Node24/macOS arm64; source/runtime fixture doubles are not native Electron, live Box or Windows proof. Main syntax and diff checks. Two rounds total: R1 before fix reproduction + planned suite; R2 after one eligible fix, only invalidated cases and required integration. Stop ACCEPTED for scoped source/component correction or BLOCKED with failed requirement; no third round.

Cause: main null-surface positive hold throws before server request. Introduced by9a3c97394f7ac1ae0ed69f4795a195e4200ea7a9 (#573); public signed source978cd221 has same branch at1639–1642. Windows intentionally lacks surface; other hosts require logs to explain startup null. Upstream22daae744d3176be3e0257f919528568e33c80c2 has no-op setter only becauseb3c2f74b6375eed347bbccd1803816b5ac6e857d removed Electron browser; not a compatible port. No wholesale upstream change.

R1 handle23097 exit1:98 tests,93passed/5failed,0skipped. Three failures reproduced null-surface throw (take, future hold, durable-failure case stopped before server); two startup-dependent cases also exposed fixture cross-realm Function identity: real manager rejects VM-created createView callback. Read failure output and factory precondition; wrap that callback in the manager's realm, preserving the production admission check. Existing suites passed. Receipt0150-takeover-r1.json. No native or customer-runtime conclusion.

One supported product correction: absent surface adds/removes validated bot id in existing browserControlHolds and acknowledges, without availability change. Existing startup replays holds synchronously before surface assignment/host await; normal event/ID validation and non-null branch unchanged. Rounds1/2, productcorrections1/2; R2 confirms affected file + existing ComputerPanel ordering, reuses unaffected host/surface passes, main syntax/diff. No further scope.

R2 ACCEPTED for scoped source/component fix:17/17PASS,0failed/skipped; receipt0150-takeover-r2.json. Reused81 unaffected browser-host/browser-surface passes fromR1. Node24 main syntax and git diff --check PASS. Actual handler records hold before server take, actual startup/real manager preserve hold before host publication and recreation; absent release/serverfailure/untrustedowner cases pass. Rounds2/2 consumed,1productcorrection plus identified fixture-realm adaptation. No third cycle. No native Electron UI, live Box/VNC or Windows end-to-end proof; client startup-null cause still needs host/log evidence unless Windows policy explains it. Candidate not integrated/released; root owns integration and customer-runtime follow-through. Task-owned node_modules symlink points to original assigned worktree dependencies; no dependencies altered.
