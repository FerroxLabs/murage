# N5 export closure proposal

Read-only reconciliation at integration `51808ffc`, 2026-09-09. Parent STATE.md remains the sole programme record; this is its bounded N5 evidence attachment. Outcome: identify remaining separate conversation Markdown action and imported-role display acceptance. Scope excludes format/security rewrites, automatic role grants, production edits, tests, commits and profile mutations. Acceptance for this lane is an exact file-level proposal with existing proof distinguished from pending checks. Exporter rounds remain 2/2 consumed; no additional execution/check round was run.

## Already present

- Contextual selected-bot/team ZIP-default export and role/group selection: `src/components/TeamExportDialog.tsx`; `src/components/Sidebar.tsx:2143` supplies selected group members or selected bot to `initialBotIds`.
- Source intent is carried through both ZIP import (`server/bot-package-import.ts:173`) and legacy Markdown package import (`server/index.ts:9336`), as `installedPackage.sourceRole/sourceTeam`. Existing package import remains inert; no new role restoration protocol is required.
- Imported-role display is already in integrated `src/components/SettingsPanel.tsx:454`: current role, imported role/team, matched/unmatched intent and no-permission-grant explanation. Existing `BotRoleControl` remains at line535 under Identity & instructions. `bot-settings-sections.ts:19` labels all four roles.
- Role UI prior proof belongs to `.planning/0149-bot-settings-record.md`: two focused unit tests, four real-controls browser light/dark desktop/narrow cases, typecheck passed in R1. `src/e2e/bot-settings.human.spec.ts:36` checks imported leader label and inactive intent, and line72 verifies unchanged role/grants. Reserved settings R2 must finish its App integration/collapsed appearance confirmation. Do not repeat it as an exporter round or duplicate the role UI.
- Exporter worker evidence: `.planning/0149-export-record.md` in swarm-0149-export records 63 focused tests, eight mocked-transport browser cases with inspected desktop/narrow light/dark screenshots, corrected fixture typing and R2 typecheck + 2 round-trip passes. Both rounds consumed. Real archive/preview/import/file transaction is proven, mocked browser transport is not live HTTP. Parent STATE separately records root role-aware Markdown/no-auto-promotion/API pass; this lane did not rerun or independently strengthen that receipt.
- Conversation export backend already exists at `server/index.ts:8741`: GET `/api/threads/:threadId/export?format=markdown`, scoped `mayReadThread`, `store.activePath(threadId)`, thread-specific title, attachment `.md`, human-readable text/activity/screen/options. Existing `server/index.test.ts:1612` contains transcript export coverage. Presence of a test is not a newly obtained pass.

## Truly missing / pending

1. No frontend caller/action for the existing conversation export endpoint was found in `src`. N5 requires a separately labeled action in the active conversation menu. The server endpoint and portable-bot package must stay distinct.
2. Imported-role implementation is present; settings integration confirmation remains pending under its existing R2 owner. Small evidenced copy mismatch: Overview says “Use the role control below” although the embedded modal puts that control in Identity & instructions. Proposed one-line correction: “Open Identity & instructions to assign a role explicitly.” Coordinate with settings owner, who must also update the exact-copy assertion at `src/e2e/bot-settings.human.spec.ts:38`; no role-policy changes.

## Minimal implementation after explicit extension

- `src/lib/conversation-export.ts` (new small helper): download the existing endpoint as Markdown; use `ensureDesktopSurfaceSecret` and `desktopSurfaceHeaders` like `src/lib/team-files.ts`, check HTTP success and Markdown content type, safe `.md` filename, object URL cleanup. Do not route through portable package helpers or imply archives include attachments/private memory. Preserve existing server rendering semantics.
- `src/components/TaskPicker.tsx`: shared current-task menu already used by bot and group conversations. Add one “Export conversation (Markdown)” action for its current `threadId`, with busy/error feedback. Capture the thread ID/title at activation; switching a task during download must not change its target. This avoids adding another always-visible narrow header button and covers bot/group task identity using the existing picker seam. Coordinate before editing because the threads lane owns unintegrated TaskPicker changes.
- `src/lib/conversation-export.test.ts` and isolated `src/e2e/conversation-export.human.spec.ts` plus dedicated config: bounded checks below. No server production edits expected; use current API. Settings copy adjustment belongs to settings owner, not this lane.

## Frozen proposed extension checks (not executed)

Request one explicit bounded continuation for conversation action implementation/verification; original exporter 2/2 remains recorded. Within that continuation, at most two rounds: R1 exact checks below; R2 only invalidated checks and integrated candidate confirmation. No broad exporter-suite rerun unless these changes invalidate its code.

1. Helper unit checks: exact encoded thread URL/auth headers, Markdown download identity and `.md` filename, non-2xx/wrong-content rejection, object URL cleanup. `pnpm exec vitest run src/lib/conversation-export.test.ts`; `pnpm typecheck`; `git diff --check`.
2. Isolated real-server browser fixture following `docs/verification/README.md`: two bot tasks plus a group task with distinct transcript markers and titles, known active branch plus an inactive-branch marker, no engines or live credentials. Use real authenticated download from the UI. Check selected task title/content, no other-task/inactive-branch markers, group sender content, switch-task target correctness, and failed download visible without false success. Inspect desktop/narrow light/dark menu screenshots, keyboard activation/focus, and label distinct from bot/team export. Command: `pnpm exec playwright test --config src/e2e/conversation-export.config.ts` after the fixture/config has been implemented and inspected to match these preconditions. No global harness or user app.
3. Reuse prior ZIP/package role/selection/privacy proof and settings R1. Consume settings owner's reserved R2 result for role-display integration; do not run it independently here. No native packaging, paid provider calls, or cross-platform runtime claims.

Disposition: proposal complete; conversation action unimplemented, original exporter checks capped, role display implemented with reserved settings confirmation pending. Next authorized action is parent scope/extension disposition plus TaskPicker/settings ownership coordination. No production files changed by this lane.

## Authorized conversation action implementation

Parent authorized this previously unimplemented subtask at999ba300 after threaded TaskPicker integration5c1164cf. Own TaskPicker.tsx, dedicated conversation-export helper and tests/isolated browser fixture only; parent owns routes/shared state/other integration. Outcome: explicitly labeled Markdown export for current bot/group task, including a single-task conversation, authenticated request, safe filename, pinned request identity, truthful pending/error/download-start feedback. Existing endpoint/active-path rendering and formats unchanged; no logs of transcripts/credentials, no package/history coupling, live profiles, provider calls or publishing. Old exporter2/2 remains consumed; this new action has two rounds,0used. Role settings acceptance is independent.

Frozen checks: `pnpm exec vitest run src/lib/conversation-export.test.ts src/components/TaskPicker.test.ts`; `pnpm typecheck`; `pnpm exec playwright test --config src/e2e/conversation-export.config.ts`; `git diff --check`. Browser fixture uses actual TaskPicker/GroupTaskPicker via real StoreProvider and isolated verification server seeded with synthetic task trees. Test real authenticated Markdown downloads, exact selected-task content and no sibling/inactive branch, single-task group export, pending task-switch identity, visible failure/no false success, desktop/narrow light/dark, keyboard action and opener focus. Fixture configuration/seed preconditions inspected before running; no global harness. R1 full frozen set, R2 only invalidated checks/required integrated confirmation. Stop accepted/blocked at the limit; reuse existing portable-export proof without rerun.

R1: helper/TaskPicker14/14PASS(session57277,2.51s), typecheckPASS(session67744), diffcheckPASS. Memoryworker confirmed launcher save coherent before browser start; public launch defaults preserved. Browser session93463 exit1:3/5PASS36.2s. Both desktop real bot/group downloads and pending task-switch/error case PASS; both narrow cases exported the correct authenticated Markdown but failed the viewport-bound check before screenshots/group completion. Change-induced menu positioning defect: absolute `right:0` anchors to wrapper right, while clamp calculation measured the smaller trigger button right; the isolated block wrapper makes these differ. Correction1 measures the actual wrapper anchoring box, retaining width/clamp and assertions. No endpoint/helper/selection changes. R2 confirms four width/skin bot/group cases after anchor correction and typecheck/diffcheck; reuse R1 pending-identity/error and14 focused passes. Exact browser command adds `--grep 'current bot/group' --output=.planning/conversation-export-browser-r2`. Two rounds maximum remains; no third attempt.

R2 ACCEPTED: browser session12255 exit0,4/4PASS18.0s; typecheck session49792 exit0; diffcheckPASS. All four desktop/narrow light/dark PNGs inspected in `.planning/conversation-export-browser-r2/`: export label/explanation/status legible, action within viewport, safe filename wraps, existing task/new-task controls retained. Real browser download contents prove selected visible branch without sibling/inactive canaries, authenticated GET, group sender and one-task group path; R1 retains pending task-switch identity and synthetic404/no-false-download check. Original portable package63/8 evidence reused, not rerun. New action2/2 rounds consumed,1correction; old exporter2/2 unchanged.

Implemented directly in shared integration: only TaskPicker production change plus dedicated helper. Existing semantic styles/design retained; browser guide shaped isolated real-server fixtures. No routes, schemas, role/settings/grants, logs, package formats or clipboard changed. Fixture temporary profiles/server/Vite closed by awaited cleanup. Screenshots/traces retained; no publication or provider calls. Limitations: component surface uses real StoreProvider/TaskPicker and live isolated endpoint, not a full App/native packaged-client test; Markdown keeps server's existing active-branch text representation and does not download attachment binaries. Parent owns any final combined app/native qualification. Passing source ready for scoped commit; no push or automatic cross-audit under frozen check set.
