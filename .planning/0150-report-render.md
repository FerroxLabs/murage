# Saved report transient-render correction

Base3194784a isolatedwork/0150-report-render. Own ChatView transient state plus
new focused component fixture/config/record. New observed product defect, not a
reset of the joined journey cap. No joined fixture invocation here.
Frozen checks: explicitNode24; R1 real ChatView with synthetic profile and artifact
metadata, fake browser clock: waiting→saved artifact→activity within520ms, and
waiting→artifact→threadB→A, require exact message marker/card after settling;
normal saved reply still uses520ms animation. R1 establishes before failure;
R2 only minimal product correction, same focused cases, client/server types.
No real engine turn, credentials, owner profile or full account/native checks.
Inspect scoped screenshot; no broad visual/style audit. Stop after2rounds or
2unsuccessfulcorrections. No push. Rounds0/2 corrections0/2.

R1 process86583 exit1: two before failures at exact marker assertion (activity
before520ms and A/B/A), normal animation control PASS; actual ChatView fixture
preconditions established. Minimal product correction clears transient state
before effect early return and includes thread identity in reset/dependencies.
R2 process76152 PASS3/3 in9.7s, Node24.20.0 explicit. All three screenshots inspected
in `.planning/report-render-confirmation/`: exact report card visible after
settling, activity error remains separate, normal control preserved. Server types
12345 PASS. Client types29235 FAIL: fixture static VerificationServer type import
pulls mcp-server into browser ES2022 and reports findLastIndex/implicit-any at657.
This is a new fixture typing issue, not product failure. Exact proposed patch:
remove `import type { VerificationServer } from "../../scripts/control-murage.ts"`;
replace it locally with `interface VerificationServer { info: { url: string;
dataDir: string }; close(): Promise<void> }`. These are the only members this
fixture uses; runtime dynamic URL import remains unchanged. Confirm only explicit
Node24 `node_modules/typescript/bin/tsc -b`; preserve server/browser passes. No
correction/rerun yet; root requested exact type-only confirmation disposition.
Rounds2/2, productcorrection1; joined cap unchanged/no joined run. Fixture
servers/temp data closed by teardown. Hold commit; qualification remains blocked.

Root records user “Ok keepgoing” as approval of this exact fixture-only type
correction and ONE explicit Node24 client tsc-b confirmation. Static type import
replaced with the proposed local structural interface; runtime URL import and
browser assertions unchanged. No global cap reset; existing3UI/server type passes
retained and joined test not rerun. Confirmation pending.

Confirmation97416 exit0: explicit Node24 client tsc-b PASS. ACCEPTED scoped
renderer defect with R1 two reproduced failures/normal control and R2 three
passing cases plus server types retained. Original2rounds and one explicit
fixture-type confirmation preserved; productcorrection1, fixturecorrection1.
Commit source/fixture/config/record and three inspected successful screenshots;
failure traces retained locally, not published. No end-to-end joined/restart,
native/platform or release claim. Coordinator integration next; no push.
