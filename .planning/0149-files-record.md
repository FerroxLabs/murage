# 0.1.49 Files

Outcome: durable verified deliverables from exact authorized bot/task workspace paths; explicit registration, no prose inference or directory scans. Immutable managed snapshots preserve report history after source overwrite/deletion. Registry links bot/thread/run and supplies friendly names, SQL search/page/type/date/bot/task filters, source missing/changed status, revalidated preview/download. Same artifact ID is shared by Files, Chat and Inbox. HTML sandbox has no scripts, app origin/storage/bridge or automatic network access; unsupported formats download. Manual relative-path registration supports engines without the agents MCP tool. Limits:25MiB/file,512MiB managed snapshot storage,2MiB preview; no automatic eviction.

Ownership: new server/artifacts.ts, shared/artifacts.ts, Files.tsx and focused unit/real-HTTP browser fixtures. Root owns authenticated register_artifact MCP tool/API/database/sidebar/chat/inbox/native bridge seams. Root supplies trusted bot/thread/run and pinned non-home workspace; client paths never select a root. Owner may access retained thread artifacts only through an existing authorized bot/root descriptor; deleted bots gain no resurrected authority. Initial public routes owner-only as root authorized; no remote native actions. No live profiles, home scan, cwd migration, watcher, broad file manager, public hosting, scripts execution or publication.

Checks frozen: native Node strip-types import of new backend module; focused Vitest artifact registration/snapshot/restart/dedup/authorization/path/symlink/overwrite/missing/tamper/quotas/filter/hash tests; pnpm typecheck; real HTTP Files browser fixture with actual HTML create→register→list→preview/download hash, sandbox hostile script/network/bridge probes, desktop/narrow light/dark; git diff --check. Round1 core/API/UI; Round2 reserved for root-integrated registration/routine/source-reference and native seams. No more than2 rounds; corrections/attempts recorded without resets. Source/current root integrity and unavailable native platforms are explicit limits, not inferred passes.

Source inspection: canonical tool-completed events contain no verified path. Root confirmed explicit register_artifact tool through existing authenticated agents-proxy/internal claims. No external-engine automatic capture claimed. Rounds0/2, corrections0. Next: snapshot registry and interfaces for root integration.

Round1 exact commands: `node --experimental-strip-types --input-type=module -e "await import('./server/artifacts.ts')"`; `pnpm exec vitest run server/artifacts.test.ts`; `pnpm typecheck`; `pnpm exec playwright test --config src/e2e/files.config.ts`; `git diff --check`. Browser fixture creates actual HTML in a temporary workspace and uses real HTTP adapter/SQLite/filesystem. It probes script/remote image/frame/meta refresh/link/CSS network attempts, parent bridge access and localStorage under an opaque sandbox, then verifies downloaded SHA256 and retained original report after source overwrite. No real external URLs are used; forbidden network probes target only fixture loopback /leak. Four standalone viewport/skin cases. Root-integrated tool/routine/Chat/Inbox/native and backup gates remain Round2 dependencies.

Backup inspection found artifact-files missing from installation-state-snapshot.ts DIRECTORY_COMPONENTS and installation-damaged-export.ts DIRECTORIES; root notified to add preservation and focused tests. Registry is in messages.db, already included. Native path returns only verified immutable snapshot bytes; public responses never include the path. HTML uses inert template sanitization plus restrictive CSP and sandbox, stripping navigation/active elements before rendering. No unsafe direct HTML response.

Round1 result: native strip-types import PASSED;8 backend tests PASSED (1.51s); typecheck PASSED;4 real-HTTP browser cases PASSED (49.3s); git diff --check PASSED. Darwin arm64/Node25.8.1. Four overview screenshots inspected: actual light/dark tokens, responsive filters/card controls, friendlynames and original-changed/saved-version feedback visible without horizontal overflow. HTML preview behavior/security/hash checks passed, but the initial overview captures put its content below the scrolled viewport; a second preview-focused screenshot step is prepared for Round2 (not rerun now). No product corrections; rounds1/2 consumed. Temp browser workspace/database and server cleaned; screenshots retained in `.planning/0149-files-browser-results/`.

Scoped core/API/UI behavior verified; full package remains PENDING ROOT INTEGRATION: explicit register_artifact actor binding, real routine→shared Chat/Inbox artifact reference, Files/toolbar entry, managed snapshot inclusion in backup/restore, native deliberate open/reveal and platform proof. Do not claim transparent capture across unsupported engines. Candidate is for integration before the reserved Round2, not independent release. Task-owned dependency symlink retained for Round2; no live file or credential changes.

Root granted exact UI integration ownership before Round2: FilesDialog/ArtifactCards, Sidebar Files event+Tools entry, ChatView artifact cards/working-folder Files control, client Message.artifactIds, and Inbox explicit-artifact projection/link. Implemented these in the same candidate without running Round2. Added initialArtifactId direct preview and metadata-only GET /api/artifacts/:id; describe checks registry/scope/file availability without payload allocation. Public cards still revalidate content only when preview/downloading; native action only exposes root's optional artifactAction(id,open|reveal), never an original path. New focused cases cover metadata/no-read and explicit-artifact Inbox grouping; ordinary text remains excluded.

Root backend/native/tool/backup commits are pending; requester will supply integrated head before remaining confirmation. Working-folder location is shown when known, but no arbitrary-path reveal was invented: existing new artifactAction reveals the immutable saved copy. A trusted bot/thread-ID original-folder reveal seam is still root-owned if required. Files and Inbox source navigation await actual task-switch response; artifact cards always resolve IDs through metadata API rather than infer files from prose. Round count remains1/2; no added audit or unrelated settings changes.
