# 0.1.51 engine incident investigation

Contract: diagnosis only against published 0.1.50 source 15c3cbd64056a7288777739f5dd244ca4365ea0b in sibling signing-fix. Explain what existing first-hand receipts establish for -32603 and unexpected 1073807364, inspect shared ACP/Fuigo lifecycle, and name the smallest missing evidence/reproduction. Exclude product/live changes, provider calls, process interruption, credentials, raw private prompts, broad database reads and parent computer-resource/Mac-shutdown lanes. Own this record only.

Acceptance/checks: read exact source identity and relevant RPC/cancel/close paths; inspect named prior native receipts/diagnostics; classify confirmed versus unproven customer causes; propose a bounded incident-specific next step. Read-only source/metadata investigation, no new fixture or test rounds authorized. Prior ACP cancellation 2/2 rounds and native caps stay consumed. Stop once supported classification and missing evidence are explicit. ijfw-debug hypothesis workflow used; no fix, revert or memory mutation applies to this diagnosis-only contract.

Initial hypotheses: H1 provider HTTP error wrapped by JSON-RPC -32603 (confirmed historical Finch HTTP402 report, need original bounded receipt); H2 requested cancellation explains Windows close code (client race fixed in published source, exact customer linkage absent); H3 unsolicited termination/provider error of different cause (requires incident timestamp, ACP method/result and process-close correlation).

Current: diagnosis complete within available evidence; original customer causes remain UNRESOLVED. No product changes, engine runs, provider calls, process interruption or verification-budget reset. Parent owns disposition and any subsequent implementation authority.

## Findings

1. Exact checkout HEAD verified 15c3cbd64056a7288777739f5dd244ca4365ea0b. Shared ACP `core.ts:636` correlates response ID with child-local pending method, retains numeric RPC code/data, then `core.ts:849` emits runtime.error with safe method/code/http details. `core.ts:46` allows only known method names and numeric HTTP/code facts. Fuigo uses this shared driver (`server/drivers/acp/fuigo.ts:255`). A bare -32603 therefore does not identify which operation/provider condition failed; the current release already supplies those distinguishing fields when present.

2. Historical STATE.md:895-901 records Finch on installed0.1.46 at2026-09-06T13:36:44.526Z: handshake succeeded and provider HTTP402 exhausted credits was nested inside -32603. The shipped classifier (`shared/provider-error.ts:25`, `core.ts:29`) recognizes this exact shape and provides credit recovery. This historical incident has a recorded cause, but its original native receipt was not located in the bounded current log sample; do not transfer that cause to the later Windows report. No credit/account action or paid retry performed.

3. Original Windows1073807364 cause remains unidentified. Current `core.ts:666` close handler checks `cancelRequested`; `core.ts:684` sets it before cancel/kill. `core.ts:472` sets settled before stopping the engine. Thus requested cancellation and ordinary post-result cleanup cannot emit the same unexpected-exit error through those paths in this source. A genuine unsolicited close remains a failed turn. Accepted 0150 cancellation record proves the former client race with fake ACP stdio (2/2 rounds,55/55 tests), not the original Windows cause.

4. First-hand native receipt `/Volumes/Mando/WaylandBots/murage-astra/.planning/windows-emergency-0149-guest/native-evidence/fuigo-tool-cases.json` confirms signedFuigo1.0.8 SHA2562f63a4ad768cfec7cf60cb6e334c8ced227a241937ec8d3a8c75ac4f153f7d21 on WindowsNode24.18.1. Interrupt requested13:56:49.989Z, turn cancelled49.996Z, kill requested49.996Z, childPID1592 closed50.019Z with code null/signalSIGTERM. Earlier failed-read tool recovered to successful final. Zero external model calls; scripted loopback fixture. This does not exercise1073807364. Preserve the prior PATH-empty/taskkill-fallback limitation; no rerun.

5. `server/procs.ts:128` uses Windows taskkill /PID /T /F and falls back to child.kill; POSIX uses SIGTERM to owned process group. `core.ts:666` receives only code, discarding close signal; protocol tee records ACP messages but not child start/close/termination-route metadata. Consequently protocol history alone cannot attribute an unexpected exit to cancellation, host shutdown, external killer or engine process. This is an evidenced diagnostic limitation, not proof of a new engine defect. Microsoft NTSTATUS specification maps0x40010004 to DBG_TERMINATE_PROCESS; the name is not a causal incident trace. Source: https://learn.microsoft.com/en-us/openspecs/windows_protocols/ms-erref/596a1078-e883-4972-9bbc-49e60bebca55 . No debugger/shutdown attribution inferred from the number.

6. Bounded native-log inspection used Node24.20.0 and source-defined `/Users/seandonahoe/.murage/native`, no database or credential reads. Initial filename/size metadata found87files; selected six newest tails (maximum1MiB each) plus11files last-modified2026-09-06. Extracted only timestamps, allowlisted source/method, numeric RPC/HTTP code and a credit-exhausted boolean; no raw messages/params/prompt or keys emitted. No numeric -32603 in these selected records. Two historical non-ACP error fields were excluded from this incident. Six tails were partial and files were live, so this is not proof of absence or complete history. The original Windows thread/timestamp is still missing. A metadata script initially used a missing spread Stats.mtime getter; corrected to mtimeMs, no data mutation or product failure.

## Smallest next action

First obtain the exact failing Windows thread/time and installed app/engine identity, then read only that thread's current+previous native and canonical event segments. Extract pending-ID/method, numeric RPC+HTTP status, cancellation sent, terminal ordering and retained/omitted-history markers; correlate existing Windows process/event metadata for the same time if present. No new provider turn is needed to classify an existing incident.

If retained logs lack process correlation, recommend one narrow instrumentation package (not implemented here): add sanitized spawn/close/stop-request metadata to the existing bounded native diagnostic stream, including turn correlation, PID, code, signal and enumerated termination reason. Preserve cancellation and unsolicited-failure semantics. Freeze fake-stdio regression plus Windows-native actual taskkill-path fixture separately before execution; the fixture needs a real ACP-held prompt, no external provider, taskkill available and exact cancel-versus-unsolicited ordering. Do not substitute another broad fresh-install/native happy-path run or upgrade the bundled engine without a causal defect.

No eligible product correction is established for the unexplained incidents by this lane. Original incident diagnosis is pending exact evidence; source/receipt classification is complete. Owned record is the sole write.
