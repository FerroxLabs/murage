# Startup and background — 0.1.49

Contract: explicit General → Startup & background controls, native tray Open/Inbox/Pause automations/Quit, close-to-background only with a live reopen surface, opt-in sign-in launch and quiet sign-in only with background enabled and a confirmed tray. Preserve unset Mac resident default; Windows/Linux default off. Explicit Off quits on last close across platforms. First-close explanation once; sleep/off does not imply running. Quit/updater/OS shutdown bypass hide and retain existing bounded cleanup/lease ownership. Pause admits no new scheduled/webhook work, preserves accepted queues/active limits, and does not block manual routines or paired owner channel requests.

Scope: background lifecycle/login/tray helpers + node tests; StartupSettings and scoped SettingsModal/preload/type/main wiring. Root owns Files IPC and final pause API/admission callbacks. Per-profile preference file lives under actual owned data directory, not shared global userData. No owner OS login modification during verification. Linux sign-in file generation is tested only under scratch autostart; real native OS integration limitations reported separately. No Core/auth changes, no old N7 rerun or counter reset.

Frozen checks: lifecycle/provider node tests (Mac/Windows/Linux fakes, live tray loss, close explanation, explicit quit/shutdown, login failures), pause-specific RoutineManager tests plus existing scheduler regression, client/server types, isolated StartupSettings browser controls in light/dark 1440px/390px, and an owned Electron/macOS fixture with native window/tray while login provider remains fake. Maximum two rounds; R2 now exhausted and classification BLOCKED on native tray/close proof. Main safe-quit/lease implementation remains intact; no real login/OS-session mutation proof inferred from mocks.

Evidence inputs: Electron official app docs constrain login-item API to macOS/Windows and native approval; Tray.getBounds is not Linux-supported. Linux tray readiness therefore probes StatusNotifierHost registration over session D-Bus and fails toward visibility. XDG autostart desktop-entry specification defines per-user entries and Exec escaping. Root approved scoped main/preload/types/SettingsModal and schedule/webhook-only pause callback changes; root will wire owner GET/POST /api/automation-admission {paused:boolean}.

Implementation candidate ready, not yet accepted. Main/preload/General controls and exact Sidebar Inbox listener are wired; settings persist under owned DATA_DIR/startup-background.json. Native login APIs are opt-in on Windows/primary Mac; secondary Mac explicitly unsupported rather than registering the wrong profile. Linux creates/removes only its marked per-profile XDG desktop entry; no host entry was modified by tests. Live tray loss reopens a hidden window; quiet launch requires confirmed tray and server readiness. Existing safe-quit path unchanged except the early hide-bypass flag. Startup rechecks cancellation after async discovery and preserves any window already opened from the tray.

R1: 12 lifecycle/login node tests passed; 60 scheduler/pause/StartupSettings tests passed (3 files, 13.14s). Main/preload syntax and client/server types passed. Pause callback gates schedule enqueue and schedule/webhook dispatch; manual routines/channel owner requests and active time limits remain available. Accepted webhook retries retain their original receipt. Existing interval coalescing prevents per-occurrence catch-up queues.

R1 UI/native fixtures failed before product exercise. The generated Vite bootstrap lacked one closing brace; its source is corrected and now syntax-checked before the fixture starts. Native fixture used top-level await app.whenReady, which prevents Electron ESM initialization from finishing before ready; corrected to the production .then pattern. The timed-out task-owned native process group was stopped by its 45s bound and temporary user data removed. Browser traces/screenshots preserved in .planning/background-first-attempt. No host login writes occurred. R2 not started, pending root pause endpoint/callback integration. A further node test covers disposal while tray discovery is pending. Native fixture uses a real isolated window/tray/preload and actual StartupSettings UI, but a fake login provider; receipt has a fresh run UUID. Actual OS sign-in registration remains unproven.

Sources: https://www.electronjs.org/docs/latest/api/app ; https://www.electronjs.org/docs/latest/tutorial/esm ; https://github.com/electron/electron/blob/main/docs/api/tray.md ; https://specifications.freedesktop.org/autostart/latest/ ; https://specifications.freedesktop.org/desktop-entry/latest/exec-variables.html . The running app is not a daemon and no sleep blocker was added.

## R2 integrated outcome — native gate blocked

Integrated root 0da7dde9 as merge 9ff84921; tree matched root after retaining its malformed-pause-body assertion and both Files/Inbox Sidebar listeners. Root pause callbacks/owner routes and generic-config no-reload allowance included. No N7 work was reopened.

Passed: `node --test electron/background-lifecycle.node-test.mjs electron/background-login.node-test.mjs` — 13/13. Prior unaffected scheduler/StartupSettings 60-test evidence retained. `pnpm exec vitest run server/background-admission-api.test.ts` — 1/1, 15.33s: owner-only admission API, malformed boolean rejection, manual fake-CLI PID survives generic pause config, and one automatic scheduled run resumes afterward. `node --check electron/main.mjs`, `node --check electron/preload.cjs`, client `tsc -b` and server `tsc -p tsconfig.server.json --noEmit` all passed. `pnpm exec playwright test --config src/e2e/startup.config.ts` — 2/2, 50.5s; light/dark desktop 1440px and narrow 390px controls, confirmed boolean changes and missing-tray visible-start copy. Viewed desktop light/narrow dark screenshots under .planning/background-evidence/browser.

Failed required native confirmation: `node scripts/verify-background-native.mjs` reached Electron ready and created the native fixture window, but scripts/testing/background-native-fixture.mjs:28 asserted trayAvailable true and received false at the first probe. It stopped before native UI/close/reopen/quit checks, so those claims remain unverified. The tracked app icon exists (63,399 bytes); raw native tray bounds/probe error were not captured, so asynchronous registration versus native host support is unresolved. No native success receipt was written. Node wrapper rejected the missing fresh receipt, exited 1, cleaned its temporary profile; pgrep confirmed no matching native fixture process remained. No real OS login settings were changed.

Disposition: BLOCKED for the agreed full background outcome, with UI/admission/helper subchecks passed. No third attempt, fixture changes or weakened tray assertion made. Specific bounded remedy for authorization: instrument the actual tray image/bounds/error, wait for confirmed native tray readiness through bounded lifecycle.refresh checks rather than assuming the first synchronous probe is ready, then run this exact owned native fixture once. If the host cannot expose a usable tray, retain that native-platform gate as explicitly deferred/unproven; do not hardcode availability. Windows/Linux native tray, sign-in registration and OS shutdown remain unproven; their mocked-provider evidence is separate.
