# Flux single-key backend package

Outcome: one authoritative Flux credential across chat, search, images and transcription, with explicit selection for differing existing keys. Scope: pure credential resolution/consolidation policy and focused regression tests; root owns persistence, HTTP/Electron seams, active-work fencing and assignment migration. Other providers and owner credentials are excluded. Never read owner data or call providers.

Acceptance: equal credentials can converge without losing identity; differing credentials refuse implicit selection; revisions fence stale choices; disabled credentials never become implicitly active; unrelated provider records survive. Existing IDs needing migration are returned explicitly so the caller can fence active work and remap assignments before persistence. Read-only resolution performs no migration.

Checks: Node pure-policy tests and existing affected Flux resolver Vitest tests, using fake strings only. Maximum two verification rounds, two corrections per issue. Stop after accepted helper tests and concrete root seam instructions, or report the unmet requirement. Integration/runtime/UI checks remain root-owned.

State: pure helper implemented and accepted; round 1/2 used, corrections 0/2. `node --test electron/flux-credential-policy.node-test.mjs electron/provider-connections.node-test.mjs` passed 12/12. No existing Flux resolver changed, so its Vitest checks are unaffected and remain integration-owned (isolated checkout has no node_modules). No runtime, provider, combined encrypted transaction or UI claim.

Integration contract: retain config.flux.apiKey as canonical, add encrypted desktop fluxApiKey slot (none exists currently), atomically mutate it plus modelProviderConnections and secretless aliases via updateSecureCredentialDocument. Caller validates snapshot revision, fences active work, commits all fields together, projects env/config, invalidates catalog listeners, and conditionally compensates the exact revision after a lost acknowledgement. Resolve historical IDs via resolveFluxAlias without listing aliases; preserve each enabled flag. Disconnect supplies blank canonical key and aliases resolve null. Compare ambient and persisted workspace credentials before precedence projection when both exist, because loadConfig currently overlays FLUX_API_KEY on disk and would otherwise hide differences.

Root seam notes: server/index.ts providerConnections legacy row at 411, bank transaction 11607, image resolver 1864 supports model:ID; shared modelSelection IDs in bots/tasks and routedProviderId hash make remapping unsafe. Existing search/voice/chat/avatar callers retain fluxKey() and need no independent key resolver. Root owns Electron/server persistence and alias resolver integration. No user credentials read, no external requests, no push/publication.

Root extended this package after helper checkpoint 722aa05a: backend worker now owns isolated shared/server/Electron integration. Canonical routes are GET /api/flux-connection, POST /api/flux-connection/mutate (dev), POST /api/flux-connection/test (catalog only), and privileged POST /api/flux-connection/replace with begin/commit/rollback/finish reservation. Desktop IPC is flux-connection:mutate. Precommit reservation expires after 30 seconds; committed reservation never expires blindly. Aliases preserve enabled flags and IDs, forwarding catalogs to legacy-flux without listing duplicate rows. Raw file and inherited ambient credentials become explicit choices; first explicit mutation establishes encrypted custody and managed environment precedence.

Reserved round 2 check set, frozen before execution: Node Flux policy/controller + existing provider custody + diagnostics tests; Vitest Flux transaction, Flux HTTP fixture, provider HTTP/service, Flux config, and redaction tests; frontend/server TypeScript checks; joined UI review against actual isolated runtime by UI worker. HTTP fixture must use only fake keys, owned temporary data and HTTPS interception. Do not run round 2 until UI card, wiring and bridge are joined. No third round; report unresolved requirements at limit. Unrelated watch-window fixture approval does not change this Flux package.

Round 2 backend classification: BLOCKED. UI commits 408c83df/2e30e525/d61842ff joined locally as 42069c11/677595a3/69a93533; backend edits remain isolated and uncommitted. Focused Vitest 8 files 71/71 passed, including actual isolated HTTP Flux/provider routes, alias catalogs, explicit raw-file/env/named conflict choice, conditional rollback and disconnect. Frontend tsc -b passed. Node policy/controller/custody 18 tests passed; diagnostics was incorrectly included under Node initially (invocation failure), then correctly run under Vitest: 49/50, failing credential-env parity. Server tsc failed TS2339 because IndependentThreadRuns has forBot(), not size.

Two change-induced acceptance blockers, no corrective reruns consumed: (1) replace directRuns.size in server/index.ts idle guard with store.bots.some(bot => directRuns.forBot(bot.id).length > 0); (2) add MURAGE_FLUX_CONNECTION_ALIASES beside ambient key in electron/diagnostics.mjs to match server env stripping. Bounded extension requested from root for these exact corrections plus server tsc and diagnostics only; retain all unaffected passes. No third verification round run. Actual joined UI review remains independently authorized and delegated to UI worker. No owner credentials/network inference used. The commit skill started its required background IJFW critique; generated .ijfw artifacts are not part of candidate changes.

Approved closeout extension: root relayed user's “just keep going stop having me prompt” as approval of the named corrections and one confirmation. Both exact product corrections applied; confirmation limited to explicit Node 24 server TypeScript and diagnostics. UI worker may correct only fixture onboarding precondition (the fixture already creates Ember) and run one joined UI confirmation. Prior 71/18/frontend passes retained; counters do not reset. No other product changes authorized by this extension.

Closeout backend confirmation: `/Users/seandonahoe/.nvm/versions/node/v24.20.0/bin/node --version` = v24.20.0. Explicit Node 24 `node_modules/typescript/bin/tsc -p tsconfig.server.json` exited 0. Explicit Node 24 `node_modules/vitest/vitest.mjs run electron/diagnostics.test.mjs` passed 51/51. Both named backend blockers resolved. UI one confirmation is in flight; do not call the complete package accepted until its result is recorded.

Final classification: ACCEPTED for the agreed isolated source/browser package. UI worker's single approved Node 24 confirmation exited 0: actual Models, saved named-only selection, canonical selection, catalog Test (2), Replace with cleared input, disconnect cancel/confirm, and Connect passed. Aliases/other provider retained, no key reflection, six light/dark 390/820/1440 screenshots inspected without overflow or JavaScript errors. External transport was limited to the intercepted catalog GET. Fixture evidence: .planning/flux-joined-ui-evidence/results.json; script .planning/flux-joined-ui-smoke.mjs. Owned browser/server closed. Packaged native Electron IPC has controller/source coverage but is not runtime-proven by this browser fixture. No publication or live key calls. Counters: two original rounds plus the one explicitly approved narrow closeout confirmation; no resets or further audits.
